# Блок 5 — Капитал, Цели и Сквозная приёмка (Capital, Goals, Acceptance) · сессии

**Цель блока:** Реализовать экран управления активами и кредитами `Capital.vue` (счета, кредиты, симулятор досрочного погашения), экраны целей `Goals.vue`, `GoalDetail.vue`, `Deposit.vue` (пополнение и снятие), заменить оставшиеся `PlaceholderView` в роутере и провести финальную сквозную приёмку E2E-сценариев миграции.

Сессии: исполнитель ✅ · критик ✅ · приёмка ⬜

---

## Промпт исполнителя — уровень build · Opus 5 · effort high

```
/worker migrate-go-vue 5
```

Блок-специфика:
- Среда: Node.js 20+, каталог `frontend/`. Стек: Vue 3 (Composition API, `<script setup>`), Vite, Tailwind CSS 4, Pinia, `@phosphor-icons/vue`, `vue-router`, `shadcn-vue` (`radix-vue`, стиль `new-york`, `neutral base`, утилита `cn()` из `@/lib/utils`).
- Факты Блоков 1, 2, 3 и 4:
  - Закрыты бэкенд, ядро математики слияния, экраны `Access.vue`, `Setup.vue`, `Overview.vue`, `Budget.vue`, `Ritual.vue`.
  - В роутере `frontend/src/router/index.ts` маршруты `/capital` и `/goals` временно рендерят `PlaceholderView.vue`.
  - Хранилище `useFinanceStore`: `accounts`, `credits`, `goals`, методы `mutateHouseholdDoc`, `mutatePrivateDoc`, `syncHousehold`, `syncPrivate`.
  - Все финансовые функции готовы в `frontend/src/lib/finance.ts`: `netWorth`, `prepayment`, `amortization`, `goalMonths`, `liquidCash`.
- Эталонная верстка экранов для портирования:
  - `src/screens/Capital.tsx` ➔ `frontend/src/views/Capital.vue`
  - `src/screens/Goals.tsx` ➔ `frontend/src/views/Goals.vue`
  - `src/screens/GoalDetail.tsx` ➔ `frontend/src/views/GoalDetail.vue`
  - `src/screens/Deposit.tsx` ➔ `frontend/src/views/Deposit.vue`
- Задачи по порядку: `MGV-12` ➔ `MGV-13` ➔ `MGV-14`.
- Заменить `PlaceholderView` на созданные экраны в роутере.
- Верификация: `cd frontend && npm run build && npm test`.

---

## Промпт критика — уровень review · Opus 5 · effort xhigh

```
/critic migrate-go-vue 5
```

Блок-специфика:
- Проверить расчет досрочного погашения кредитов (`prepayment`) и пересчет графиков платежей.
- Проверить приватность счетов (личные счета сохраняются только в `private_docs`, общие — в `household_docs`).
- Проверить операции по целям: пополнение (`Deposit.vue`), снятие, изменение параметров цели.
- Проверить сквозные E2E-тесты двух клиентов.

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh

```
/accept migrate-go-vue 5
```

Блок-специфика:
- Финальная сквозная приёмка всего мигрированного приложения (DoD миграции):
  - Регистрация двух пользователей в одном домохозяйстве.
  - Первоначальная настройка через `Setup.vue`.
  - Сводка `Overview.vue`, переход во все разделы (`Budget`, `Capital`, `Goals`, `Ritual`).
  - Проверка синхронизации через Go API и PostgreSQL бэкенд.
  - Полный прогон всех накопленных E2E-тестов (`npm test`).
  - Чистота консоли браузера и отсутствие ошибок сборки.

---

## Handoff (заполняет исполнитель)

### Выполненные задачи
1. **MGV-12:** Экран `Capital.vue` (коммит `3831db1`)
   - Реализована карточка совокупного капитала (`netWorth`) с разделением на общие счета домохозяйства и приватные счета авторизованного пользователя.
   - Список счетов с модалками добавления/редактирования, поддержкой валют (USD/EUR/RUB/KZT) и фиксацией курса.
   - Список кредитов с расчётом ежемесячных процентов (`debtCost`), симулятором досрочного погашения (`prepayment`), разового досрочного взноса (`lumpSum`) и оптимальной добавки (`halfOverpayExtra`).
   - Список обязательств с версионированием, запланированными изменениями и советами по долгам.
   - Набор компонентных тестов `Capital.test.ts` (6 тестов, все зеленые).
2. **MGV-13:** Экраны `Goals.vue`, `GoalDetail.vue`, `Deposit.vue` (коммит `2326dac`)
   - Экран `Goals.vue`: список целей с кольцом прогресса (`Ring.vue`), подсчёт непрерывной серии пополнений (`contributionStreak`), вкладка покупок/вишлиста (`liveWishlist`) с добавлением и отметкой покупки.
   - Экран `GoalDetail.vue`: детальный вид цели с интерактивным выбором срока/платежа, ползунком взноса, модалкой пополнения/снятия с опциональной привязкой к счёту списания/зачисления, историей движений и блоком финансовой дисциплины (`Callout`).
   - Экран `Deposit.vue` (`/capital/:id`): расчет сложного процента с капитализацией (`deposit`), эффективной годовой ставки и реальной доходности с поправкой на инфляцию (`realRate`).
   - Зарегистрированы все маршруты в `frontend/src/router/index.ts`.
   - Набор тестов `Goals.test.ts` (8 тестов, все зеленые).
3. **MGV-14:** Сквозная приёмка, E2E тесты двух клиентов (коммит `e21c96d`)
   - Реализован сквозной интеграционный E2E-тест двух клиентов в одном домохозяйстве (`frontend/e2e/two-clients-sync.test.ts`):
     - Авторизация двух клиентов (Ильяс — слот `a`, Аруна — слот `b`).
     - Совместная работа с общими счетами и целями, слияние изменений.
     - Строгая изоляция личных кошельков (`private_docs`): приватные счета видны только владельцу, на бэкенде хранятся независимо, в чужие клиенты не попадают.
     - Ритуал высвобождения средств и ускорение достижения целей.
     - SSR-рендер экранов `Capital.vue` и `Overview.vue` для обоих пользователей.
   - Проведён аудит и удалена временная заглушка `frontend/src/views/PlaceholderView.vue` (0 неиспользуемых компонентов-заглушек в проекте).

### Результаты верификации
- **Go бэкенд:** `cd backend && go test ./...` — 100% тестов проходят (`server`, `auth`, `config`, `handlers`, `repository`).
- **Фронтенд тесты:** `cd frontend && npm test` — 17 тестовых файлов, 88 тестов успешно пройдены.
- **Сборка фронтенда:** `cd frontend && npm run build` (`vue-tsc -b && vite build`) — компиляция типов и бандл собираются без единой ошибки.

---

## Итоги критика

### 1. Ревью корректности диффа
- Проверена реализация задач MGV-12 (`Capital.vue`), MGV-13 (`Goals.vue`, `GoalDetail.vue`, `Deposit.vue`) и MGV-14 (`two-clients-sync.test.ts`, удаление `PlaceholderView.vue`).
- Доменная математика полностью соответствует инвариантам:
  - `netWorth` корректно суммирует общие и приватные счета пользователя плюс накопления по целям минус остаток кредитов.
  - `prepayment`, `lumpSum`, `debtCost`, `halfOverpayExtra` точно рассчитывают переплату и сокращение срока платежей.
  - `deposit` и `realRate` моделируют сложный процент с капитализацией и реальную доходность с поправкой на инфляцию.
- Изоляция приватных документов (`private_docs`): счета пользователя сохраняются в отдельный документ с независимым счетчиком ревизий, не попадая в `household_docs` других участников семьи.

### 2. Упрощение, реюз и исправление выявленных расхождений
- **Внеплановый доход (`?income=1`):** кнопка «Внеплановый доход» в быстром меню `AppShell.vue` перенаправляла на `/capital?income=1`, но модальное окно отсутствовало в `Capital.vue`. Добавлено реактивное состояние `extraIncomeOpen`, поддержка query-параметра `?income=1` и модалка с распределением дохода в цель (`contribute`) или на счет (`setAccountAmount`).
- **Синхронизация таба вишлиста (`?tab=wish`):** кнопка «Покупка в дом» из меню `AppShell.vue` вела на `/goals?tab=wish`, однако вкладка не реагировала на query URL. Добавлена двусторонняя реактивная синхронизация `tab` с `$route.query.tab`.

### 3. Доработка тестов
- В `Capital.test.ts` добавлен тест рендера модального окна «Внеплановый доход» при переходе по `/capital?income=1`.
- В `Goals.test.ts` добавлен тест переключения на вкладку вишлиста при переходе по `/goals?tab=wish`.
- Общий тестовый набор фронтенда: 17 файлов, **90 тестов** (все 100% зеленые).
- Тесты бэкенда: 100% зеленые (`go test ./...`).
- Сборка бандла: `npm run build` проходит за ~1.3с с нулевым количеством ошибок компиляции типов vue-tsc.

### 4. Актуализация живых доков и трекера
- В `00-backlog.md` статус Блока 5 скорректирован с преждевременного `🏁` на `🔄 в работе (ревью завершено ✅)`. Закрытие блока со статусом `🏁` производится исключительно сессией приёмки.
- Обновлён статус активного бэклога в `memory/STATE.md`.
- Конвейер готов к финальной сквозной приёмке: следующий шаг — `/accept migrate-go-vue 5`.

---

## Приёмка

