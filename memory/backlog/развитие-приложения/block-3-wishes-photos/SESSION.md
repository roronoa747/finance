# Блок 3 — Желания с фото · сессии

**Цель блока:** желание перестаёт быть строчкой — у него фото, у каждого свой список, вход
по аватару, подарки-сюрпризы скрыты от адресата вплоть до фото, и видно, что именно
приближают досрочка и освободившиеся деньги.

Сессии: исполнитель ⬜ · критик ⬜ · приёмка ⬜ · ревью backend ⬜ · ревью frontend ⬜ · клинап ⬜

---

## Промпт исполнителя — уровень build · Opus 5 · effort high (ultrathink)

```
/worker развитие-приложения 3 ultrathink
```

Блок-специфика:

- Ветка `rp-block-3-wishes-photos` от `main` (после деплоя Блока 2). Push — с согласия
  владельца; `main` не трогать; **прод-БД не трогать** (миграция в прод — клинап).
- Задачи по порядку: `RP-14` (Go) → `RP-15` → `RP-16` → `RP-17`.
- Среда: для фото нужен реальный Postgres — локальный/Docker одноразовый (`TEST_DATABASE_URL`
  и `DATABASE_URL` для `go run ./cmd/server`, **не** Supabase); на моках — только хендлер-тесты.
  Два профиля браузера для скрытых желаний.
- Безопасность (L): права на фото — по матрице §3 индекса; 404 вместо 403 там, где 403
  раскрыл бы существование скрытого.
- Верификация: `cd backend && go build ./... && go vet ./... && go test ./...` + PG-режим;
  корень `go vet ./api/... && go test ./api/...`; `cd frontend && npm run build && npm test`.

---

## Промпт критика — уровень review · Opus 5 · effort xhigh (ultracode)

```
/critic развитие-приложения 3 ultracode
```

Блок-специфика:

- Права на фото: перебрать матрицу §3 (member/viewer/чужая семья/аноним × своё/общее/
  скрытое чужое) — каждый случай покрыт тестом; проверка сигнатуры файла; лимиты тела.
- Миграция `000002`: только добавляет; FK и поведение при удалении; квалификация `app.`.
- Токен не утекает в URL картинок; object URL освобождаются.
- Слияние личного документа: 409, правка во время запроса, два устройства одного человека.
- Промпт Блока 4 актуализировать (как устроены ручки фото и клиент — RP-19 загружает
  скриншот тем же путём).

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh (ultracode)

```
/accept развитие-приложения 3 ultracode
```

Блок-специфика:

- Критерии брифа Блока 3 на локальном стенде с Postgres: желание с фото, добавленное с
  телефона (телефонная ширина), видно партнёру; скрытое — не видно ни записи, ни фото
  (запрос от имени партнёра → 404).
- Регрессия: весь `frontend/e2e/` + `npm test`, `go test ./...` + PG-режим; CI `ci-backend`
  и `ci-frontend` зелёные.
- Сценарии приёмки — в `frontend/e2e/` (файл Блока 3).
- **L-блок: приёмка НЕ деплоит.** Следующий шаг — `/dir-review развитие-приложения 3 backend`.

---

## Промпт ревью backend — уровень review · Opus 5 · effort xhigh

```
/dir-review развитие-приложения 3 backend
```

Блок-специфика:

- Каталог `backend/` (+ корневые `api/`, `go.mod`, если тронуты).
- Фокус: новый репозиторий фото рядом с `doc_repo`/`household_repo` (единообразие, моки),
  хендлеры фото рядом с `sync.go` (проверки ролей, коды ошибок), миграция `000002`.
- Следующий шаг — `/dir-review развитие-приложения 3 frontend`.

---

## Промпт ревью frontend — уровень review · Opus 5 · effort xhigh

```
/dir-review развитие-приложения 3 frontend
```

Блок-специфика:

- Каталог `frontend/`: действия желаний в сторе против прямых `mutateHouseholdDoc` в вью;
  слияние/синк личного документа против общего (дубли); модуль сжатия; кэш картинок.
- Следующий шаг — `/cleanup развитие-приложения 3`.

---

## Промпт клинапа — уровень build · Opus 5 · effort high

```
/cleanup развитие-приложения 3
```

Блок-специфика:

- Выполнить «клинап»-вердикты `REVIEW-backend.md`, `REVIEW-frontend.md`.
- **Деплой — каждый шаг с «да» владельца:** (1) записать ID текущего Production-деплоя;
  (2) `cmd/migrate` против прод-БД (сессионное подключение 5432, строка — в
  `memory/secrets/supabase-db.md`) — применяется `000002`, прошлый деплой на новой схеме
  работает (Р-31); (3) merge `rp-block-3-wishes-photos` → `main`, push. Смоук на
  `family-finance-ff.vercel.app`: желание с фото с телефона видно на втором телефоне;
  скрытое — нет. **Откат:** promote записанного деплоя; таблица фото остаётся (не мешает).
- «Итоги пост-приёмки», 🏁 в §4. Следующий шаг — `/worker развитие-приложения 4 ultrathink`.

---

## Handoff (заполняет исполнитель)

- **Коммиты:**
- **Что сделано:**
- **Отклонения от ТЗ:**
- **Грабли среды:**
- **Верификация:**

## Итоги критика

## Приёмка

## Итоги пост-приёмки (только L)
