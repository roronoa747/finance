# Блок 0 — Хвосты переключения · сессии

**Цель блока:** закрыть хвосты переключения на Go + Vue — правка партнёра больше не
затирается тапом по полю, статус синка честный, демо стало черновиком будущей семьи — и
подготовить слияние к новым данным блоков 1–7, чтобы ни один деплой не терял данные (Р-14).

Сессии: исполнитель ✅ · критик ⬜ · приёмка ⬜

---

## Промпт исполнителя — уровень build · Opus 5 · effort high

```
/worker развитие-приложения 0
```

Блок-специфика:

- Ветка `rp-block-0-tails` от `main`. Push ветки — с согласия владельца (CI); `main` не
  трогать (это деплой приёмки).
- Задачи по порядку: `RP-01` → `RP-02` → `RP-03` → `RP-04` → `RP-05`. RP-03 — единственная
  задача Go в блоке (тот же `sync.go`, pg + моки).
- Среда: локальный стенд из §6 (`go run ./cmd/server` на моках + `npm run dev`), два профиля
  браузера для сценариев двух клиентов. Preview Vercel не использовать (§6, грабли).
- RP-05 опирается на `logout`, который чистит стор (RP-04), — не дублировать сброс.
- Верификация: `cd frontend && npm run build && npm test`; `cd backend && go build ./... &&
  go vet ./... && go test ./...` + PG-режим для RP-03 (одноразовая БД в Docker/локальном
  Postgres; если поднять негде — записать в Handoff, CI `integration` покроет).

---

## Промпт критика — уровень review · Opus 5 · effort xhigh

```
/critic развитие-приложения 0
```

Блок-специфика:

- `merge.ts`: коммутативность и идемпотентность сохранены на новых правилах (незнакомые
  ключи, поля проигравшего); явный `null` не воскрешает старое значение.
- RP-03: слияние ключей атомарно с проверкой ревизии (нет окна между чтением и записью);
  `resetDoc` нового клиента по-прежнему обнуляет всё, что он знает.
- RP-04/05: выход из аккаунта не теряет неотправленное молча; демо не шлёт ни одного
  запроса; вход по коду из демо не смешивает документы.
- Живые доки: `frontend/README.md` / `backend/README.md` — только если там описаны синк или
  демо.
- Промпт Блока 1 актуализировать по факту: как именно RP-02 сливает незнакомые ключи
  (нужно RP-06 для `payments`), где флаг «неотправленное».

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh

```
/accept развитие-приложения 0
```

Блок-специфика:

- Критерии брифа Блока 0 на локальном стенде, два профиля браузера: правка «Еда и быт» на
  клиенте A не затирается тапом по полю на B; в демо нет запросов к `/api` и нет «не
  сошлось»; демо → регистрация → «да» → заполненное видно после входа с другого профиля;
  демо → вход по коду приглашения → данные семьи не изменились.
- Регрессия: весь `frontend/e2e/` + `npm test`, `go test ./...`, CI `ci-backend` и
  `ci-frontend` зелёные на HEAD ветки.
- Сценарии приёмки — в `frontend/e2e/` (файл Блока 0).
- **Деплой (M):** с «да» владельца — записать ID текущего Production-деплоя Vercel (откат),
  merge `rp-block-0-tails` → `main`, push; смоук на `family-finance-ff.vercel.app`: вход,
  данные на месте, правка «Еда и быт» видна на втором устройстве, бейдж без сети — «нет
  сети». **Откат:** promote записанного деплоя в Vercel.
- После деплоя попросить владельца открыть приложение на обоих телефонах (старый JS
  отработает последний раз, дальше — код с RP-02) — до начала Блока 1.
- Следующий шаг — `/worker развитие-приложения 1 ultrathink`.

---

## Handoff (заполняет исполнитель)

Сессия исполнителя 2026-09-24 (Opus 5.5). Репо `finance`, ветка `rp-block-0-tails` от `main`
(`59b96c3`); ветка **не пушилась** — push даёт CI и делается с «да» владельца (§5).

- **Коммиты:**
  - `15e7aed` RP-01 — «Еда и быт»: коммит только изменённого значения
  - `5422626` RP-02 — слияние не теряет незнакомые поля
  - `540a44a` RP-03 — сервер не теряет ключи документа от старого клиента
  - `f3c4c4d` RP-04 — честный статус синка, флаг неотправленного, выход, Н-6
  - `bd4e9fd` RP-05 — демо без сервера, перенос демо при регистрации
  - `6328e36` RP-04 (доводка по браузеру) — нет связи с сервером → «нет сети»
  - `d328d5c` RP-05 (доводка) — вход в демо не стирает правки, ждущие входа
  - + коммит доков (этот Handoff, §4, STATE.md)
- **Что сделано:**
  - **RP-01.** `lib/num.ts` `numChanged(text, initial, kind)` — сравнение чисел, а не строк
    («150 000» = «150000»); `NumFieldBlur` коммитит только изменённое, Enter лишь уводит фокус
    (один коммит). Второй рубеж в сторе (`unchanged()`): `setCategoryAmount`, `correctSalary`,
    `updateAccount` (→ `setAccountAmount`, имя/заметка), `setPerson`, `setDeposit`,
    `correctObligation` при том же значении не трогают `updatedAt`, статус и синк.
  - **RP-02.** `mergeDocs`: незнакомые ключи верхнего уровня (`mergeUnknownKeys`, список
    `KNOWN_KEYS`) — массив объектов с `id` (включая `[]`) сливается `mergeList` по id; прочее —
    со стороны, где ключ есть, при обеих — remote. `mergeList`: `{ ...loser, ...winner }` —
    поле, которого нет у победителя, остаётся от проигравшего; явный `null` — значение. Шапка
    `merge.ts` — правило 3. **Для RP-06:** новый список (`payments`) добавить в `SyncDoc`,
    `defaultSyncDoc()`, явно в `mergeDocs` **и в `KNOWN_KEYS`** (иначе его обработает и общее
    правило, и явное — явное перекроет, но это лишний проход и путаница).
  - **RP-03.** Успешный push общего и личного документа: `SET data = CASE WHEN
    jsonb_typeof(data)='object' AND jsonb_typeof($1::jsonb)='object' THEN data || $1::jsonb ELSE
    $1::jsonb END` (`keepStoredKeysSQL`, `doc_repo.go`) — в том же `UPDATE` строки, залоченной
    `SELECT … FOR UPDATE` после проверки ревизии (окна между чтением и записью нет). Присланные
    ключи (`[]`, `null`) побеждают; не-объект пишется как есть; вставка первого личного
    документа — как раньше. Моки — `keepStoredKeys` (`mock_repo.go`), то же поведение.
  - **RP-04.** Стор `finance.ts`: флаг неотправленного `unsent` / `privateUnsent` / `hasUnsent`
    (ключ `ff_unsent`, переживает перезапуск; при старте с ним статус `dirty`); `pullHousehold`
    сливает, если есть неотправленное, и досылает (`scheduleSync`); иначе — замена. Документ
    привязан к семье: `docHousehold` (`ff_doc_household`), `claimFor(id)` — другая семья стирает
    документ, та же — оставляет; движок при старте присваивает хозяина документу до RP-04.
    `clearLocal()` стирает оба документа, ревизии, флаги и ключи `localStorage`; счётчик
    `session` не даёт ответу, запрошенному до выхода, вернуть прежнюю семью. `auth.logout(choice?)`
    — без неотправленного выходит и стирает; с неотправленным возвращает `false`, экран
    (`AppearancePanel`) спрашивает: «Отправить и выйти»; не вышло — «Войти заново» (`'keep'`:
    правки ждут входа в эту же семью) или «Выйти, правки пропадут» (`'discard'`). Движок: старт
    без сети — сразу `offline`. `pullHousehold` без сети не ходит. Запрос, не дошедший до
    сервера (ошибка не `ApiError`), — статус `offline`, ответ сервера с ошибкой — `error`.
    `Access.vue`: после входа/кода — `enterFamily` (claimFor + pull общего и **личного**).
  - **RP-05.** Демо = документ «семьи демо» (`DEMO_HOUSEHOLD`, `isDemo` в сторе): `syncHousehold`,
    `pullHousehold`, `pullPrivateDoc`, `scheduleSync`, push личного — в демо без запросов (Н-7);
    бейдж — «демо». Выход из демо — «Создать семью или войти» в Оформлении (`clearAuth`,
    черновик остаётся). `Access.vue` с черновиком открывается на «Создать»; регистрация →
    «Взять то, что вы заполнили в демо?»: «Да» — `adoptDemo` (весь общий и личный документ в
    новую семью, участник `a` — имя из регистрации, `setupDoneAt` сохранён → мастер не
    открывается), «Нет» и регистрация не из демо — `startNewFamily` (чистый документ). Вход и
    код — `enterFamily` стирает черновик до pull; строка «Заполненное в демо сюда не
    переносится: у семьи уже есть свои данные…» — заранее на вкладках «Войти»/«По коду».
    «Вернуться в демо» продолжает черновик. Пока правки семьи ждут входа — демо не
    предлагается. Начавшийся синк снимает запланированный (без лишнего push после
    `adoptDemo`). Сценарии — `frontend/e2e/block0-tails.test.ts`.
- **Отклонения от ТЗ:**
  - **RP-01 п.3:** кроме имени/заметки счёта (уже с проверкой) нашёл поле суммы обязательства
    `Capital.vue:1137` (`correctObligation` по blur без проверки) — защита добавлена в
    `correctObligation`. Защита `updateAccount` общая: любой patch с теми же значениями.
  - **RP-02 п.1:** незнакомый не-список при обеих сторонах — **remote**, а не local. Этот код
    незнакомый ключ править не умеет, значит его локальная копия — прошлый ответ сервера;
    local-wins позволил бы старому PWA затереть свежую правку нового клиента (ровно путь потери
    Р-14). Выбор описан в шапке `merge.ts`. **п.3:** `movements`/`versions` не менял — они
    append-only, копии одного элемента одинаковы, перенос полей ничего не даёт. Смежно:
    `boughtOn` при снятии «куплено» теперь `null`, а не `undefined` (`Goals.vue`, тип) — иначе
    правило «поле проигравшего» его воскрешало. Найден хвост `mergeObligation` (§4).
  - **RP-04 п.4:** pull сливает по **флагу неотправленного**, а не «при любом статусе, кроме
    idle»: `error`/`offline` без правок после сброса данных партнёром (resetDoc без надгробий)
    воскресили бы стёртое. **п.3:** третий выбор «Войти заново» + привязка документа к семье —
    без них при истёкшем входе (401 — это и есть Н-6) выход предлагал бы только потерю; привязка
    не даёт смешать семьи при входе другим человеком. Флаг неотправленного — и для личного
    документа, чтобы выход не терял молча и его. `pullPrivateDoc` раньше нигде не вызывался
    (личный жил только в `localStorage`); раз выход стирает — вызывается при входе, при
    неотправленном личном досылает поверх (слияние личного — RP-15). **п.1:** одной проверки
    `navigator.onLine` мало (браузер: после перезагрузки без сети `onLine === true`) — статус по
    сбою `fetch`. Из-за этого поменял ожидание старого теста `finance.test.ts` «ответ,
    пришедший во время синка…»: `push` падает `Error('network')` → теперь `offline`, а не `error`.
  - **RP-05:** демо определяется по документу (`docHousehold`), а не по токену — тот же
    `claimFor` сам стирает черновик при входе/коде; демо, начатое до RP-05, движок помечает.
    Выход из демо — не `logout` (стёр бы черновик и предупреждал о «неотправленном»). Строка о
    неперенесении — заранее, до входа, а не после. Потоки в сторе (`enterFamily`,
    `startNewFamily`, `adoptDemo`) — чтобы e2e гонял тот же код, что `Access.vue`. «Да» переносит
    и второго участника примера (b «Аруна») — «весь демо-документ» по ТЗ.
  - **Для критика — известные границы:** `unreachable()` считает «нет сети» любую ошибку не
    `ApiError`, включая баг кода в слиянии (текст виден в `lastError`); личный документ по-прежнему
    перезаписывается целиком (RP-15); на `/access?code=…` из демо код теряется (демо-сессия
    уводит с `/access`, а «Создать семью или войти» открывает без кода).
- **Грабли среды:**
  - Docker и локального Postgres нет. PG-режим — embedded PostgreSQL 16.9 из кэша
    `~/.embedded-postgres-go` через `fergusstrange/embedded-postgres` v1.34.0 (есть в go mod
    cache, `GOPROXY=off`): раннер во временной папке сессии; `TEST_DATABASE_URL` → localhost,
    порт 55432, БД `fftest`, учётка библиотеки по умолчанию, `sslmode=disable`.
  - Браузер: `playwright-core` 1.63 из корневого `node_modules` (React-эталон) + установленный
    Chrome (`channel: 'chrome'`) — headless shell 1243 не скачан. Профили = отдельные
    `BrowserContext`, ширина 390.
  - Офлайн-эмуляция под service worker: после перезагрузки `navigator.onLine === true`, события
    `online` при возврате сети нет → «синхронизировано» возвращает фоновый круг (60 с).
    Офлайн-перезагрузку проверять только на прод-сборке (`vite preview` проксирует `/api`, SW есть).
  - Vite dev отдаёт `/src/api/client.ts` — фильтр запросов к API по `pathname.startsWith('/api/')`.
  - `gofmt -l` на Windows перечисляет всё из-за CRLF рабочей копии — проверять
    `tr -d '\r' < файл | gofmt -l`.
  - `python` в Git Bash — заглушка магазина Windows, виснет. `npm run dev` / `vite preview`
    переживают остановку фоновой задачи — добивать `node` по PID.
  - «По коду» в браузере: вышедший пользователь получает 401 (хвост §4) — в сценарии подставлен
    токен заранее зарегистрированного участника.
- **Верификация:**
  - `cd frontend && npm run build && npm test` — ✅, 24 файла, 140 тестов.
  - `cd backend && go build ./... && go vet ./... && go test ./...` — ✅; PG-режим
    `go test -p 1 -count=1 -run Postgres ./...` — ✅ (новый
    `TestPostgresPushKeepsKeysMissingFromOldClient`: старый клиент, `[]`, 409 без записи,
    личный документ); корень `go vet ./api/... && go test ./api/...` — ✅.
  - Красные до правки (контроль): RP-01 e2e без защиты стора; Н-6 со старым условием pull;
    PG-тест с `$1::jsonb` вместо слияния.
  - Браузер, локальный стенд (Go на моках + Vite, headless Chrome 390px), 22/22:
    RP-01 — два профиля, A: 180 000; B тапает по полю и уходит — push 1→1, у сервера и B 180 000.
    Н-6 — истёкший токен → правка 205 000 → «не сошлось» → «Выйти» спрашивает → «Отправить и
    выйти» не вышло → «Войти заново» → вход → 205 000 на сервере и у второго профиля. Выход без
    неотправленного — `ff_household_doc`, `ff_unsent`, `ff_auth_token` стёрты. Демо: бейдж «демо»,
    0 запросов к `/api` при правках, нет «не сошлось»; демо → регистрация → «Да, взять» →
    второй профиль той же почтой видит 333 000, участник a — «Дана»; демо → «По коду»: строка о
    неперенесении, ревизия семьи 7→7, «Еда и быт» прежняя, у вошедшего — данные семьи.
  - Прод-сборка (`vite preview`, SW управляет страницей): Offline → перезагрузка → «нет сети»;
    сеть вернулась → «синхронизировано».
  - Скриншоты новых экранов (выход с неотправленным, вопрос «Взять демо?», строка при входе по
    коду, Оформление в демо) — светлая тема, токены; в репо не кладутся.
  - **CI не запускался:** ветка не пушилась (нужно «да» владельца).

## Итоги критика

## Приёмка
