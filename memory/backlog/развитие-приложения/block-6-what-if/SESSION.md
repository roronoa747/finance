# Блок 6 — «А если…» · сессии

**Цель блока:** дать семье спокойствие «мы выдержим» — сценарий «без одного дохода» за
секунду показывает, сколько месяцев продержимся и что поставить на паузу, без допроса о
декрете.

Сессии: исполнитель ⬜ · критик ⬜ · приёмка ⬜

---

## Промпт исполнителя — уровень build · Opus 5 · effort high

```
/worker развитие-приложения 6
```

Блок-специфика:

- Ветка `rp-block-6-what-if` от `main` (после деплоя Блока 5). Push — с согласия владельца;
  `main` не трогать.
- Задачи по порядку: `RP-24` → `RP-25`.
- Опора — функции Блоков 1, 2, 5 (производные остатки, зарплаты, группы подписок, жилищный
  вклад, `keep`): имена — из Handoff/итогов критика этих блоков (критик Блока 5 вписывает сюда).
- Ручной расчёт для теста RP-24 — показать шаги в комментарии теста.
- Верификация: `cd frontend && npm run build && npm test`; браузер — локальный стенд.

---

## Промпт критика — уровень review · Opus 5 · effort xhigh

```
/critic развитие-приложения 6
```

Блок-специфика:

- Пересчитать один сценарий независимо (таблица по месяцам) и сверить с `finance.ts`.
- Краевые случаи: доход ≥ расходов (нет бесконечного цикла), нулевые ликвидные, кредит
  закрывается внутри горизонта.
- Тексты: без имён в негативном контексте, без сравнения участников.
- Промпт Блока 7 актуализировать: какие числа `finance.ts` пойдут в сводку для модели
  (итог месяца RP-13, запас прочности RP-24 и т.п.).

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh

```
/accept развитие-приложения 6
```

Блок-специфика:

- Критерий брифа Блока 6: «А если» без одного дохода даёт число месяцев, сверенное с ручным
  расчётом на живых числах.
- Регрессия: весь `frontend/e2e/` + `npm test`; CI `ci-frontend` зелёный.
- Сценарии приёмки — в `frontend/e2e/` (файл Блока 6).
- **Деплой (M):** с «да» владельца — записать ID текущего Production-деплоя (откат), merge
  `rp-block-6-what-if` → `main`, push; смоук на `family-finance-ff.vercel.app`: экран «А
  если…» на живых данных. **Откат:** promote записанного деплоя.
- Следующий шаг — `/worker развитие-приложения 7 ultrathink`.

---

## Handoff (заполняет исполнитель)

- **Коммиты:**
- **Что сделано:**
- **Отклонения от ТЗ:**
- **Грабли среды:**
- **Верификация:**

## Итоги критика

## Приёмка
