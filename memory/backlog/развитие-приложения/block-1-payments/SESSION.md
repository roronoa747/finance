# Блок 1 — «Оплатил», досрочка, подписки · сессии

**Цель блока:** одно нажатие «оплатил» для всего по графику — остатки счетов и кредитов
честно выводятся из записей и не теряются при двух офлайн-телефонах; досрочку можно
применить и видеть сэкономленное; подписки собраны в группы и спрашивают «оставить?» без
надоедания.

Сессии: исполнитель ✅ · критик ✅ · приёмка ⬜ · ревью frontend ⬜ · клинап ⬜
<!-- Каталоги ревью — по факту коммитов Handoff-а; план — только frontend. -->

---

## Промпт исполнителя — уровень build · Opus 5 · effort high (ultrathink)

```
/worker развитие-приложения 1 ultrathink
```

Блок-специфика:

- Ветка `rp-block-1-payments` от `main` (после деплоя Блока 0). Push ветки — с согласия
  владельца; `main` не трогать.
- Задачи по порядку: `RP-06` → `RP-07` → `RP-08` → `RP-09`. RP-06 — фундамент (модель,
  слияние, математика, стор): не переходить к UI, пока его тесты не зелёные.
- **Деньги (L):** каждая сумма, попадающая в документ или на экран, — из `finance.ts`,
  целая; в компонентах арифметики с деньгами нет. Слияние `payments` — явное (§6), не через
  страховку RP-02 для незнакомых ключей.
- Что оставил Блок 0 (факты кода, сверены критиком 2026-09-24):
  - **`payments` в слиянии:** тип `SyncDoc` → `defaultSyncDoc()` → объект `known` в
    `mergeDocs` (`lib/merge.ts`). Больше никуда: незнакомым считается всё, чего нет в `known`
    (списка `KNOWN_KEYS` больше нет). Документы до Блока 1 приходят без `payments` — читать
    `doc.payments ?? []`.
  - **`payments: []` в `defaultSyncDoc()` обязателен:** сервер (RP-03) хранит ключи верхнего
    уровня, которых нет в push, — «Сбросить данные» (`resetDoc` шлёт `defaultSyncDoc()`)
    обнулит `payments` на сервере, только если ключ прислан.
  - **`mergeList` берёт `{ ...проигравший, ...победитель }`:** поле очищать `null`, не
    `undefined` (в JSON ключ пропадёт, значение проигравшего воскреснет). «Не списывать» —
    `accountId: null`; снятие отметки — надгробие `deletedAt`.
  - **Флаг неотправленного** (`unsent` / `privateUnsent` / `hasUnsent`, ключ `ff_unsent`)
    ставит `mutateHouseholdDoc` / `mutatePrivateDoc` — отметки только через них, иначе выход
    сотрёт их без вопроса. Демо (`financeStore.isDemo`) — отметки работают локально, запросов
    нет.
  - **Защита RP-01** `unchanged()` в сторе (`setAccountAmount`/`updateAccount`, `setDeposit`,
    `correctSalary`, `correctObligation`, `setCategoryAmount`, `setPerson`) сравнивает с тем,
    что отдаёт стор. После производных остатков `setAccountAmount` с тем же видимым остатком
    не пишет ни базу, ни якорь — это верно (остаток не изменился), не «чинить». У
    `updateCredit` защиты нет — якорь при смене `principal` ставить явно.
  - Поле суммы, коммитящее по blur, — `NumFieldBlur` (коммит только изменённого, `numChanged`
    в `lib/num.ts`).
- Среда: локальный стенд (§6), два профиля браузера, телефонная ширина. Офлайн-сценарий —
  DevTools → Offline в обоих профилях, отметки, затем онлайн.
- Верификация: `cd frontend && npm run build && npm test` (весь `e2e/`).

---

## Промпт критика — уровень review · Opus 5 · effort xhigh (ultracode)

```
/critic развитие-приложения 1 ultracode
```

Блок-специфика:

- Деньги: сверить формулы разбивки аннуитета, «снизить платёж», сэкономленных процентов с
  независимым расчётом (другой способ — например, помесячный график); округление только в
  `finance.ts`; нет двойного учёта (дедупликация пары, якорь ручной правки).
- Слияние: две офлайн-отметки, одна пара с двух устройств, снятие и повторная отметка,
  конфликт 409 — тесты есть и зелёные; коммутативность `mergeDocs` с `payments`.
- Все места, читающие остаток (`amount`, `principal`), получают производное значение —
  grep по вью; запись остатка ставит якорь.
- Viewer: кнопок нет; сервер и так отвергает push (403).
- Живые доки: `NEXT.md` — только отметить сделанное в «Развитии после переключения» п.1,
  если там есть статусы.
- Промпт Блока 2 актуализировать: имена полей `payments`, функции «следующий платёж»,
  «оплачено за период», как устроен `kind` (RP-10 добавит зарплату).

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh (ultracode)

```
/accept развитие-приложения 1 ultracode
```

Блок-специфика:

- Критерии брифа Блока 1 на локальном стенде, два профиля: «оплатил аренду» одним нажатием →
  следующий платёж, карта уменьшилась у обоих; две офлайн-отметки с двух профилей — обе
  доехали, без удвоения; досрочка «сократить срок» меняет остаток и срок, показывает
  сэкономленные проценты — сверено с `finance.ts` до тенге; подписка рабочей группы не
  спрашивает «оставить?», досуговая годовая — спрашивает.
- Правки критика (браузер): «Оплатил» → взнос в цель с той же карты → «Снять отметку» —
  деньги вернулись; отметка → сверка остатка карты вручную → «Другая сумма или счёт» →
  «Сохранить» — второго списания нет; коммуналка в «До зарплаты» спрашивает сумму.
- Регрессия: весь `frontend/e2e/` + `npm test`; CI `ci-frontend` зелёный на HEAD ветки.
- Сценарии приёмки — в `frontend/e2e/` (файл Блока 1).
- **L-блок: приёмка НЕ деплоит.** Следующий шаг — `/dir-review развитие-приложения 1 frontend`.

---

## Промпт ревью frontend — уровень review · Opus 5 · effort xhigh

```
/dir-review развитие-приложения 1 frontend
```

Блок-специфика:

- Каталог `frontend/`: как записи платежей легли рядом с `movements` целей и версиями
  обязательств (единообразие слияния), дубли логики «следующий платёж» между `Budget`,
  `Overview`, `Capital`; производные остатки — нет ли места, читающего сырое `amount`.
- От критика (дубли с кодом вне блока — не правил, решить вердиктом): хром модалок/листов
  (~19 копий `fixed inset-0`, у листа `PaidRow` нет Escape — `kit/Sheet`?); склонение
  числительных (`groupNote` Capital, `dayWord` Overview, `monthWord` Goals); генерация id
  `Math.random().toString(36).slice(2, 10)` (9 копий); переключатели-чипы (Capital, Goals,
  GoalDetail) против `Segmented`.
- Если коммиты блока тронули `backend/` — добавить ревью backend в трекер и промпт.
- Следующий шаг — `/cleanup развитие-приложения 1`.

---

## Промпт клинапа — уровень build · Opus 5 · effort high

```
/cleanup развитие-приложения 1
```

Блок-специфика:

- Выполнить «клинап»-вердикты `REVIEW-frontend.md`.
- **Деплой** (с «да» владельца): записать ID текущего Production-деплоя (откат), merge
  `rp-block-1-payments` → `main`, push. Смоук на `family-finance-ff.vercel.app` с живыми
  данными: остатки кредитов и счетов до первой отметки **те же**, что до деплоя (старые данные
  без записей); одна настоящая отметка владельцем → видно на втором телефоне. **До первой
  отметки и до сверки остатков** — полностью перезапустить PWA на обоих телефонах (закрыть из
  переключателя приложений) и убедиться, что видна кнопка «Оплатил»: остаток, введённый на
  невыросшей версии Блока 0, спишет отметки второй раз (хвост §4). **Откат:**
  promote записанного деплоя; записи `payments` в документе прошлый клиент сохранит (RP-02/03).
- «Итоги пост-приёмки», 🏁 в §4. Следующий шаг — `/worker развитие-приложения 2`.

---

## Handoff (заполняет исполнитель)

- **Коммиты** (ветка `rp-block-1-payments` от `2c098a9`, не запушена — §5, нужно «да» владельца):
  `91c6996` RP-06 · `666da80` RP-07 · `aaf39ee` RP-08 · `c46bd02` RP-09 · docs-коммит Handoff.
  Затронут только `frontend/` (бэкенд не менялся) — план ревью «frontend» в силе.
- **Что сделано:**
  - **RP-06.** `SyncDoc.payments?: Payment[]` — `kind: 'obligation' | 'credit' | 'prepay'`,
    `targetId`, `period` (`YYYY-MM` по графику), `amount`, `principal?` (тело, снимок),
    `accountId: string | null`, `by`, `at` (+ поля досрочки RP-08). В `defaultSyncDoc()` и явно
    в `mergeDocs` (`mergeList` по id, надгробие сильнее). Якоря: `Account.amountSetAt`,
    `Credit.principalSetAt`. `finance.ts`: `creditSplit` (проценты = остаток × ставка / 12,
    округление до тенге; последний платёж ≤ остаток + проценты), `creditDueAmount`,
    `countedPayments` (надгробия не считаются; пара `kind:targetId:period` — одна запись: ранняя
    по `at`, при равенстве меньший id; досрочки не схлопываются), `paidFor` («оплачено за
    период»), `accountBalance` / `creditBalance` (база − отметки с `at >= якоря`),
    `nextObligationDue` / `nextCreditDue` («следующий платёж»: первый неоплаченный с текущего
    месяца по Алматы, горизонт 24 мес.), `lastAccountFor` (счёт прошлой оплаты цели;
    `null` — «не списывать», `undefined` — спросить), `untilPayday` с `payments`. Стор: геттеры
    `accounts` / `householdAccounts` / `privateAccounts` / `credits` отдают производные
    остатки (копии), `payments`; `markPaid(kind, targetId, by, {period?, amount?, accountId?})`,
    `unmarkPaid(kind, targetId, period)` (надгробие на все живые записи пары).
  - **RP-07.** `components/PaidRow.vue` — одна строка «Оплатил» для обязательства и кредита:
    одно нажатие = отметка суммой графика со счёта прошлой оплаты; лист (Teleport в body) —
    только для первой оплаты (счёт спрашивается один раз, есть «Не списывать»), оценки
    (коммуналка — сразу спрашивает сумму), «Другая сумма или счёт», деталей и «Снять отметку»
    (подтверждение). Оплаченное: «оплачено · дальше <дата> · <сумма>», у кредита — «остаток N»;
    viewer — отметки видны, кнопок нет. Встроена: Бюджет (календарь дня, список), Обзор
    («До зарплаты» — неоплаченные + оплаченные в окне ниже; «Впереди» — оплаченное вниз),
    модалки кредита и обязательства в Капитале.
  - **RP-08.** `finance.ts`: `lumpPlan(principal, rate, payment, lump, 'term' | 'payment')` —
    остаток, платёж, платежей до/после, «не отдадим банку», всё целое; `prepaySaved` — сумма по
    живым досрочкам. Стор: `applyPrepayment` (запись `prepay`, тело = взнос, `saved` — снимок,
    «снизить платёж» меняет `Credit.payment`, прежний/новый — в записи), `removePrepayment`.
    Капитал: в калькуляторе (режим «Разово») блок «Применить к кредиту» с превью, выбор счёта,
    список применённых со «Снять»; строка «Досрочками уже сэкономили на процентах N ₸» под
    обязательствами.
  - **RP-09.** `Obligation.group`, `noAsk`, `keptAt`; `parentId` чистится `null`.
    `liveObligations` — только то, что платится (без групп), `liveGroups` — группы;
    `dueIn(группа) = false`; `groupChildren` / `groupTotal` (доли месяца, целые);
    `isSubscription` (d4 без `estimate`, не группа); `keepQuestions(list, now)` + `KEEP_ASK_DAYS
    = 14`. Стор: `keptAt` при заведении, `addGroup`, `moveToGroup`, `keepSubscription`,
    `removeGroup`. Капитал: группы с итогом и подписками внутри, окно группы (название, флаг
    «Рабочие — нет», вынуть/добавить, удалить), выбор группы у подписки, «Месяц списания» у
    годовой, подпись годовых «в год». Обзор: одна карточка «Оставить «X»?» (годовая — «Продлится
    5 октября», ежемесячная — «Раз в квартал сверяем подписки»), «Отменить» с подтверждением.
- **Отклонения от ТЗ** (минимальные локальные решения):
  - RP-06 п.1: `today()` / `monthKey()` без аргумента **целиком переведены на Алматы** (одна
    правда; оба принимают `Date`). Затронуто всё, что зовёт их без аргумента: календарь и план
    Бюджета, Обзор, Капитал, Ритуал, `SalaryDialog`, заголовок `AppShell`, серии Goals/GoalDetail,
    умолчания `finance.ts`; `obMonth` формы годовой — в RP-09. Не переведены даты взносов
    `GoalMovement.date` (ISO UTC, месяц серии — `date.slice(0, 7)`) — хвост §4.
  - Якорь: отметка действует при `at >= якоря` (в ту же миллисекунду — «после»: так делают
    только скрипты «завёл счёт → отметил»). Любая запись счёта/кредита без смены остатка
    переписывает якорь прежним значением или явным `null` — иначе победитель слияния без ключа
    взял бы якорь проигравшего к своей старой базе (правило 3 `merge.ts`) и потерял бы отметки.
  - `markPaid` без счёта и без истории — «не списывать»: без выбора деньги не двигаются (UI
    спрашивает при первой оплате).
  - `untilPayday`: сумма кредита — `creditDueAmount` (последний платёж обрезан), закрытый кредит
    (остаток 0) не ждёт платежа; добавлены `kind`/`targetId` у пунктов и список `paid`. Старые
    цифры меняются только у кредита с остатком меньше платежа или 0.
  - «До зарплаты» на Обзоре получает производные остатки **общих** счетов (личные, как и раньше,
    не входят), а не сырой `householdDoc`.
  - Модалки Капитала фиксируют месяц платежа при открытии: после «Оплатил» строка показывает
    «оплачено · дальше …», а не перескакивает на следующий месяц с новой кнопкой (двойное
    нажатие не отмечает октябрь).
  - Закрытый долг не показывается в событиях Бюджета и «Впереди», если не закрыт этим месяцем;
    `budgetAmounts` (d2) его платёж пока считает — «освободилось N ₸» это RP-12.
  - RP-08: `lumpPlan` считает в непрерывных месяцах, как `annuityMonths`/`lumpSum`; новый платёж —
    `Math.round`, платежей — `Math.ceil`. Взнос, закрывший долг, платёж не переписывает. Снятие
    «снизить платёж» возвращает прежний платёж, только если он с тех пор не менялся
    (`payment === newPayment`) — иначе снятие затёрло бы более позднее решение.
  - RP-09: `liveObligations` исключает группы — одна точка, через которую группа выпадает из
    бюджета, календаря, «до зарплаты», «Впереди», отметок (`markPaid` по группе — `null`
    дополнительно). `keptAt` ставится при заведении любого обязательства (заведено = решено
    оставить); подписки, заведённые до RP-09 (`keptAt` нет), спросятся по одному разу в текущем
    квартале. Карточка — по одному вопросу за раз; viewer её не видит. Добавлено «Удалить группу»
    (подписки остаются без группы, `parentId: null`) — без этого группу нельзя убрать.
  - Тесты: пример незнакомого ключа в RP-02-тестах `merge.test.ts` переименован `payments` →
    `receipts` (ключ стал знакомым, тесты продолжают проверять путь незнакомых); в
    `e2e/block5-capital-goals.test.ts` счёт перечитывается после правки (геттеры отдают копии).
- **На что смотреть владельцу при деплое:** в первый месяц уже внесённые до деплоя платежи этого
  месяца видны неотмеченными. Первая отметка каждого платежа спрашивает счёт — если остаток
  карты вводили уже после этой оплаты, выбрать «Не списывать». «Интернет» и любые d4 без оценки —
  подписки: раз в квартал спросят «оставить?», пока их не положить в группу «Рабочие — нет».
- **Грабли среды:**
  - Пояс машины — Europe/Berlin (UTC+2): Алматы на 3 ч впереди, вечером последнего дня месяца
    месяцы расходятся. Тесты с календарём — на фиксированных моментах (`vi.setSystemTime`).
  - Браузер (как в Блоке 0): `playwright-core` 1.63 из корневого `node_modules` + Chrome
    `channel: 'chrome'`, профили = контексты, 390px; стенд — `go run ./cmd/server` + `npx vite
    --port 5173`. Заголовки секций — CSS `uppercase`, `innerText` отдаёт «ВПЕРЕДИ»; `Field`
    оборачивает переключатели в `<label>` — имя первой кнопки «Как вносите Разово» (нужен
    `exact`); прокрутка у оболочки внутренняя — `fullPage` бесполезен, `scrollIntoViewIfNeeded`;
    новый контекст до первого синка стоит на `/setup` — после синка повторный `goto`.
  - Тема на старте не применяется (хвост §4): после перезагрузки страница светлая, пока тему не
    выбрать в «Оформлении»; тёмная проверена переходами внутри SPA.
  - Консоль vitest приглушена — эталонные числа `finance.ts` для браузера снимались записью в файл
    временным тестом (удалён).
  - Vite/Go после остановки фоновой задачи живы — добивать по портам 5173/8080.
- **Верификация:**
  - `cd frontend && npm run build` ✅; `npm test` ✅ — 26 файлов, 192 теста (Блок 0: 24/140).
    Новое: `e2e/block1-payments.test.ts` (5 сценариев: две офлайн-отметки разных платежей с двух
    клиентов; одна пара с двух клиентов и снятие; RP-07 «оплатил аренду» → следующий платёж, карта,
    второй клиент; RP-08 досрочка «сократить срок» у обоих до тенге; RP-09 группы и «оставить?»),
    `components/PaidRow.test.ts` (SSR), RP-06…09 в `lib/finance.test.ts` (в т.ч. разбивка
    аннуитета: тело по графику = долг; «снизить платёж» сверено помесячным графиком в пределах
    1%; граница месяца/квартала по Алматы), `lib/merge.test.ts`, `stores/finance.test.ts`.
  - Браузер, локальный стенд §6, два профиля + viewer (роль в `ff_member`) — **60/60**:
    - RP-07 (28/28, повторён после RP-09): первая оплата — лист «С какого счёта» с «Не списывать»;
      «оплачено · дальше 5 октября · 220 000 ₸», карта 1 000 000 → 780 000; второй профиль после
      синка — то же; октябрь в «До зарплаты» — одно нажатие без листа, «Списаний до неё» 255 000 →
      35 000; «Впереди» — оплаченное ушло вниз; кредит из модалки: остаток 969 500, «дальше 15
      октября»; «Снять отметку» с подтверждением — остаток и карта вернулись; viewer — ни одной
      кнопки, отметки видны; **офлайн в обоих профилях**: A — интернет и коммуналка 35 000, B —
      кредит и коммуналка 36 000 → онлайн → у обоих всё отмечено, карта 452 000 (коммуналка
      списана один раз — ранняя), на сервере обе записи коммуналки; тёмная тема — только токены.
    - RP-08 (17/17): превью «Сократить срок» 200 000 — остаток 800 000, 18 платежей вместо 24, «не
      отдадим банку 154 457 ₸» (= `lumpPlan`), без прошлой оплаты кнопка неактивна; счётчик 154 457
      у обоих профилей; «Снизить платёж» 100 000 — 50 750 вместо 58 000, 27 456 (= `lumpPlan`), счёт
      по умолчанию — прошлый; счётчик 181 913; снятие — остаток 800 000, платёж 58 000, карта
      800 000; viewer — без «Применить» и «Снять».
    - RP-09 (15/15): «Рабочие» 1 подписка · рабочие · 3 000, «Досуг» 2 подписки · 5 989 (доля
      годовой), дети под группой, годовые «в год»; «Свободно в сентябре» до и после группировки —
      855 511; вопросы: iCloud (продлится 5 октября) → Интернет → Netflix, Slack (рабочие) — ни
      разу; «Отменить» Netflix — ушёл у обоих, у второго профиля вопросов нет; форма годовой —
      «Месяц списания» (по умолчанию сентябрь), сохранено `month: 3`.
  - CI не запускался: ветка не запушена (push — с «да» владельца, §5). Бэкенд не менялся.

## Итоги критика

Сессия критика 2026-09-24 (Opus 5.5, `ultracode`: ревью по 6 измерениям — деньги, синк,
остатки/группы, UI, реюз, тесты — и состязательная проверка каждой находки; 37 находок, 36
подтверждены, после сведения дублей ~20). Дифф `2c098a9..c46bd02` сверен с критериями
RP-06…RP-09; коммиты критика — ветка `rp-block-1-payments`: `dfc7ab0` (код), `df19ef9` (тесты)
+ коммит доков.

- **Корректность (по пунктам промпта критика):**
  - Деньги: тело по помесячному графику = долг; `lumpPlan` сверен с независимым дискретным
    графиком `creditSplit` — `saved` расходится не больше чем на 103 ₸ (0,18%): это оценка в
    непрерывных месяцах, теперь сказано в JSDoc. Эталоны Handoff (154 457 / 18 из 24;
    46 400 и 74 820; 50 750 и 27 456; счётчик 181 913) подтверждены. Округление — только
    `finance.ts`.
  - Слияние: две офлайн-отметки, одна пара с двух устройств, снятие и повтор — зелёные;
    **409 на деле не проверялся** (синк сначала делает GET) — добавлен настоящий.
    Коммутативность по содержимому — кроме надгробия с обеих сторон (хвост).
  - Производные остатки: экраны читают геттеры стора. Сырой `householdDoc` остался только
    у `budgetAmounts` (Бюджет, Обзор): `principal` там не читается, но из-за этого закрытый
    кредит нельзя убрать из плана (хвост).
  - Viewer: кнопок «Оплатил», досрочки, «Снять», «Оставить?» нет — SSR-тесты под viewer.
- **Исправлено (`dfc7ab0`):**
  - **Якорь сверки двигали обычные операции** (высокая). Взнос в цель со счёта / снятие с
    цели (`GoalDetail`) и внеплановый доход (`Capital`) шли через `setAccountAmount` → якорь
    «сейчас». Итог: снятая после этого отметка не возвращала деньги (Р-7), офлайн-отметка
    партнёра до этого момента после слияния не списывалась, снятие досрочки уносило 200 000
    из капитала. Теперь `shiftAccountAmount`: база ± дельта (`finance.ts` `shiftedBase`),
    якорь прежний; `setAccountAmount` — только ручной ввод остатка.
  - **Правка отметки после сверки списывала платёж второй раз** (высокая): «Другая сумма
    или счёт» → «Сохранить» = снять + отметить с `at` = сейчас. Теперь `editPaid`: новая
    запись с `at` исходной, без изменений — ничего не пишет, проценты месяца кредита — из
    исходной записи (`creditResplit`; правка сентября при отмеченном октябре больше не
    сдвигает долг). «Деньги вернутся на счёт» — только для записи после якоря.
  - Правило «закрытый кредит платежа не ждёт» было в 4 вариантах (2 вью + `finance.ts`),
    расходились при платеже 0 → одно `creditDueIn`; `nextCreditDue` — по `creditDueAmount`;
    «снизить платёж» не даёт платёж 0 при живом остатке.
  - Бюджет: сумма события кредита — `creditDueAmount` (строка показывала обрезанный
    последний платёж, «На обязательства» — полный); отмеченное — суммой отметки, как в
    `untilPayday` (итог = строки).
  - `PaidRow`: признак оценки — из обязательства (в «До зарплаты» коммуналка отмечалась
    одним нажатием по оценке). «Оставить?»: сумма продления годовой (при новой версии на
    месяц продления показывалась старая); подтверждение «Отменить» привязано к подписке
    (синк менял вопрос — удалилась бы другая).
  - `creditBalance` — только `credit`/`prepay` (готово к `kind` зарплаты RP-10); шапка
    `merge.ts` — точная оговорка о коммутативности.
- **Упрощено / реюз:** `atLabel` (`dates.ts`) вместо двух копий; `payableAccounts` вместо двух
  фильтров KZT; `newPayment` в сторе — один конструктор записи и правило Р-5 для
  `markPaid` / `editPaid` / `applyPrepayment`; `NO_ASK_OPTIONS`. Дубли с кодом вне блока (хром
  модалок, склонение, генерация id, чипы) — в промпт ревью frontend.
- **Тесты (`df19ef9`), +38:** эталоны досрочки до тенге и сверка с дискретным графиком
  (150 ₸ / 0,3% вместо 1%), `creditResplit`, `shiftedBase`, `creditDueIn`, «до зарплаты» с
  оплаченным заранее; слияние: сверка против отметки (до/после якоря), сдвиг против
  офлайн-отметки, коммутативность; стор: сдвиг (общий/личный, обрезка у нуля), `editPaid`
  (после сверки, no-op, смена счёта, кредит), другая сумма кредита, досрочка больше остатка,
  граница месяца по Алматы; e2e: настоящий 409, взнос в цель против офлайн-отметки, правка
  после сверки на другом телефоне; SSR: итог группы в Капитале, viewer, «оценка», сумма
  продления.
- **Верификация:** `npm run build` ✅; `npm test` ✅ 26 файлов / 230 тестов (было 192).
  Мутации — вернуть `at` = сейчас в `editPaid` и якорь «сейчас» в сдвиге — 5 и 4 красных
  теста. Браузер (стенд §6, Chrome 390px, `playwright-core`) — 14/14: «Оплатил» → взнос в цель
  с карты → «Снять отметку» — карта 950 000; отметка → сверка 700 000 → «Сохранить» и правка
  на 225 000 — карта 700 000, текст снятия без обещания возврата; коммуналка в «До зарплаты» —
  лист суммы. **CI не запускался** (ветка не пушилась).
- **Принятые границы исполнителя:** модалки Капитала фиксируют месяц при открытии (прошлый
  месяц снимается из Бюджета / «Впереди»); `keptAt` при заведении; первая отметка без
  истории — «не списывать»; `lumpPlan` в непрерывных месяцах.
- **Границы правки критика:** `editPaid` со сменой счёта держит `at` исходной — у нового счёта
  со сверкой позже этой оплаты запись не спишется; счёт «прошлой оплаты» ищется по `at`, и
  сменённый при правке старого месяца счёт «последним» не станет.
- **Хвосты в §4 (6):** закрытый кредит в плане (предложение — RP-12); досрочка при платеже ≤
  процентов; «снизить платёж» — LWW; сдвиг остатка — LWW базы; старый PWA и якорь (держит
  процедура клинапа); тай-брейк `mergeList`.
- **Доки:** в `frontend/README.md` и `NEXT.md` затронутых секций / статусов нет — не правил.
  Промпты этого блока: приёмке — сценарии правок критика, ревью frontend — дубли каталога,
  клинапу — перезапуск PWA до первой отметки и сверки.
- **Промпт Блока 2** актуализирован по коду: поля `Payment`, куда встаёт `kind` зарплаты и где
  знак (`accountBalance`), `newPayment` / `markPaid` / `editPaid` / `unmarkPaid`, якорь только у
  ручного ввода, `paidFor` / `nextObligationDue` / `nextCreditDue` / `creditDueIn` /
  `untilPayday` (геттеры стора, не `householdDoc`), `PaidRow`, хвост закрытого кредита для RP-12.

## Приёмка

## Итоги пост-приёмки (только L)
