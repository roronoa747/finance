# Блок 1 — «Оплатил», досрочка, подписки · сессии

**Цель блока:** одно нажатие «оплатил» для всего по графику — остатки счетов и кредитов
честно выводятся из записей и не теряются при двух офлайн-телефонах; досрочку можно
применить и видеть сэкономленное; подписки собраны в группы и спрашивают «оставить?» без
надоедания.

Сессии: исполнитель ⬜ · критик ⬜ · приёмка ⬜ · ревью frontend ⬜ · клинап ⬜
<!-- Каталоги ревью — по факту коммитов Handoff-а; план — только frontend. -->

---

## Промпт исполнителя — уровень build · Opus 5 · effort high (ultrathink)

```
/worker развитие-приложения 1 ultrathink
```

Блок-специфика:

- Ветка `rp-block-1-payments` от `main` (после деплоя Блока 0). Push ветки — с согласия
  владельца; `main` не трогать.
- Задачи по порядку: `RP-06` → `RP-07` → `RP-08` → `RP-09`. RP-06 — фундамент (модель,
  слияние, математика, стор): не переходить к UI, пока его тесты не зелёные.
- **Деньги (L):** каждая сумма, попадающая в документ или на экран, — из `finance.ts`,
  целая; в компонентах арифметики с деньгами нет. Слияние `payments` — явное (§6), не через
  страховку RP-02 для незнакомых ключей.
- Что оставил Блок 0 (факты кода, сверены критиком 2026-09-24):
  - **`payments` в слиянии:** тип `SyncDoc` → `defaultSyncDoc()` → объект `known` в
    `mergeDocs` (`lib/merge.ts`). Больше никуда: незнакомым считается всё, чего нет в `known`
    (списка `KNOWN_KEYS` больше нет). Документы до Блока 1 приходят без `payments` — читать
    `doc.payments ?? []`.
  - **`payments: []` в `defaultSyncDoc()` обязателен:** сервер (RP-03) хранит ключи верхнего
    уровня, которых нет в push, — «Сбросить данные» (`resetDoc` шлёт `defaultSyncDoc()`)
    обнулит `payments` на сервере, только если ключ прислан.
  - **`mergeList` берёт `{ ...проигравший, ...победитель }`:** поле очищать `null`, не
    `undefined` (в JSON ключ пропадёт, значение проигравшего воскреснет). «Не списывать» —
    `accountId: null`; снятие отметки — надгробие `deletedAt`.
  - **Флаг неотправленного** (`unsent` / `privateUnsent` / `hasUnsent`, ключ `ff_unsent`)
    ставит `mutateHouseholdDoc` / `mutatePrivateDoc` — отметки только через них, иначе выход
    сотрёт их без вопроса. Демо (`financeStore.isDemo`) — отметки работают локально, запросов
    нет.
  - **Защита RP-01** `unchanged()` в сторе (`setAccountAmount`/`updateAccount`, `setDeposit`,
    `correctSalary`, `correctObligation`, `setCategoryAmount`, `setPerson`) сравнивает с тем,
    что отдаёт стор. После производных остатков `setAccountAmount` с тем же видимым остатком
    не пишет ни базу, ни якорь — это верно (остаток не изменился), не «чинить». У
    `updateCredit` защиты нет — якорь при смене `principal` ставить явно.
  - Поле суммы, коммитящее по blur, — `NumFieldBlur` (коммит только изменённого, `numChanged`
    в `lib/num.ts`).
- Среда: локальный стенд (§6), два профиля браузера, телефонная ширина. Офлайн-сценарий —
  DevTools → Offline в обоих профилях, отметки, затем онлайн.
- Верификация: `cd frontend && npm run build && npm test` (весь `e2e/`).

---

## Промпт критика — уровень review · Opus 5 · effort xhigh (ultracode)

```
/critic развитие-приложения 1 ultracode
```

Блок-специфика:

- Деньги: сверить формулы разбивки аннуитета, «снизить платёж», сэкономленных процентов с
  независимым расчётом (другой способ — например, помесячный график); округление только в
  `finance.ts`; нет двойного учёта (дедупликация пары, якорь ручной правки).
- Слияние: две офлайн-отметки, одна пара с двух устройств, снятие и повторная отметка,
  конфликт 409 — тесты есть и зелёные; коммутативность `mergeDocs` с `payments`.
- Все места, читающие остаток (`amount`, `principal`), получают производное значение —
  grep по вью; запись остатка ставит якорь.
- Viewer: кнопок нет; сервер и так отвергает push (403).
- Живые доки: `NEXT.md` — только отметить сделанное в «Развитии после переключения» п.1,
  если там есть статусы.
- Промпт Блока 2 актуализировать: имена полей `payments`, функции «следующий платёж»,
  «оплачено за период», как устроен `kind` (RP-10 добавит зарплату).

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh (ultracode)

```
/accept развитие-приложения 1 ultracode
```

Блок-специфика:

- Критерии брифа Блока 1 на локальном стенде, два профиля: «оплатил аренду» одним нажатием →
  следующий платёж, карта уменьшилась у обоих; две офлайн-отметки с двух профилей — обе
  доехали, без удвоения; досрочка «сократить срок» меняет остаток и срок, показывает
  сэкономленные проценты — сверено с `finance.ts` до тенге; подписка рабочей группы не
  спрашивает «оставить?», досуговая годовая — спрашивает.
- Регрессия: весь `frontend/e2e/` + `npm test`; CI `ci-frontend` зелёный на HEAD ветки.
- Сценарии приёмки — в `frontend/e2e/` (файл Блока 1).
- **L-блок: приёмка НЕ деплоит.** Следующий шаг — `/dir-review развитие-приложения 1 frontend`.

---

## Промпт ревью frontend — уровень review · Opus 5 · effort xhigh

```
/dir-review развитие-приложения 1 frontend
```

Блок-специфика:

- Каталог `frontend/`: как записи платежей легли рядом с `movements` целей и версиями
  обязательств (единообразие слияния), дубли логики «следующий платёж» между `Budget`,
  `Overview`, `Capital`; производные остатки — нет ли места, читающего сырое `amount`.
- Если коммиты блока тронули `backend/` — добавить ревью backend в трекер и промпт.
- Следующий шаг — `/cleanup развитие-приложения 1`.

---

## Промпт клинапа — уровень build · Opus 5 · effort high

```
/cleanup развитие-приложения 1
```

Блок-специфика:

- Выполнить «клинап»-вердикты `REVIEW-frontend.md`.
- **Деплой** (с «да» владельца): записать ID текущего Production-деплоя (откат), merge
  `rp-block-1-payments` → `main`, push. Смоук на `family-finance-ff.vercel.app` с живыми
  данными: остатки кредитов и счетов до первой отметки **те же**, что до деплоя (старые данные
  без записей); одна настоящая отметка владельцем → видно на втором телефоне. **Откат:**
  promote записанного деплоя; записи `payments` в документе прошлый клиент сохранит (RP-02/03).
- «Итоги пост-приёмки», 🏁 в §4. Следующий шаг — `/worker развитие-приложения 2`.

---

## Handoff (заполняет исполнитель)

- **Коммиты:**
- **Что сделано:**
- **Отклонения от ТЗ:**
- **Грабли среды:**
- **Верификация:**

## Итоги критика

## Приёмка

## Итоги пост-приёмки (только L)
