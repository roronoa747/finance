# Блок 7 — Android · сессии

**Цель блока:** тот же код в оболочке Capacitor на Android — нативный вход Google, приём
выписки из «Поделиться» Kaspi, нативный пуш, и приложение в Google Play (закрытое тестирование →
производство); веб-версия при этом не меняет поведения.

Сессии: исполнитель ⬜ · критик ⬜ · приёмка ⬜

---

## Промпт исполнителя — уровень build · Opus 5 · effort high

```
/worker идея-и-редизайн 7
```

Блок-специфика:

- Ветка `b2c-block-7-android` от `main`. Push — с согласия; прод не трогать (env `CORS_ORIGIN`,
  `FCM_*` — в приёмке при деплое).
- Задачи: B2C-33 → B2C-34 → B2C-35 → B2C-36. Владелец заранее: Android Studio + SDK, USB-отладка,
  OAuth-клиент Android (SHA-1), проект Firebase, аккаунт Play Console (25 $) — попросить в
  начале сессии одним списком (AskUserQuestion / инструкция), пока идёт B2C-33.
- Новые зависимости — только из ТЗ (Capacitor и плагины). `frontend/android/` — в git без
  сборочных каталогов; `google-services.json` и keystore — **только** `memory/secrets/android/`.
- CI оболочку не собирает (§6): каждая сборка проверяется на телефоне владельца; веб —
  стандарт §6 (`e2e/`, `npm test`, `go test`).
- Следующий шаг — `/critic идея-и-редизайн 7`.

**Факты кода после Блока 5/6** (заполняет критик предыдущего блока): —

---

## Промпт критика — уровень review · Opus 5 · effort xhigh

```
/critic идея-и-редизайн 7
```

Блок-специфика:

- Веб не регрессировал (`isNative()` ветки, `baseUrl`, SW); CORS — только нужные origins; FCM
  токен сервисного аккаунта не в логах; приём share — размер/тип/viewer; секреты не в git
  (`git ls-files` по `google-services`, `.keystore`, `.jks`).
- Актуализировать промпт Блока 8 (что из оболочки переиспользуется на iOS); §6 — «после Блока 7».
- Следующий шаг — `/accept идея-и-редизайн 7`.

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh

```
/accept идея-и-редизайн 7
```

Блок-специфика:

- Веб: регрессия `e2e/` + `npm test` + `go test`; CI. Оболочка: установка из внутренней
  дорожки Play на оба телефона, чек-лист смоука `STORE.md`.
- **Деплой (M) — с «да» владельца:** (1) ID текущего деплоя; (2) env Vercel `CORS_ORIGIN`,
  `FCM_SERVICE_ACCOUNT`, `FCM_PROJECT_ID`; (3) merge → push; (4) закрытое тестирование в Play
  (5–10 семей по ссылке) — по решению владельца сразу или после веб-проверки; (5) выход в
  производство — отдельное «да». **Откат:** веб — promote; магазин — новый релиз, приостановка
  раскатки в консоли.
- 🏁 в §4, `STATE.md`. Следующий шаг — `/worker идея-и-редизайн 8 ultrathink` (на Mac владельца).

---

## Handoff (заполняет исполнитель)

- **Коммиты:**
- **Что сделано:**
- **Отклонения от ТЗ:**
- **Грабли среды:**
- **Верификация:**

## Итоги критика

## Приёмка
