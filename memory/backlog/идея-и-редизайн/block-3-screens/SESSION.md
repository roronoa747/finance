# Блок 3 — Экраны и лёгкий флоу · сессии

**Цель блока:** приложение выглядит и ведёт себя по утверждённому дизайну — главный экран
продаёт мечту с фото, неделя идёт по одному решению от выписки до раскладки зарплаты, цели с
шаблонами и сторис-карточкой, первый запуск из выписки вместо мастера, а всё сложное живёт на
втором уровне «Деньги» и считает как прежде.

Сессии: исполнитель ⬜ · критик ⬜ · приёмка ⬜ · ревью frontend ⬜ · ревью backend ⬜ · клинап ⬜

---

## Промпт исполнителя — уровень build · Opus 5 · effort high (ultrathink)

```
/worker идея-и-редизайн 3 ultrathink
```

Блок-специфика:

- Ветка `b2c-block-3-screens` от `main` (после деплоя Блока 1; Блок 2 кода не имел). Push — с
  согласия владельца; `main` не трогать; прод-БД не трогать (миграция `000003` в прод — клинап).
- **Спецификация — `../block-2-design/DESIGN.md`** (§2 маршруты, §3 таблица «что → где», §4
  токены, §5 компоненты, §6 тексты, §7 сторис, §8 чек-лист). Фреймы Figma — по ссылкам оттуда,
  через `get_screenshot`/`get_design_context` при сомнении; Figma целиком не читать.
- Задачи по порядку: B2C-12 → B2C-13 → B2C-14 → B2C-15 → B2C-16 (Go) → B2C-17 → B2C-18 →
  B2C-19 → B2C-20 → B2C-21. Самый большой блок инициативы: каждая задача — свой коммит,
  Handoff дописывать по ходу, не в конце.
- Среда: Postgres одноразовый для фото (`DATABASE_URL` у `go run ./cmd/server`, не Supabase);
  два профиля браузера + viewer, 390px, **обе темы** на каждом экране.
- Деньги: ни одного расчёта в компонентах — `finance.ts` / `lib/statements`; «Свободно по
  факту» и раскладка проверяются ручным расчётом по документу (пример — в Handoff).
- e2e — **адаптировать, не удалять** (B2C-21); удаление сценария — с причиной в Handoff.
- Новые зависимости — нет (всё на pdf.js/Vue/canvas/Web Share). Google Fonts — по `DESIGN.md`.
- Верификация: `cd frontend && npm run build && npm test`; `cd backend && go build ./... && go
  vet ./... && go test ./...` + PG-режим; корень `api/`; браузер по критериям каждого ТЗ.
- Следующий шаг — `/critic идея-и-редизайн 3 ultracode`.

**Факты кода после Блока 1** (критик Блока 1, 2026-09-27; подробно — §6 индекса «После Блока 1»):

- Экран выписок — `views/Statements.vue` (`/statements`, ленивый; pdf.js грузится при выборе
  файла) → B2C-13 делает адрес `/week` с редиректом со `/statements`. Его тесты
  (`views/Statements.test.ts`, `stores/operations.test.ts`, `e2e/b2c-block1-parser.test.ts`) —
  **адаптировать, не удалять**.
- Стор `stores/operations.ts`: `all` / `ops` (своя копия), `draft` → `draftOps` (правила +
  пары внутренних), `answer(match, to)` (правило), `send()` (сначала `pull`, затем итоги
  `writeTotals` и очередь), `recategorize(match, to)` (смена раздела задним числом), `pull`,
  `flush`, `pendingCount`, `uploads`, `status` / `lastError` (в UI пока не выведены — хвост §4 →
  B2C-21). Итоги в документ пишет только `writeTotals` — B2C-15/19 зовут её, а не пишут
  `spendTotals` сами.
- Чистые функции для экранов уже есть — **переиспользовать, не дублировать**: `picture(totals,
  week, month)` (сумма обоих по разделам — основа `weekPicture` B2C-14), `unknownGroups(ops,
  categoryId)`, `partnerHints`, `draftSummary`, `periodsOf`, `weekKey` / `weekRange`
  (`lib/dates.ts`).
- B2C-15: `MerchantRule.to` — союз `{ categoryId } | { internal: true } | { person }`, новый
  вариант `{ payment }` добавить туда и в `fromRule` (`model.ts`); `applyRules` сравнивает только
  `categoryId` и `internal`; `categorize` отдаёт `personLabel`, но его никто не хранит (хвост §4).
  Платёж по Kaspi Кредиту — `transfer-out` «Оплата Kaspi Кредита» (раздел `sc_credit`); вид
  `income` парсеры **не ставят** — зарплата ищется среди `transfer-in`.
- B2C-19: операции одной выписки — `draft.files[].parsed.operations` (до отправки); повторяемость
  — по `normalizeMerchant` / `normalizeCounterparty`.
- B2C-21 (правила и разделы): `removeMerchantRule` — надгробие; **после удаления правила
  `{ internal: true }` пересчитать `internal` от разбора** (парсер + `pairInternalTransfers`) —
  `categorize` без правила оставляет текущее `op.internal`. `spendCategories` сеются один раз
  (`seedSpendCategories`) при первой отправке.
- **Не трогать нормализацию:** `sanitize` / `normalizeMerchant` входят в id операции — их правка
  после деплоя Блока 1 удвоит повторные загрузки (словарь в id не входит).

---

## Промпт критика — уровень review · Opus 5 · effort xhigh (ultracode)

```
/critic идея-и-редизайн 3 ultracode
```

Блок-специфика:

- Деньги: `freeByFact` (двойной учёт обязательств против выписок — `plannedElsewhere`),
  сопоставление (одна операция — одна отметка, повторная загрузка — без дубля, `unmark`),
  `allocations` (повторная раскладка невозможна, слияние с двух телефонов), `closerWish`.
- Дизайн: соответствие `DESIGN.md` — токены в обеих темах, цвет только из токенов (grep на
  hex и `bg-black/`), тексты §6, карточка сторис без сумм.
- Права: viewer без форм на всех новых экранах; скрытые подарки — не в общем документе, фото
  партнёру 404; токен не в URL картинок; object URL освобождаются.
- Go: миграция `000003` только добавляет; `bytea` с обоими DSN; заголовки кэша.
- Актуализировать промпт Блока 4 по факту кода (маршруты, `/start`, `/settings`, стор
  операций/фото), §6 индекса — строка «после Блока 3».
- Следующий шаг — `/accept идея-и-редизайн 3 ultracode`.

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh (ultracode)

```
/accept идея-и-редизайн 3 ultracode
```

Блок-специфика:

- Стенд с Postgres, два профиля + viewer, 390px, обе темы: главный (герой, неделя, «Свободно»
  посчитано руками), неделя от загрузки настоящей выписки до раскладки, сопоставление
  (кредит/аренда/зарплата), цель из шаблона с фото у партнёра, подарок скрыт (404), сторис
  без сумм, первый запуск новой семьи ≤ 7 вопросов, второй уровень — все прежние операции
  денег; демо на новых экранах.
- Регрессия: весь `frontend/e2e/` + `npm test`; `go test ./...` + PG; CI зелёные.
- Сценарии приёмки — `frontend/e2e/b2c-block3-screens.test.ts`.
- **L-блок: приёмка НЕ деплоит.** Следующий шаг — `/dir-review идея-и-редизайн 3 frontend`.

---

## Промпт ревью frontend — уровень review · Opus 5 · effort xhigh

```
/dir-review идея-и-редизайн 3 frontend
```

Блок-специфика:

- Каталог `frontend/`: дубли старых и новых экранов/компонентов (недоснесённое: `Overview`,
  `Ritual`, `Setup`, `HomeView`, `PaidRow`/`SalaryRow`), старые токены без потребителей,
  расчёты в компонентах, размер чанков, кит против самодельного хрома.
- Следующий шаг — `/dir-review идея-и-редизайн 3 backend`.

---

## Промпт ревью backend — уровень review · Opus 5 · effort xhigh

```
/dir-review идея-и-редизайн 3 backend
```

Блок-специфика:

- Каталог `backend/` (+ корень): репозиторий фото рядом с выписками/документами, хендлеры
  фото (роли, 404 vs 403, лимиты), миграция `000003`.
- Следующий шаг — `/cleanup идея-и-редизайн 3`.

---

## Промпт клинапа — уровень build · Opus 5 · effort high

```
/cleanup идея-и-редизайн 3
```

Блок-специфика:

- Выполнить «клинап»-вердикты `REVIEW-frontend.md`, `REVIEW-backend.md`; снести старые токены
  без потребителей.
- **Деплой — каждый шаг с «да» владельца:** (1) ID текущего Production-деплоя; (2) `cmd/migrate`
  против прод-БД (`000003`); (3) merge `b2c-block-3-screens` → `main`, push. **Перезапуск PWA
  на обоих телефонах** до первой загрузки/раскладки. Смоук на проде: главный с героем у
  обоих, неделя на настоящей выписке, цель с фото видна партнёру, старые адреса открывают
  новые экраны. **Откат:** promote записанного деплоя; таблица фото остаётся; новые ключи
  документа (`spendCategories` и т.д. — уже с Блока 1; `allocations`, `Goal.main/photoId`)
  старый клиент сохраняет.
- «Итоги пост-приёмки», 🏁 в §4, `STATE.md`. Следующий шаг — `/worker идея-и-редизайн 4 ultrathink`.

---

## Handoff (заполняет исполнитель)

- **Коммиты:**
- **Что сделано:**
- **Отклонения от ТЗ:**
- **Грабли среды:**
- **Верификация:**

## Итоги критика

## Приёмка

## Итоги пост-приёмки (только L)
