# Блок 5 — Недельный ритм · сессии

**Цель блока:** приложение само зовёт загрузить выписку — пуш обоим в выбранный день и час по
Алматы, отдельно напоминает отставшему партнёру, а картина недели честно показывает «без
выписки <имя>»; ритм «раз в неделю» держится не памятью, а уведомлением.

Сессии: исполнитель ⬜ · критик ⬜ · приёмка ⬜

---

## Промпт исполнителя — уровень build · Opus 5 · effort high

```
/worker идея-и-редизайн 5
```

Блок-специфика:

- Ветка `b2c-block-5-weekly-rhythm` от `main`. Push — с согласия; прод и прод-БД не трогать
  (миграция `000007` — в приёмке при деплое).
- Задачи: B2C-29 (Go + workflow) → B2C-30. Новая зависимость, разрешённая ТЗ:
  `github.com/SherClockHolmes/webpush-go` (и в корневой `go.mod`).
- Секреты: VAPID-ключи сгенерировать локально (тестовая пара — для стенда; прод-пара —
  владелец/агент при деплое), `CRON_SECRET` — случайная строка; значения — `memory/secrets/
  push.md`, env Vercel, Secrets GitHub; в доки — имена.
- Стенд: Chrome на localhost получает Web Push без HTTPS; cron вызывать `curl` с секретом и
  dev-параметром `?now=`.
- Верификация — стандарт §6; браузер по критериям B2C-30.
- Следующий шаг — `/critic идея-и-редизайн 5`.

**Факты кода после Блока 4** (заполняет критик Блока 4): —

---

## Промпт критика — уровень review · Opus 5 · effort xhigh

```
/critic идея-и-редизайн 5
```

Блок-специфика:

- `due(...)`: часовой пояс Алматы, «не чаще раза в неделю», второй пуш только отставшему, одиночка;
  удаление мёртвых подписок; cron без секрета; время работы функции.
- SW: `sw.js` scope прежний, `importScripts` в собранном `dist`; ключи не в логах.
- Актуализировать промпт Блока 6 (если открыт) или Блока 7; §6 — строка «после Блока 5».
- Следующий шаг — `/accept идея-и-редизайн 5`.

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh

```
/accept идея-и-редизайн 5
```

Блок-специфика:

- Стенд: подписка с двух профилей, cron в нужный час → оба получили; партнёр без загрузки → на
  следующий день второй пуш только ему; настройки дня/часа; viewer; iPhone-подсказка (эмуляция
  UA). Регрессия: весь `e2e/` + `npm test`; `go test` + PG; CI.
- Сценарии — `frontend/e2e/b2c-block5-weekly-rhythm.test.ts`.
- **Деплой (M) — с «да» владельца:** ID текущего деплоя → env Vercel (`VAPID_*`, `CRON_SECRET`)
  и Secrets GitHub (`CRON_SECRET`, домен) → `cmd/migrate` (`000007`) → merge → push → включить
  workflow `weekly-push.yml` (`workflow_dispatch` — проверка ответа `{sent, removed}`) →
  перезапуск PWA на обоих телефонах → смоук: подписка с телефона владельца (Android),
  тестовый вызов cron в ближайший час → уведомление на телефоне. **Откат:** promote; workflow
  выключить; таблица остаётся.
- 🏁 в §4, `STATE.md`. Следующий шаг — по §4: Блок 6 открыт → `/worker идея-и-редизайн 6
  ultrathink`; отменён → `/worker идея-и-редизайн 7`.

---

## Handoff (заполняет исполнитель)

- **Коммиты:**
- **Что сделано:**
- **Отклонения от ТЗ:**
- **Грабли среды:**
- **Верификация:**

## Итоги критика

## Приёмка
