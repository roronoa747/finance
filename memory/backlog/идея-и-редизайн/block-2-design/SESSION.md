# Блок 2 — Дизайн · сессии

**Цель блока:** утверждённый владельцем облик продукта — мудборд, ключевые экраны и лэндинг в
Figma (светлая и тёмная), таблица «что есть → где живёт» и спецификация `DESIGN.md`, по которой
Блок 3 переносит дизайн в код без догадок.

Сессии: исполнитель-дизайнер ⬜ · утверждение владельца (= приёмка) ⬜ · 🏁
<!-- Блок без кода (§5 индекса): критика и приёмки кодом нет; приёмка = утверждение владельцем,
записанное ниже. -->

---

## Промпт исполнителя-дизайнера — уровень deep · Fable 5.1 · effort high

```
/worker идея-и-редизайн 2
```

Блок-специфика (переопределяет generic-роль исполнителя):

- **Кода нет.** Артефакты — файл Figma «Family Finance — B2C» (страницы «00 Компоненты»,
  «01 Референсы», «02 Мудборд», «03 Экраны», «04 Лэндинг») и
  `memory/backlog/идея-и-редизайн/block-2-design/DESIGN.md`. Ветка не нужна: доки коммитятся в
  `main` (`B2C-09: …`, `B2C-10: …`, `B2C-11: …`).
- Модель сессии — **Fable 5.1, effort high** (решение владельца 2026-09-26): направление и
  вкус, а не чек-лист. `/model` до старта.
- Инструменты: Figma MCP — **сначала загрузить навык `/figma-use`** (обязателен перед
  `use_figma`), затем `create_new_file` / `use_figma` / `get_screenshot`; Mobbin MCP —
  `search_screens` один раз, отказ → публичные скриншоты по ссылкам (в репо картинки не
  класть); скриншоты текущего приложения «было» — локальный стенд (§6 индекса) + Playwright,
  в scratchpad.
- **Владелец в контуре**: после мудборда (B2C-09), после каждой группы экранов (B2C-10) и по
  таблице §3 (B2C-11) — AskUserQuestion с вариантами и рекомендацией; правки — ещё круг. Не
  идти дальше без утверждения предыдущего шага.
- Факты о том, что показывает приложение после Блока 1 (поля операции, разделы трат, экран
  `/statements`, «без выписки <имя>», незнакомые названия) — ниже в этом файле (актуализирует
  критик Блока 1). Данные макетов — правдоподобные, не реальные суммы семьи.
- Тон и запреты — Р-11; карточка сторис без сумм — Р-10; тёмная тема у каждого экрана.
- Верификация: ссылки на фреймы открываются; `DESIGN.md` §1–§8 заполнены; у каждого токена —
  пара; проверка полноты по критериям B2C-11.
- Финал: секция «Приёмка» ниже — что утвердил владелец и когда; 🏁 в §4 индекса и `STATE.md`
  ставит эта же сессия по слову владельца. Следующий шаг — `/worker идея-и-редизайн 3 ultrathink`.

**Факты после Блока 1** (заполняет критик Блока 1): —

---

## Промпт критика — не проводится (§5: блок без кода)

## Промпт приёмки — не проводится (§5: приёмка = утверждение владельца)

---

## Handoff (заполняет исполнитель-дизайнер)

- **Коммиты:**
- **Что сделано:** (ссылки на страницы Figma; направление; фраза-идея)
- **Отклонения от ТЗ:**
- **Грабли среды:** (Mobbin — работает / отказ; Figma MCP)
- **Верификация:**

## Приёмка (утверждение владельца)

<направление · экраны (группы) · таблица §3 — «утверждено», дата; правки, если были>
