# Блок 1 — Парсер и операции · сессии

**Цель блока:** выписки Kaspi и Freedom разбираются на телефоне в личные операции без ФИО и
номеров, семья видит итоги по разделам, повторная загрузка ничего не удваивает — владелец и
партнёр начинают недельный ритм ещё до редизайна; замер незнакомых названий решает судьбу
Блока 6.

Сессии: исполнитель ✅ 2026-09-26 · критик ✅ 2026-09-27 · приёмка ✅ 2026-09-27 · ревью backend ⬜ · ревью frontend ⬜ · клинап ⬜

---

## Промпт исполнителя — уровень build · Opus 5 · effort high (ultrathink)

```
/worker идея-и-редизайн 1 ultrathink
```

Блок-специфика:

- **Предусловие:** корпус выписок владельца лежит в `memory/secrets/statements/kaspi/` и
  `memory/secrets/statements/freedom/` (вне git). Нет — остановиться на B2C-01, написать в
  Handoff и попросить владельца; дальше не идти.
- Ветка `b2c-block-1-parser` от `main`. Push — с согласия владельца; `main` не трогать;
  **прод-БД не трогать** (миграция `000002` в прод — клинап).
- Задачи по порядку: B2C-01 → B2C-02 → B2C-03 → B2C-04 → B2C-05 → B2C-06 (Go) → B2C-07 →
  B2C-08. Формат выписок — только из `CORPUS.md` (B2C-01), не по памяти.
- Среда: для B2C-06/B2C-07 нужен реальный Postgres — локальный/Docker одноразовый
  (`TEST_DATABASE_URL` и `DATABASE_URL` для `go run ./cmd/server`, **не** Supabase; строка — в
  `memory/secrets/`, в Handoff — только ссылка); на моках — хендлер-тесты. Два профиля
  браузера (владелец и партнёр), 390px.
- Приватность (L): ни одно текстовое поле операции не содержит 6+ цифр подряд и полного ФИО
  (Р-23) — проверяется тестами парсеров, гвардом сервера и SQL-запросом на стенде; файл
  выписки на сервер не уходит (Network). В Handoff — ни имён, ни сумм из корпуса.
- Новые зависимости, разрешённые ТЗ: `pdfjs-dist` (B2C-01); одна библиотека xlsx — только если
  Freedom в корпусе xlsx (B2C-04). Другие — нет.
- Верификация: `cd frontend && npm run build && npm test` (фикстуры, e2e); `cd backend && go
  build ./... && go vet ./... && go test ./...` + PG-режим; корень `go vet ./api/... && go test
  ./api/...`; браузер по критериям B2C-07.
- Handoff: помимо обычного — цифры замера B2C-08 и строка в §4 «Блок 6 открыт/отменён»;
  сколько файлов корпуса разобрано без `skipped`.
- Следующий шаг — `/critic идея-и-редизайн 1 ultracode`.

---

## Промпт критика — уровень review · Opus 5 · effort xhigh (ultracode)

```
/critic идея-и-редизайн 1 ultracode
```

Блок-специфика:

- Приватность: перебрать все пути текста из выписки до сервера (парсер → `sanitize` →
  правило → батч → SQL) — цифры 6+ и ФИО нигде не просачиваются; фикстуры и `CORPUS.md` без
  имён/сумм; логи Go без тел операций.
- Деньги: `spendTotals` = сумма списаний по разделам, внутренние переводы и приходы исключены;
  пересчёт итогов из **всех** операций периода, а не только новых; повторная загрузка — те же id.
- Личный документ: 409, правка во время запроса, два устройства одного человека; правило
  продавца не утекает партнёру.
- Go: миграция `000002` только добавляет, `app.`-квалификация, PK `(user_id, id)`, курсор без
  пропусков, viewer 403, чужая семья ничего не видит.
- Актуализировать промпт Блока 2 (дизайн): что реально показывает загрузка (поля операции,
  разделы трат, «без выписки <имя>», незнакомые) — дизайнеру нужны факты, не догадки; и
  промпт Блока 3 в части фактов кода (стор операций, экран `/statements`).
- Решение по Блоку 6 (B2C-08) — проверить арифметику и запись в §4/шапке.
- Следующий шаг — `/accept идея-и-редизайн 1 ultracode`.

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh (ultracode)

```
/accept идея-и-редизайн 1 ultracode
```

Блок-специфика:

- Стенд с Postgres, два профиля + viewer, 390px: настоящая выписка из корпуса каждого банка →
  предпросмотр → вопросы → отправка; SQL-проверка таблиц (нет 6+ цифр, операции только у
  владельца строки); партнёр видит итоги и загрузки, операций не видит; повторная загрузка не
  удваивает; офлайн-загрузка досылается; viewer без кнопки; демо без запросов.
- Регрессия: весь `frontend/e2e/` + `npm test`; `go test ./...` + PG-режим; CI `ci-backend`,
  `ci-frontend` зелёные (push ветки — с «да» владельца).
- Сценарии приёмки — `frontend/e2e/b2c-block1-parser.test.ts`.
- Docker Desktop (Postgres `ff-b2c-pg`) — запустить первым делом: движок поднимается минутами,
  у критика завис (PG-режим после критика не перезапускался — бэкенд он не менял). Правки
  критика для проверки на стенде: ФИО Freedom → «Имя Ф.», второе устройство того же человека
  (итоги = все операции), курсор с запасом — «Итоги критика».
- **L-блок: приёмка НЕ деплоит.** Следующий шаг — `/dir-review идея-и-редизайн 1 backend`.

---

## Промпт ревью backend — уровень review · Opus 5 · effort xhigh

```
/dir-review идея-и-редизайн 1 backend
```

Блок-специфика:

- Каталог `backend/` (+ корневые `api/`, `go.mod`, если тронуты). Фокус: репозиторий выписок
  рядом с `doc_repo`/`household_repo` (единообразие, моки), хендлеры рядом с `sync.go` (роли,
  коды, лимиты тел), миграция `000002`, гвард цифр, отсутствие тел в логах.
- Следующий шаг — `/dir-review идея-и-редизайн 1 frontend`.

---

## Промпт ревью frontend — уровень review · Opus 5 · effort xhigh

```
/dir-review идея-и-редизайн 1 frontend
```

Блок-специфика:

- Каталог `frontend/`: `lib/statements/*` (чистота функций, деньги целые, дубли с `finance.ts`),
  стор операций против стора документа (дубли механики синка, `localStorage`), параметризация
  синка личного документа, ленивые чанки pdf.js/xlsx, фикстуры и скрипты.
- Следующий шаг — `/cleanup идея-и-редизайн 1`.

---

## Промпт клинапа — уровень build · Opus 5 · effort high

```
/cleanup идея-и-редизайн 1
```

Блок-специфика:

- Выполнить «клинап»-вердикты `REVIEW-backend.md`, `REVIEW-frontend.md`.
- **Деплой — каждый шаг с «да» владельца:** (1) записать ID текущего Production-деплоя;
  (2) `cmd/migrate` против прод-БД (сессионное подключение 5432, строка — `memory/secrets/
  supabase-db.md`) — применяется `000002`, прошлый деплой на новой схеме работает (Р-19);
  (3) merge `b2c-block-1-parser` → `main`, push. **Перезапуск PWA на обоих телефонах** до первой
  загрузки. Смоук на проде: владелец загружает настоящую выписку с телефона → партнёр видит
  итоги на своём; SQL-проверка таблиц на цифры (только чтение). **Откат:** promote записанного
  деплоя; таблицы остаются (не мешают).
- «Итоги пост-приёмки», 🏁 в §4, `STATE.md`. Следующий шаг — `/worker идея-и-редизайн 2`
  (дизайн; исполнитель — Fable 5.1, см. SESSION.md Блока 2).

---

## Handoff (заполняет исполнитель)

Исполнитель 2026-09-26, ветка `b2c-block-1-parser` (от `main` `dfebb90`, **не запушена**; прод-БД не
тронута).

- **Коммиты:** `97fb7ae` B2C-01 · `e20e31a` B2C-02 · `ba840dc` B2C-03 · `3fcc9cd` B2C-04 ·
  `018f867` B2C-05 · `01d01e5` B2C-06 · `cb3e582` + `df74894` B2C-07 · `f176835` B2C-08 (+ коммит
  доков этой сессии).
- **Что сделано:**
  - **B2C-01.** Корпус: по одному PDF на банк (Kaspi Gold 10 стр. / 345 операций, Freedom Super Card
    31 стр. / 643 строки с датой), оба за 3 месяца; xlsx нет. `CORPUS.md` — формат, подвохи,
    что вычищать, сверка с итогами банков. `lib/statements/pdf.ts` — `groupItems` (чистая) +
    `pdfToRows` (браузер — обычная сборка и worker-URL, Node — legacy; выбор в одном месте).
    `scripts/statement-rows.mjs` (снимает строки корпуса в scratchpad; импортирует тот же `pdf.ts`
    — Node 24 снимает типы сам). Фикстуры — **синтетика** (см. отклонения):
    `scripts/statement-fixtures.mjs` → `fixtures/{kaspi,freedom}-0{1,2}.rows.json`; гвард
    `fixtures.test.ts` (6+ цифр, IBAN, пароли, фамилии из `memory/secrets/statements/README.md`
    — список создан, фамилию партнёра владелец проверит/допишет).
  - **B2C-02.** `types.ts`, `dictionary.ts` (17 разделов `sc_*`, словарь целыми словами — `\b` в JS
    не видит кириллицу, поэтому своя граница), `model.ts` (`sanitize` — ещё и IBAN целиком и
    полное ФИО → «Имя Ф.»; `normalizeMerchant`, `fingerprint` cyrb53 14 hex + `assignIds` с
    ordinal; `categorize`/`applyRules`; `pairInternalTransfers` — только между **разными**
    банками и без получателя; `matchPerson`; `spendTotals`), `weekKey`/`weekRange` в `dates.ts`;
    `spendCategories`/`spendTotals` в `SyncDoc`, `defaultSyncDoc`, `mergeDocs`; `merchantRules`
    в личном документе, `addMerchantRule` (то же совпадение — правка записи) / `removeMerchantRule`.
  - **B2C-03 / B2C-04.** Парсеры Kaspi (колонки по серединам заголовков, переносы — строка без
    даты сразу ниже) и Freedom (колонки — по заголовкам, но «Детали» левее своего заголовка; блоки
    по зазорам ≥ 16 pt; разрыв ячейки между страницами; сумма ниже даты; ФИО → «Имя Ф.»;
    конвертации валют — внутренние; USD/EUR → `skipped` + `skippedForeign`), реестр
    `detectBank`/`parseStatement`, `StatementFormatError`.
  - **B2C-05.** Общий цикл `exchange` в сторе (слить → push → 409 со слиянием `server_doc`, до 4
    раз; правка во время запроса не затирается) — на нём и `syncHousehold`, и новый `syncPrivate`
    (оптимистичный push со своей ревизией: 409 сам отдаёт копию сервера, лишнего GET нет).
    `mergePrivateDocs`; `pullPrivateDoc` при неотправленном сливает; движок синкает личный
    теми же событиями; бейдж — `syncStatus` (общий + личный). `pushPrivateDoc(data)` оставлен
    обёрткой — на него опираются «Выйти» и старые тесты.
  - **B2C-06.** Миграция `000002` (+ CHECK в базе на вид, длину и 6+ цифр — третья линия),
    `StatementRepository` (pg + мок в `mock_repo.go`), ручки `POST/GET /api/statements`,
    `POST /api/operations/batch`, `GET /api/operations`, README, e2e в обоих режимах, PG-тесты.
  - **B2C-07.** `stores/operations.ts`, экран `/statements` (ленивый; pdf.js — только при выборе
    файла), пункт «Загрузить выписку» первым в «+» (не у viewer), карточка «Выписки недели» в
    Ритуале, досыл очереди — движком синка. Тесты: стор, SSR экрана, сквозной
    `e2e/b2c-block1-parser.test.ts` (фейк ручек B2C-06 — `e2e/support/family.ts`).
  - **B2C-08.** `corpus.test.ts` (фикстуры — всегда, с эталоном руками; корпус — по
    `STATEMENTS_DIR`, в CI пропущен). **Цифры замера** (среднее по 4 месяцам корпуса, оба банка):
    **до** пополнения словаря — 42,7 % строк трат / 19,5 % суммы; **после** — **28,2 % / 15,7 %**.
    Порог Р-29: 15 % строк или 25 % суммы → **Блок 6 открыт** (строка в §4 и шапке). **Состав
    незнакомого:** 56,6 % строк — «ИП <фамилия>» (по названию раздел не угадает и ИИ), 3,7 % —
    переводы без продавца, 39,7 % — прочие покупки (≈ 11 % всех строк трат — ниже порога).
    Решение по Р-29 механическое; если владелец сочтёт, что ИИ по «ИП …» не поможет, — правка
    порога/решения по Блоку 6 записью в `memory/decisions/` (не рабочей сессией). В словарь
    добавлены только общие: сеть Orken/Оркен, Yandex Delivery и запасное «market/маркет/magazin/
    магазин» → продукты (последним, частные записи сильнее).
  - **Корпус без `skipped` (кроме валюты):** Kaspi — 1 файл, 0 пропусков; Freedom — 1 файл,
    47 пропусков, все — операции в USD/EUR.
- **Отклонения от ТЗ:**
  - **Фикстуры — синтетика, а не обезличенный корпус** (B2C-01 п. 4). Автопроверка прав
    заблокировала запись производных от выписок файлов в репозиторий; владелец выбрал вариант
    «в git ничего из выписок» (названия продавцов раскрывают, где семья покупает).
    `statement-anon.mjs` не заведён, вместо него — генератор синтетики по раскладке `CORPUS.md`;
    настоящий корпус проверяется только локально. По **две** фикстуры на банк, но из синтетики
    (корпус — по одному файлу на банк).
  - Зарплата и «подписка отдельным видом» в корпусе не встретились — в фикстурах не выдуманы
    (формат неизвестен); `income` пока никто не ставит.
  - Файл — только PDF (`accept` без xlsx): xlsx в корпусе нет, библиотека не добавлена (Р-4).
  - Курсор `GET /api/operations`: страница **не рвётся внутри одного `updated_at`** (у строк
    одного батча одна метка `now()`) — иначе курсор терял бы строки; страница может быть длиннее
    `limit` на размер батча (≤ 2000). Формат `since`/`next` — как в ТЗ.
  - Итоги, из которых ушли все траты, **обнуляются, а не удаляются** (надгробие навсегда закрыло
    бы id `участник:вид:период:раздел`).
  - Разделы трат сеются при первой **отправке**, а не при открытии предпросмотра (в предпросмотре
    — набор по умолчанию, если в документе пусто).
  - Локальная копия операций привязана к **семье и пользователю**, и выход (`clearLocal`) стирает
    её ключи — сквозной тест нашёл утечку: на общем телефоне партнёр подхватил бы копию с диска.
  - `cmd/transfer -replace` чистит и новые таблицы (TRUNCATE падал на внешних ключах);
    `migrate_test`/`cmd/migrate` — ожидания «ровно 1 миграция / 7 таблиц» заменены на счёт по
    встроенным `*.sql` / 9 таблиц.
- **Грабли среды:**
  - **Go на машине не было** — поставлен с «да» владельца: `winget install GoLang.Go` (1.27.0,
    `C:\Program Files\Go\bin`; в bash — `export PATH="/c/Program Files/Go/bin:$PATH"`). Кэша
    embedded-postgres прошлых сессий тоже нет.
  - **Postgres** — Docker `postgres:16`, контейнер `ff-b2c-pg` (только 127.0.0.1:55432, базы
    `fftest` и `ffdev`); строки — `memory/secrets/local-pg.md`. Docker Desktop надо запускать.
  - `core.autocrlf=true`: рабочие файлы в CRLF → `gofmt -l` перечисляет почти все Go-файлы (в
    индексе LF, в CI чисто); `e2e/pwa-build.test.ts` «иконка React-PWA» **падает локально** —
    `frontend/public/favicon.svg` в рабочей копии CRLF, корневой — LF (к блоку не относится; хвост
    «Уборка старого»).
  - vitest 5 прячет `console.log` зелёных тестов — отчёт замера видно с `--reporter=verbose` или
    в файле `CORPUS_REPORT`.
  - **PWA:** прекэш — `**/*.{js,…}`, а worker pdf.js — `.mjs` (1,3 МБ): офлайн разобрать файл
    нельзя (отправка без сети — можно). Решение — критику/Блоку 3: добавить `mjs` в прекэш или нет.
- **Верификация:**
  - Фронт: `npm run build` ✅ (pdf.js — отдельные чанки `pdf-*.js` 431 КБ и worker `.mjs`, в
    главном `index-*.js` нет; экран — ленивый `Statements-*.js`); `npm test` — 829 ✅, 1 пропуск
    (замер корпуса без `STATEMENTS_DIR`), 1 ❌ — `pwa-build` «иконка» (CRLF, выше).
  - Бэкенд: `go build ./... && go vet ./... && go test ./...` ✅; PG-режим `TEST_DATABASE_URL=… go
    test -p 1 -count=1 -run Postgres ./...` ✅ (в т.ч. 4 новых `TestPostgresStatements*`); корень
    `go vet ./api/... && go test ./api/...` ✅. CI не запускался (ветка не запушена — push с
    согласия владельца).
  - Корпус: Kaspi 345 / 0 пропусков / 8 итогов банка сошлись; Freedom 596 + 47 валютных = 643;
    в тексте операций нет 6+ цифр и фамилий из списка.
  - Стенд (Go `cmd/server` на `ffdev` + Vite, Chrome 390px, профили «Алихан», «Дана», второй вход
    «Алихана»): настоящая выписка Kaspi → предпросмотр (345, на экране нет 6+ цифр) → ответ на
    вопрос → «Отправить»; `app.operations` — 345 строк, 0 с 6+ цифрами; партнёр видит запись
    загрузки и таблицу, `GET /api/operations` отдаёт ему 0, правила Алихана у него нет;
    повтор файла — «Все 345 уже были», строк 345; Freedom без сети — «отправятся при сети», после
    сети 941 (= 345 + 596); второй вход Алихана получил правило продавца (**браузерный критерий
    B2C-05**); запросы на запись — 15, все JSON, ни одного с `%PDF`, самый большой 128 КБ. Ручки
    на стенде — отдельно запросами: партнёр — 0 операций, 400 на 7 цифр, в логах сервера тел нет.
    Браузер pdf.js (B2C-01): временный вызов `pdfToRows` в `main.ts` (снят) — 417 и 971 строк,
    как в Node, worker грузится, ошибок нет.

## Итоги критика

Критик 2026-09-27 (Opus 5.5, xhigh). Дифф `dfebb90..9e16ebd` прочитан целиком против ТЗ B2C-01…08;
коммиты критика: `bc78dee` B2C-02 · `134af53` B2C-04 · `ff095f1` + `146eef4` B2C-07 (+ коммит доков).

- **Находки и правки (код):**
  1. **Р-23, полное ФИО у Freedom (важная).** Сокращение ФИО держалось на списке окончаний
     отчества: «…ична», «Серік ұлы» отдельным словом, ФИО без отчества и двойная фамилия ушли бы
     на сервер целиком (у чужих семей B2C — обязательно). Парсер Freedom теперь опирается на
     « .», которую банк ставит после ФИО отправителя (`FULL_NAME_LEAD`), `sanitize` знает
     «-ична» и двойную фамилию. Корпус владельца перепроверен локально (печатались только формы
     строк): все `counterparty` — «Имя Ф.» / «Имя Имя Ф.», 6+ цифр нет, результат до/после правки
     совпал.
  2. **Итоги со второго устройства одного человека.** `send()` считал итоги из локальной копии, не
     дождавшись `pull`: устройство без свежих операций затёрло бы полные итоги (LWW по id).
     Теперь `send()` сначала `pull()`; заодно двойное «Отправить» — одна отправка.
  3. **Курсор `updated_at` = начало транзакции:** загрузка, начатая раньше соседней и
     закоммиченная позже, вставала «за» курсор и навсегда пропадала на втором устройстве. `pull`
     берёт с запасом 60 с (`CURSOR_OVERLAP_MS`), курсор назад не уезжает; повтор строк безвреден.
  4. **Freedom молча терял кусок таблицы без даты** посреди страницы (и «перенос» с последней
     страницы) — теперь это `skipped` (ТЗ B2C-03/04). На корпусе пропусков по-прежнему 47 (валюта).
  - Тесты: +4 (`model.test` «-ична»/двойная фамилия; `freedom.test` ФИО по точке и кусок без
    даты; `operations.test` pull перед итогами + двойное нажатие; курсор с запасом — в
    существующем тесте). Мутация: без `await pull` тест второго устройства падает.
- **Проверено без правок:** миграция `000002` только добавляет, всё `app.`, PK `(user_id, id)`,
  CHECK 6+ цифр, чужой `upload_id` → NULL (подзапрос), курсор не рвёт батч, viewer 403 кроме
  списка загрузок, чужая семья не видит загрузок, тел операций в логах нет; мок повторяет pg;
  `exchange` для общего документа эквивалентен прежнему `syncHousehold`; личный — 409, правка в
  полёте, досыл движком; `mergePrivateDocs` — по id; `spendTotals` — только списания, без
  внутренних, из всех операций периода, повтор — те же id. **Арифметика Р-29:** (22,2 + 23,8 +
  24,7 + 41,9) / 4 = 28,2 % строк, (16,7 + 11,2 + 11,2 + 23,8) / 4 = 15,7 % суммы → порог строк
  15 % превышен, «Блок 6 открыт» в §4 и шапке верно (прогон замера на корпусе повторён).
- **Упрощение/реюз:** дублей с существующим кодом каталога не нашёл: экраны зовут функции
  `lib/statements`, деньги не считают; синк личного — через общий `exchange`, не копия. Мелочи —
  на усмотрение ревью каталогов: хелперы `member`/`caller`/`errorJSON` в `handlers/statements.go`
  (остальные хендлеры пишут то же вручную); ключи `ff_operations*` перечислены и в
  `stores/finance.ts` `LOCAL_KEYS`, и в `stores/operations.ts` `KEYS` (импорт дал бы цикл сторов);
  карточка «Выписки недели» в `Ritual.vue` дважды (Блок 3 переписывает экран).
- **Решение по вопросу исполнителя (PWA):** воркер pdf.js `.mjs` в прекэш **не добавлять** —
  выписку всё равно скачивают из банка по сети, 1,3 МБ легли бы на каждую установку, оболочки
  Блоков 7–8 встраивают файлы сами. Записано в §6.
- **Хвосты §4 (судьбу — владельцу):** очередь операций застревает на 4xx и статус не виден (→ B2C-21);
  повтор файла — новая запись загрузки, метрика Р-16 посчитает повтор (→ B2C-28); подпись «кому →
  что» нигде не видна (→ B2C-15/21).
- **Доки:** индекс — мусор в строке 1 убран, шапка, §6 (личный документ после B2C-05, раздел
  «После Блока 1», грабли машины), 3 хвоста §4; `CORPUS.md` — строка «что вычищать» у Freedom;
  `STATE.md`.
- **Промпты следующих блоков:** Блок 2 — «Факты после Блока 1» (что знает операция, 17 разделов,
  что видит партнёр, незнакомое и «ИП», «без выписки», тексты экрана) заполнены; Блок 3 — «Факты
  кода после Блока 1» (стор, функции для реюза, `MerchantRule.to`, `income` не ставится,
  удаление правила `internal`, нормализация входит в id) заполнены.
- **Верификация:** фронт `npm run build` ✅, `vitest run` — 833 ✅ / 1 пропуск (замер без
  `STATEMENTS_DIR`) / 1 ❌ `pwa-build` «иконка» — проверено: разница только CRLF рабочей копии,
  блок файлы не трогал; бэкенд `go build/vet/test ./...` ✅ (моки). **PG-режим критиком не
  перезапускался:** Docker Desktop не поднял движок за ~20 мин; бэкенд критик не менял — в силе
  прогон исполнителя, приёмка гоняет PG и CI заново. Браузер (Go на моках + Vite, Chrome 390px):
  одно лицо, два устройства — Kaspi с первого, Freedom со второго → 941 операция (345 + 596), ни
  одного запроса с `%PDF`, `GET /api/operations` до записи загрузки, итоги документа по каждому
  месяцу = траты всех операций сервера, 6+ цифр — 0, ошибок консоли — 0.
- **Для приёмки:** Docker Desktop запускать в начале сессии (движок поднимается долго или
  виснет — тогда перезапуск Docker Desktop); CI ещё не гонялся — push ветки нужен с «да»
  владельца.

## Приёмка

Приёмка 2026-09-27 (Opus 5.5, xhigh). **Вердикт: принято.** L-блок — деплоя нет; следующий шаг —
ревью каталогов `backend`, `frontend`.

- **Критерии ТЗ B2C-01…08** сверены — выполнены (с отклонениями из Handoff, все обоснованы:
  синтетические фикстуры вместо обезличенного корпуса — решение владельца; только PDF — xlsx в
  корпусе нет). Замер B2C-08 повторён на корпусе: 28,2 % строк / 15,7 % суммы → «Блок 6 открыт»
  верно.
- **Регрессия:** фронт `npm run build` ✅ (`vue-tsc` с `e2e/`; pdf.js — чанки `pdf-*.js` 431 КБ и
  worker `.mjs`, в главном `index-*.js` его нет); `vitest run` — 834 ✅ / 1 пропуск (замер без
  `STATEMENTS_DIR`) / 1 ❌ `pwa-build` «иконка» — CRLF рабочей копии, в CI зелёный. Бэкенд `go
  build/vet/test ./...` ✅, **PG-режим** (`-p 1 -count=1 -run Postgres`) ✅, корень `go vet/test
  ./api/...` ✅. **CI** на `7219022` (ветка запушена с «да» владельца): `ci-backend` ✅,
  `ci-frontend` ✅.
- **Новый сценарий в e2e** — `frontend/e2e/b2c-block1-parser.test.ts`, второй `it`: одно лицо —
  два устройства (ноутбук без копии загружает Freedom → итоги A = траты **всех** его операций на
  сервере), viewer — итоги и обе записи загрузок видит, `listOperations` не зовёт, кнопки на
  экране нет. Мутация: без `await pull` в `send()` сценарий падает (итоги ноутбука затирают полные).
- **Стенд** (Go `cmd/server` на одноразовом Postgres — свой контейнер `ff-accept-pg`, trust-доступ
  на 127.0.0.1, после приёмки удалён; Vite; Chrome 390px; A-телефон, A-ноутбук, B-партнёр,
  C-viewer, демо; настоящие выписки корпуса обоих банков) — 28 из 30 проверок сразу, 2 разобраны:
  - Kaspi: предпросмотр 345 операций без 6+ цифр, 32 вопроса, ответ пишет правило и сокращает
    вопросы, «Отправить» → `app.operations` 345; SQL: 6+ цифр — 0, `counterparty` только
    «Имя Ф.»/«Имя Имя Ф.», строки только у владельца.
  - Партнёр: картина, запись «Алихан · Kaspi», «без выписки Дана»; `GET /api/operations` — 0
    строк; правила A у него нет.
  - Повтор того же файла: «Все 345 уже были», строк 345.
  - A-ноутбук (второй вход): правило продавца с телефона пришло (браузерный критерий B2C-05);
    Freedom — 596 без 6+ цифр; отправка без сети — «596 операций отправятся при сети», на
    сервере 345; сеть вернулась — 941, 3 записи загрузок; SQL после Freedom — 6+ цифр и полных
    ФИО 0 (правка критика по ФИО Freedom подтверждена на настоящей выписке).
  - Итоги A в документе = траты всех операций сервера по каждому из 4 месяцев (правка критика
    «итоги со второго устройства» на стенде).
  - Viewer: список загрузок есть, кнопки загрузки нет ни на экране, ни в «+»; `GET
    /api/operations` → 403. Member: «Загрузить выписку» в «+» есть.
  - Демо: разбор и загрузка локально, запросов к выпискам/синку — 0.
  - Сеть: 22 запроса на запись, ни одного с `%PDF`, самый большой 137 КБ; 4 батча — JSON без 6+
    цифр. Гвард сервера: 7 цифр в `merchant` → 400, чужой `kind` → 400. Логи Go — без тел
    операций.
  - **Разбор двух FAIL:** (1) «Загрузки» у viewer — артефакт скрипта (заголовок `uppercase`,
    `innerText` отдаёт «ЗАГРУЗКИ»), повторно проверено — список есть; (2) ошибки консоли у viewer:
    403 на `POST /api/sync/household` на старте движка — **известный хвост §4** (старт с pull →
    B2C-25), и однократный 502 `ECONNRESET` dev-прокси Vite (не воспроизвёлся).
- **Замечено, не Блок 1 (хвосты уже в §4):** у viewer в «+» — формы дохода, целей, покупок
  (→ B2C-13/18/21).
- **Грабли среды:** Docker Desktop висел с прошлой сессии — помог перезапуск (процессы +
  `wsl --shutdown`), движок поднялся за ~1 мин. Строки подключения `memory/secrets/local-pg.md`
  приёмке прочитать не дали (автопроверка прав) — отсюда свой одноразовый контейнер. Сид стенда:
  регистрация создаёт каждому свою семью (хвост «мульти-домохозяйства») — лишние членства убраны
  в одноразовой БД; гард роутера до первого pull ведёт на `/setup` при внедрённом токене — в
  скрипте сначала открывается `/`. Сборка из шелла, стоящего в `dist/assets`, падает (Windows
  держит каталог).

## Итоги пост-приёмки (только L)
