# Блок 4 — Покупки и цели · сессии

**Цель блока:** покупки в дом снова можно править, удалять и возвращать из «Уже купили» (с
итогом, датами и автором), а цель — исправлять «Уже накоплено» без потери истории взносов и без
потери правок при закрытии окна.

Сессии: исполнитель ✅ 2026-09-25 · критик ✅ 2026-09-25 · приёмка ⬜

---

## Промпт исполнителя — уровень build · Opus 5 · effort high

```
/worker паритет-react-vue 4
```

Блок-специфика:

- Ветка `pv-block-4-wishes-goals` от `main` (после деплоя Блока 3). Push — с согласия
  владельца; `main` не трогать.
- Задачи по порядку: `PV-18` → `PV-19`.
- Эталон — React `src/screens/Goals.tsx`, `GoalDetail.tsx`, `src/store/useStore.ts` (строки в
  ТЗ); тексты дословно. Модалки Целей переводятся на `Sheet` (Р-13: экран блока).
- Стор — единственный путь правки (`mutateHouseholdDoc` через методы стора; прямые мутации из
  вью убрать). `seed`/`have` — по формуле `goalHave` (PV-04).
- **Что уже в коде после Блока 3** (факт, критик Блока 3 2026-09-25; номера строк — на
  `pv-block-3-debt-plan`, после merge могут сдвинуться — искать по именам):
  - Кит: `kit/Sheet.vue` (`open`, `title`, `z?`, emit `close`; слоты тела, `mark`, `footer`;
    Escape — верхнему, фокус в окно и назад; поведение закреплено `kit/Sheet.dom.test.ts` на
    `happy-dom`); `useSavedMark(() => id, () => updatedAt)` + `kit/SavedMark` в слот `mark`;
    `NumFieldBlur`, `Segmented`, `Field group`, `DangerZone` (`label`, `warning`,
    `confirm-label`, emit `confirm`). Образцы окна с автосохранением по blur —
    `components/capital/CreditSheet.vue`, `AccountSheet.vue` (пропс — id, emit `close`;
    `:open` — по найденной записи: у кредита — среди `liveCredits`, удалил партнёр — окно
    закрылось).
  - На Целях окна всё ещё самодельные: `Goals.vue` (`fixed inset-0 … bg-black/40` —
    `:305`, `:357`), `GoalDetail.vue` (`:312`, `:373` — окно правки цели); оба экрана блок и
    так трогает → на `Sheet` (Р-13), литерального затемнения не оставить.
  - **`wishlist` в сторе — только геттер** (`stores/finance.ts:154`), методов нет;
    `Goals.vue` по-прежнему пишет покупки напрямую (`mutateHouseholdDoc` `:137`, `:149`).
  - **`updateGoal(id, patch)`** (`stores/finance.ts:1248`) PV-14 не менял: `Object.assign` +
    `updatedAt`, без `unchanged` и без правила `seed`. План «Сначала долги» в цели **не
    пишет** (Р-9 паритета): пауза выводится `pausedGoals(activePlan, goals)`, `Goal.monthly`
    не трогается. Значит правка «Откладывать в месяц» у цели на паузе сразу меняет шаг
    плана (`planExtra` = Σ взносов пауз), а «Уже накоплено» у цели-подушки — шаг `cushion`
    (`planStep`); settle плана после `updateGoal` не нужен (долги не меняются). На
    `GoalDetail.vue` у цели на паузе — Callout «На паузе ради плана» и подпись «после плана»
    под датой; на `Goals.vue` — `Tag` вместо «N/мес»: вёрстку этих мест не ломать
    (SSR-тесты `views/Goals.test.ts`).
  - Тесты вью — SSR (`renderScreen` из `src/test/screenState.ts` — экран с роутером без
    охранника); стенд двух телефонов e2e — `e2e/support/family.ts` (`fakeServer`, `phone`,
    `screen`, `at`, `setOnline`); `e2e/` проверяется `vue-tsc -b` (`tsconfig.e2e.json`).
  - **После клинапа Блока 3** (факт, 2026-09-25; подробно — §6 бэклога): пауза цели —
    `financeStore.pausedGoalIds` (Set), шаг плана — `financeStore.planStepNow()`, не собирать
    заново. У целей на паузе бывают движения с `planId` (заметка «в долги по плану») — это
    снятие «вложить уже накопленное» при шаге месяца старта; PV-19 («Уже накоплено», `seed`) их
    не теряет и не переписывает (`planLumpTakes` считает снятое по ним). Выбор счёта списания —
    `components/AccountChoice.vue` (v-model `string | null | undefined`, подсказка слотом) —
    брать его, если окну нужен выбор счёта кнопками. Прокрутка экрана сбрасывается сама
    (`AppShell`). Хвост Н-8 ревью Б3 (дата цели на паузе) владелец отдал в PV-19 — п. 4 ТЗ.
  - **Владелец 2026-09-25 — исключение из Р-2:** ползунок «Откладывать в месяц» на экране цели
    заменить полем ввода (`NumFieldBlur`, сохранение по blur, «Сохранено») — PV-19 п. 5,
    `memory/decisions/r2-exception-goal-monthly-input.md`. React здесь не эталон.
- Среда: стенд §6, два профиля + viewer. Верификация: `cd frontend && npm run build && npm test`.
- Следующий шаг — `/critic паритет-react-vue 4`.

---

## Промпт критика — уровень review · Opus 5 · effort xhigh

```
/critic паритет-react-vue 4
```

Блок-специфика:

- Слияние: правка `have` против офлайн-взноса (история цела); правка покупки против покупки
  (LWW целиком — документировано тестом).
- Даты покупок: старые строки `dd.mm.yyyy` из прода не ломают показ.
- Тексты React дословно; viewer без кнопок.
- Промпт Блока 5 актуализировать: `Sheet` на Целях, `useSavedMark` — образцы для PV-21/PV-23.
- Следующий шаг — `/accept паритет-react-vue 4`.

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh

```
/accept паритет-react-vue 4
```

Блок-специфика:

- Критерии ТЗ на стенде, два профиля + viewer; регрессия — весь `frontend/e2e/` + `npm test`;
  CI зелёный. Сценарии — `frontend/e2e/pv-block4-wishes-goals.test.ts`.
- **От критика:** правка кита окон (`Sheet` уводит фокус из поля до `close`) задевает все окна —
  Escape и крестик в окнах Целей и Капитала сохраняют правку поля, окна без полей закрываются
  как раньше. Два хвоста критика в §4 (правку «Уже накоплено» откатывает поздний офлайн-взнос;
  поле взноса дважды) — судьба у владельца, по первому — до деплоя. На 5173 может стоять Vite
  другого проекта — свой на `--port 5174 --strictPort` (грабли ниже).
- **Деплой (M):** с «да» владельца — ID текущего Production-деплоя (откат), merge
  `pv-block-4-wishes-goals` → `main`, push; смоук: список покупок и «Уже купили» с итогом на
  живых данных, правка покупки видна на втором телефоне. **Откат:** promote записанного деплоя.
- 🏁 в §4. Следующий шаг — `/worker паритет-react-vue 5`.

---

## Handoff (заполняет исполнитель)

Исполнитель, 2026-09-25, ветка `pv-block-4-wishes-goals` от `main` (`2db5616`), не запушена
(push — с согласия владельца).

- **Коммиты:** `5da5f9e` PV-18 · `e7f21f4` PV-19 · docs (статусы, Handoff, §6, хвосты).
- **Что сделано:**
  - **PV-18.** Стор: `addWish` (в начало, `addedOn` — ISO момента), `updateWish` (с `unchanged`),
    `removeWish` (надгробие), `toggleBought` (`boughtOn` ISO / `null`); `Goals.vue` документ сам
    больше не пишет. Строка покупки — кнопка правки → `components/goals/WishSheet.vue` на `Sheet`
    (React `WishDialog`: поля по blur, `SavedMark`, «Готово», `DangerZone` с текстом React;
    удалил партнёр — окно закрылось). Создание — тексты React («Что покупаем», «Цена, ₸»,
    «Ссылка на товар», placeholder’ы, «Кто добавил» — `Segmented` в `Field group`, «Добавить в
    список»), кнопка «Добавить покупку». «Уже купили» — всегда, итог `money(boughtSum)`, «Пока
    ничего», зачёркнутое название, «{кто} · куплено {дата}», «Вернуть в список»; цены — `plain`
    (React). Даты: ISO → `atLabel` («10 сентября»), старые `24.09.2026` — как есть. Карточка
    «Это N-я покупка в дом…». Viewer — без галочек, «Вернуть», «Добавить покупку»; строка — не
    кнопка.
  - **PV-19.** `updateGoal({ have })` — правило React: `seed = max(0, have − Σ)`, `have =
    goalHave(seed, movements)`; движения (и снятия плана с `planId`) не трогаются; правка без
    изменений не пишется. Окно правки — `components/goals/GoalSheet.vue` на `Sheet` (React
    `EditGoalDialog`): «Название» (непустое), «Сколько нужно» (> 0), «Уже накоплено» (≥ 0) с
    пояснением «Взносы (N) останутся в истории…», «Откладывать в месяц» (> 0), «Цвет» сразу
    (`aria-pressed`), «Сохранено», «Готово» только закрывает, `DangerZone` — текст React; всё
    пишется по уходу из поля — закрытие фоном/крестиком ничего не теряет. Viewer — без
    карандаша. **П. 4:** `goalDoneMonth(months, key, pause?)` (`finance.ts`) — у цели на паузе
    дата от месяца без процентных долгов прогноза активного плана (`planForecast(...)
    .debtFreeMonth`) плюс месяцы цели; долги не закрываются — «Цель закроется после плана».
    **П. 5:** ползунок и `minMonthly`/`maxMonthly` убраны; поле «Откладывать в месяц, ₸»
    (`NumFieldBlur`) → `setGoalMonthly` при `v > 0` и изменении, «Сохранено» справа над полем;
    viewer — только сумма. Окно «Пополнить / Снять» — на `Sheet` (`Select` счёта, «Кто
    вносит» — `Segmented`, как React `GoalDetail.tsx:214-215`).
- **Отклонения от ТЗ:**
  1. PV-18 «Это N-я покупка»: номер берётся в момент отметки (купленных до неё + 1). В React
     `bought.length + 1` считался уже после отметки — на один больше (первая покупка — «2-я»).
  2. PV-18 стёртая ссылка пишется пустой строкой, а не `undefined` (React `v || undefined`):
     пропавший ключ `mergeList` вернул бы из записи партнёра (§6). Тест в `merge.test.ts`.
  3. PV-18 имя автора без человека в `people` — «Участник» (как было во Vue), а не React `''`
     (иначе « · куплено …» с висящей точкой).
  4. PV-18 «Кто добавил» в окне правки — при ≥ 2 людях, как в создании (React — всегда).
  5. PV-18 новая покупка у партнёра встаёт в конец списка: `mergeList` дописывает незнакомые
     записи (React `src/store/merge.ts:38-60` — так же). У автора — в начале. e2e документирует.
  6. Окна сверх ТЗ переведены на `Sheet` (Р-13, «литерального затемнения не оставить»):
     «Новая цель» (там же «Цвет» — `Field group`) и «Пополнить / Снять».
  7. PV-19 «Сохранено» у поля взноса горит по значению взноса, а не по `updatedAt` цели:
     пополнение тоже меняет `updatedAt`, и отметка у поля загоралась бы от взноса денег.
  8. PV-19 п. 4 — выбран вариант с месяцем (не «через N мес. после плана»): прогноз плана
     детерминирован, дата двигается только от факта (Р-4), не шумит. Взносы — с месяца после
     `debtFreeMonth` (`debtFreeMonth + months`; без паузы — `key + months − 1`, как было).
     Режим «от суммы» — та же дата крупно («После плана» без месяца). Взнос 0 без паузы —
     «—» вместо прежнего «undefined Infinity».
  9. Окно правки цели — отдельный компонент (`GoalSheet`, как `WishSheet` и окна Капитала), не
     разметка в `GoalDetail.vue`.
  - Найдено и оставлено хвостами §4 (правило 10): прогноз «Цель дорожает вместе с рынком» у
    цели на паузе — горизонт без паузы; viewer на Целях видит «Новая цель», «Пополнить»/«Снять».
  - На заметку критику: `atLabel` — без года («20 сентября»); у покупок годичной давности год не
    виден (дефолт ТЗ «показ через `atLabel`»).
- **Грабли среды:**
  - Мутационная проверка: `git checkout <файл>` откатывает и **незакоммиченные** правки — так
    пропала и восстановлена из копии `goalDoneMonth`. Мутации — только через копию файла (`cp`
    туда-обратно), `git stash` или worktree.
  - `innerText` заголовка `Section` приходит капсом (CSS `uppercase`) — в браузерных проверках
    «УЖЕ КУПИЛИ», не «Уже купили».
  - Серверный документ сразу после правки на стенде ещё старый (синк отложен) — ждать/опрашивать.
  - `node -e` в Bash с шаблонными строками ломает обратные кавычки — правки кода через Edit.
  - `grep -c $'\r$'` в Git Bash на CRLF-файлах даёт 0 — концы строк смотреть `od -c`.
- **Верификация:**
  - `cd frontend && npm run build` ✅ (предупреждение о чанке > 500 kB — Н-9, PV-23);
    `npm test` — **552/552** (43 файла), в т. ч. новый `e2e/pv-block4-wishes-goals.test.ts` 5/5,
    PV-18/PV-19 в `stores/finance.test.ts`, `lib/merge.test.ts`, `views/Goals.test.ts`.
  - Мутации (на копии): `goalDoneMonth` без учёта паузы — падают 2 SSR-теста п. 4; `seed = have`
    — падают 5 тестов.
  - Браузер (стенд §6: Go-моки + Vite, Chrome 390px, два профиля + viewer) — **45/45**: правка
    названия/цены и закрытие фоном, удаление, «Отметить» → карточка «2-я», итог «Уже купили» =
    сумме цен на сервере, партнёр видит всё, «Вернуть в список» у партнёра → активные у A;
    viewer без кнопок; цель — поле взноса 73 000 → сумма, «декабрь 2029», «Сохранено» гаснет, 0
    не пишется; «Уже накоплено» 400 000 и закрытие фоном → seed 350 000, 2 взноса целы,
    название крестиком, партнёр видит; viewer без карандаша и поля; цель на паузе — «июнь 2033» =
    «июнь 2027» экрана плана + 72; консоль без ошибок. Скрипт и скриншоты (в репо не кладутся):
    `C:/Users/SW/AppData/Local/Temp/claude/c--Users-SW-finance/073450fa-34cd-46df-acc8-85625f7e47d3/scratchpad/stand/`
    (`block4.mjs`, `stand.mjs`, `run-2.log`, `w*.png`, `g*.png`).
  - CI — не запускался: ветка не запушена (push — с согласия владельца).

## Итоги критика

Критик, 2026-09-25, ветка `pv-block-4-wishes-goals` (не запушена). Коммиты: `c10c9c3`, `40e0d9d`,
docs.

- **Правки (4):**
  1. **Escape и крестик теряли правку поля в окне** (`c10c9c3`). Поля окон пишут по уходу из
     поля, а родитель на `close` сразу обнуляет запись окна; `blur` приходил позже — из `hide()`
     кита, когда фокус возвращается на кнопку, открывшую окно, — и `if (goal.value)` молча
     пропускал запись. Escape фокус не трогает; крестик в Safari тоже (Chrome отдаёт ему фокус
     на mousedown — поэтому браузер исполнителя с крестиком и фоном был зелёным). Задевало
     «Уже накоплено» и название цели, покупку, окна кредита, счёта, обязательства. Проба на
     `happy-dom`: Escape — `have` 50 000 остался, контроль `blur()` — 120 000. Правка — в одном
     месте: `kit/Sheet.vue` `close()` уводит фокус из окна до `emit('close')` (Escape, крестик,
     фон). Тесты: `Sheet.dom.test.ts` (blur до close, три способа), `goals/GoalSheet.dom.test.ts`
     (Escape — seed; крестик без фокуса — название); мутация ловится тремя тестами.
  2. **Строка покупки** — одна разметка для viewer и участника (`component :is`, как `kit/Row`),
     а не две копии имени и подписи (`40e0d9d`).
  3. **«Цвет» цели** — `components/goals/HuePicker.vue` для окон создания и правки; в окне
     создания выбранный цвет теперь `aria-pressed` (было только в правке).
  4. **`GoalDetail`** показывал цель, удалённую партнёром (`goals.find` без `liveGoals`): окно
     правки закрывалось, а экран с «Пополнить» оставался — взнос ушёл бы в надгробие, счёт
     списался бы. Теперь «Цель не найдена» (так же было в React — не решение, а недосмотр).
- **Добито тестов (+9):** `goals/WishSheet.dom.test.ts` — обработчики окна покупки (e2e правит
  методами стора): название, цена, стёртая ссылка — `''` (мутация `v || undefined` ловится),
  «Кто добавил», пустое название не пишется, удаление спрашивает и закрывает; SSR — строка
  участника — кнопка, viewer — нет; «Цвет» в обоих окнах; удалённая цель.
- **Сверено, без правок:** критерии и секции «Тесты» PV-18/PV-19 — закрыты кодом и тестами
  исполнителя; тексты React (`Goals.tsx:178-388`, `GoalDetail.tsx:230-319`) — дословно,
  отклонения 1–9 Handoff обоснованы (1 — поправлен баг React «первая покупка — 2-я»);
  `updateGoal({ have })` — правило React, движения плана (`planId`) не трогаются;
  `goalDoneMonth` — `debtFreeMonth` в том же смысле, что «долги с процентами закроются в …»
  экрана плана, взносы — с месяца после; слияние «покупка против покупки» — LWW записью
  (тест), «have против взноса» — сходится при взносе раньше правки (позже — хвост 1); старые
  `dd.mm.yyyy` — как есть (тест); viewer — без кнопок правки. `setGoalMonthly` не слит с
  `updateGoal` — нужен Ритуалу и e2e. Копии заглушки `localStorage` в новых тестах — хвост Н-10.
- **Хвосты §4 (2):** правку «Уже накоплено» откатывает поздний офлайн-взнос партнёра
  (`mergeGoal` берёт seed победителя, взнос двигает `updatedAt`; проба: 180 000 вместо 150 000;
  в React так же) — рекомендация: якорь `seedSetAt`; поле «Откладывать в месяц» дважды —
  на карточке и в окне правки.
- **На заметку приёмке и владельцу:** `atLabel` без года (дефолт ТЗ, согласован) — у покупки
  годичной давности «10 сентября» без года, хотя карточка обещает «через год будет видно».
- **Промпт Блока 5** — «Что уже в коде после Блока 4»: окна и `close()` кита, образцы
  `useSavedMark`, `HuePicker` (для «Цветов разделов» PV-22 — пропсом, не копией), `lib/theme.ts`,
  `categoryName`, текущие номера строк из ТЗ (уехали с Блока 1).
- **Грабли среды:** на 5173 работал Vite другого проекта (`C:\Users\SW\project`) — не трогать,
  свой — `npx vite --port 5174 --strictPort`, в обвязке стенда `APP` из env. Стенд после
  остановки фоновой задачи жив — добивать по портам 8080/5174.
- **Верификация:** `npm run build` ✅ (предупреждение о чанке — Н-9, PV-23); `npm test` —
  **561/561** (45 файлов). Браузер (стенд §6, Chrome 390px) — сценарий критика **16/16** (Escape в
  окнах цели, покупки и кредита — правка на экране и на сервере, фокус вернулся на карандаш;
  крестик и фон; «Цвет» в окне создания; строка viewer; цель удалена партнёром; консоль чистая)
  и повтор сценария исполнителя на коде критика **45/45**. Скрипты (в репо не кладутся):
  `C:/Users/SW/AppData/Local/Temp/claude/c--Users-SW-finance/f631722e-44f0-4463-a4a8-2336d9ff7566/scratchpad/stand/`
  (`critic.mjs`, `block4.mjs`, `stand.mjs`, `run-block4.log`). CI — не запускался: ветка не
  запушена (push — с согласия владельца).

## Приёмка
