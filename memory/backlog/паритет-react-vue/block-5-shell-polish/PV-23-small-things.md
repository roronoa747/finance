# PV-23 — Мелочи Б-22: пояснения мастера, подсказки, «Сохранено», фокус, `Hint`, `NumField`, заголовки, «без процентов» (фронт)

Блок 5 · MVP · Каталог: `frontend/` · Зависит от: Блок 1 (по порядку — Блок 4) · Роли: все

## Контекст (что уже есть)

Сверка Б-22, по пунктам (React → Vue):

1. **Пояснения мастера** (`src/screens/Setup.tsx` → `frontend/src/views/Setup.vue`):
   - день зарплаты: React `:276-279` «День нужен, чтобы календарь показал провал между вашей
     зарплатой и зарплатой партнёра — когда платежи уже прошли, а деньги ещё не пришли.» — Vue
     поле `:287-289` без пояснения;
   - коммуналка: React `:313-319` поле «Коммуналка в месяц, ₸ — примерно» + «Коммуналку
     приложение будет помечать как оценку: она плавает по сезонам, и выдавать её за точную
     цифру нечестно.» — Vue `:310-312` без пояснения;
   - ГЭСВ — сделано в PV-03 (не дублировать);
   - код один раз: React `:488-491` «Продиктуйте его партнёру. Он открывает тот же адрес,
     регистрируется и выбирает «Войти по коду». Код действует две недели и срабатывает один
     раз.» — Vue `:420-422`, `:426-428` без «срабатывает один раз»;
   - подзаголовки шагов Vue `:254-274` сокращены против React `:259-260, :289, :330, :412, :460`;
     «Есть кредит?» (React) vs «Есть действующий кредит или рассрочка?» (Vue `:317`).
2. **Подсказки планирования суммы** — сделано в PV-11 (placeholder, autofocus, «Месяц, который
   выберете…», годовая сумма). Здесь — проверить, что не осталось расхождений.
3. **«Сохранено»**: `kit/SavedMark.vue` только рисует; `useSavedMark` — PV-09. `Deposit.vue`:
   `saved = true` на `:51, 59, 66, 75, 83, 90, 97` без сброса (показ `:122`), в
   `onAmountCommit`/`onRateCommit` не проверяется изменение; модалка счёта — PV-12 (сделано);
   кредит — PV-10, обязательство — PV-11 (сделано).
4. **Окно пополнения цели**: Vue `GoalDetail.vue:307` placeholder «10 000», без autofocus; React
   `GoalDetail.tsx:210-217`: заголовок «Пополнить «{name}»», placeholder `plain(goal.monthly)`,
   `autoFocus`, кнопка «Внести».
5. **`SalaryDialog.vue:187-188`** «Новый оклад, ₸» без фокуса; React `Budget.tsx:426-427`
   `autoFocus`.
6. **`kit/Hint.vue:59`** — `absolute bottom-full left-1/2 -translate-x-1/2 … w-64 max-w-[85vw]`,
   у края не прижимается; закрытие только по pointerdown снаружи и Escape (`:19-43`). React
   `kit.tsx:340-399`: `position: fixed`, `width = min(268, innerWidth − 24)`, `left = clamp(12,
   r.left, innerWidth − width − 12)`, `top = r.bottom + 6`, закрывается при прокрутке (`:355`).
7. **`kit/NumField.vue:29-46`** `onInput`: `next = clean(raw, kind, modelValue)` → emit; если
   `clean` вернул то же значение (набрана буква), модель не меняется, Vue не перерисовывает —
   мусор остаётся в поле. Правила — `lib/num.ts:27-37` (`clean`).
8. **Заголовки маршрутов**: `AppShell.vue:35-43` по `startsWith` — `/goals/:id` → «Цели и
   покупки», `/capital/:id` → «Капитал»; React `AppShell.tsx:23-39` — точные пути, иначе
   `startsWith('/goals') ? 'Цель' : 'Вклад'`. `document.title` не ставится нигде (и в React).
9. **Кредит 0%**: `Capital.vue:751` «рассрочка» → React `Capital.tsx:156` «без процентов» (в
   `note` нового кредита «рассрочка» в обоих — не трогать).

Кит: `Sheet` (модалки Целей — после PV-18/PV-19, `SalaryDialog` — здесь на `Sheet`, Р-13).

## Задача

1. Мастер: пояснения и подзаголовки как React (п. 1), «Есть кредит?».
2. `Deposit.vue`: `useSavedMark` (горит по `updatedAt`, гаснет), коммит только изменённого.
3. Окно пополнения цели: заголовок, placeholder `plain(goal.monthly)`, autofocus, «Внести».
4. `SalaryDialog`: autofocus на «Новый оклад»; хром → `Sheet`.
5. `Hint`: `position: fixed`, ширина и `left` с зажимом к краям, `top` под знаком, закрытие при
   прокрутке (порт React).
6. `NumField`: при неизменённой модели принудительно `el.value = next` (мусор не остаётся).
7. Заголовки: `/goals/:id` → «Цель», `/capital/:id` → «Вклад».
8. Строка кредита 0% — «без процентов».

## Тесты

- `views/views.test.ts` (SSR `Setup`, шаг 1): пояснение про день зарплаты присутствует.
- `components/kit/NumField.test.ts` (SSR + логика): `clean('12a', 'int', '12')` → модель не
  меняется, но обработчик выставляет `el.value` (проверить через фейковый `event.target`).
- `components/kit/Hint.test.ts`: функция расчёта позиции (вынести чистую `hintPosition(rect,
  innerWidth)`) — зажим у левого и правого края.
- `views/Goals.test.ts` (SSR `GoalDetail`): окно пополнения с placeholder `plain(monthly)` и
  кнопкой «Внести»; `Deposit.test.ts`: «Сохранено» гаснет по таймеру (`vi.useFakeTimers`).
- `views/Capital.test.ts`: строка кредита 0% — «без процентов».
- `AppShell` заголовки: SSR по маршрутам `/goals/x` и `/capital/x`.

## Критерии приёмки

- Браузер: каждый пункт 1–9 виден на телефонной ширине; `Hint` у правого края не обрезается;
  буква в числовом поле не остаётся; «Сохранено» на вкладе гаснет.
- `cd frontend && npm run build && npm test` зелёные.

## Вне скоупа

- ГЭСВ-подсказка (PV-03), подсказки планирования (PV-11), «Сохранено» кредита/обязательства/
  счёта (PV-10…PV-12) — сделаны раньше; здесь только проверка.
- `document.title` — в React не было.
