# Блок 5 — Оболочка и мелочи · сессии

**Цель блока:** оболочка говорит по-русски и ведёт себя как в React: понятные ошибки входа и
кода, полная шторка синка с «Начать бюджет заново», имя и цвета разделов в «Оформлении», и
десяток мелочей текста и удобства, которые семья замечает каждый день.

Сессии: исполнитель ✅ 2026-09-26 · критик ⬜ · приёмка ⬜

---

## Промпт исполнителя — уровень build · Opus 5 · effort high

```
/worker паритет-react-vue 5
```

Блок-специфика:

- Ветка `pv-block-5-shell-polish` от `main` (после деплоя Блока 4). Push — с согласия
  владельца; `main` не трогать.
- Задачи по порядку: **хвост «seed»** → `PV-20` → `PV-21` → `PV-22` → `PV-23`.
- **Хвосты §4 → Блок 5 (владелец, 2026-09-25, приёмка Блока 4)** — описание и варианты в §4:
  - **Первой задачей, до PV-20:** правку «Уже накоплено» откатывает поздний офлайн-взнос партнёра
    — вариант (а) критика Б4: якорь `Goal.seedSetAt` (как `principalSetAt`, RP-06): ставит только
    `updateGoal({ have })`, `mergeGoal` берёт seed стороны с поздним якорем, без якоря — seed
    победителя; тест в `merge.test.ts` (A правит, B позже офлайн вносит → have = seed_A + Σ).
    Коммит — с ID задачи, к которой ближе (`PV-19`), в сообщении — «хвост Б4».
  - **В PV-23:** (1) поле «Откладывать в месяц» убрать из окна `GoalSheet` (остаётся на карточке
    цели — исключение из Р-2); (2) «Готово» в `GoalSheet`/`WishSheet` — через `close()` кита, как
    крестик (DOM-тест: правка поля + «Готово» без смены фокуса — записана); (3) «История цели» —
    новые сверху и после слияния (`mergeGoal` сортирует «новые первыми», Vue разворачивает).
- Р-15: бэкенд не трогать — тексты Go переводятся на фронте (таблица в PV-20); участники
  шторки — из `people` документа (PV-21, дефолт составителя).
- Тексты React дословно (строки в ТЗ). Экраны блока — на `Sheet` (`SyncBadge`, `SalaryDialog`).
- **Что уже в коде после Блока 4** (факт, критик Блока 4 2026-09-25; ТЗ писались до Блока 1 —
  номера строк в них уехали, ниже — текущие на `pv-block-4-wishes-goals`; после merge искать по
  именам):
  - **Окна.** `kit/Sheet.vue`: закрытие Escape, крестиком и фоном сначала уводит фокус из поля
    (критик Б4) — поле с записью по уходу (`NumFieldBlur`, `@blur`) правку не теряет, родитель
    может сразу обнулять запись окна. Образцы окна правки записи — `components/goals/GoalSheet.vue`,
    `WishSheet.vue`, `components/capital/CreditSheet.vue` (пропс — id, `:open` по живой записи,
    emit `close`); окна создания — `Sheet` на месте с `v-model` (`Goals.vue` «Новая цель»,
    «Покупка в дом»). Автофокус поля в листе — как PV-11: `nextTick(() => …querySelector('input')
    ?.focus())` (`ObligationSheet.vue:141`), не атрибут `autofocus` (лист вставляется после
    загрузки). Самодельные окна остались: `SyncBadge.vue:78` (PV-21 → `Sheet`),
    `SalaryDialog.vue:129` (PV-23 п. 4 → `Sheet`), два листа `AppShell.vue:187, :264` (Н-8 —
    владелец не принял; «Оформление» PV-22 живёт в одном из них — свой хром без Escape и без
    `close()` кита).
  - **«Сохранено»:** `useSavedMark(() => id, () => updatedAt)` + `kit/SavedMark` в слот `mark`
    (`GoalSheet`, `WishSheet`). Если `updatedAt` записи двигают и другие действия — метку вешать
    на значение поля (`GoalDetail.vue` «Откладывать в месяц»: `() => String(goal.monthly)`,
    отклонение 7 Б4) — пригодится `Deposit.vue` (PV-23 п. 3: `saved.value = true` `:56-102`,
    показ `:127`).
  - **Цвет:** `components/goals/HuePicker.vue` (v-model `HueKey`, `Field group` с подписью «Цвет»,
    `aria-pressed`, образцы `HUES[h].light`) — окна цели. «Цвета разделов» PV-22 — тот же ряд:
    подпись — пропсом, а не третья копия. В `Setup.vue:371` своя копия с `Field` без `group`
    (подпись входит в имя первой кнопки, §6) — PV-23 п. 1 и так трогает мастер (Р-13).
  - **`lib/theme.ts` после PV-08:** `read` / `write` (`localStorage` в `try/catch`, не
    экспортируются), `readThemeChoice`, `readAccent`, `applyCurrentPalette()` → `applyTheme({
    theme, accent, categories: CATEGORIES })` (цвета разделов — константа `CATEGORIES` модуля),
    `setThemeChoice`, `setAccent`, `watchSystemTheme`, `isDark`. PV-22: `readCategoryHues` /
    `setCategoryHue` — рядом на тех же `read`/`write`, `applyCurrentPalette` берёт их вместо
    `CATEGORIES`.
  - **Имена разделов после PV-11/PV-15:** `DEFAULT_CATEGORY_NAMES` и `categoryName(categories,
    key)` (`lib/palette.ts:83, :92`) — запасное имя незаведённого раздела; строки Бюджета —
    `budgetLines`.
  - **Текущие строки из ТЗ:** `stores/finance.ts` — `resetDoc` `:293`, `setPerson` `:500`,
    `resetAll` `:1366`; `AppearancePanel.vue` — `userName` `:21`, `saveName` `:33`;
    `SalaryDialog.vue` «Новый оклад» `:173`; `Access.vue` регэкспы `:118-123`; `Overview.vue` —
    `makeInvite` `:158`, баннер `:186` (без `isViewer`); `Setup.vue` `handleMakeInvite` `:130`;
    `GoalDetail.vue` окно «Пополнить / Снять» `:306` (уже `Sheet`, «Кто вносит» — `Segmented`;
    заголовок и «Внести» React (PV-23 п. 4) — только в режиме пополнения, у «Снять» свои);
    `Capital.vue:143` «рассрочка» в строке кредита (`:346` — `note` нового кредита, не трогать);
    `router/index.ts` — все экраны статическим импортом (Н-9, PV-23 п. 9).
  - Хвосты §4 для PV-23 (судьбу даёт владелец, без неё не делать): «viewer без форм» (Капитал и
    Цели — «Новая цель», «Пополнить»/«Снять»).
- Среда: стенд §6 (Go на моках даёт те же тексты ошибок, что прод), два профиля + viewer.
  Верификация: `cd frontend && npm run build && npm test`.
- Следующий шаг — `/critic паритет-react-vue 5`.

---

## Промпт критика — уровень review · Opus 5 · effort xhigh

```
/critic паритет-react-vue 5
```

Блок-специфика:

- Полнота таблицы `authErrorText` против всех `error`-строк в `backend/internal/handlers/*.go`
  и `internal/auth/middleware.go` (grep) — ни одного английского текста на экране.
- `resetAll` шлёт полный `defaultSyncDoc()` (включая `plans`); «Начать заново» недоступно viewer.
- Тексты React дословно; `Hint` у краёв; `NumField`.
- **Последний блок:** вместо промпта следующего блока — проверить готовность бэклога к
  закрытию (DoD BACKLOG-GUIDE): все хвосты §4 с судьбой, живые доки актуальны, e2e пополнен.
- Следующий шаг — `/accept паритет-react-vue 5`.

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh

```
/accept паритет-react-vue 5
```

Блок-специфика:

- Критерии ТЗ на стенде, два профиля + viewer; **полная сверка паритета**: пройти
  `../развитие-приложения/parity-react-vue.md` пункт за пунктом (А-1…А-9, А-11, Б-12…Б-22) и
  отметить в «Приёмка» закрытие каждого со ссылкой на сценарий e2e (критерий брифа).
- Регрессия: весь `frontend/e2e/` + `npm test`; CI зелёный. Сценарии —
  `frontend/e2e/pv-block5-shell-polish.test.ts`.
- **Деплой (M):** с «да» владельца — ID текущего Production-деплоя, merge
  `pv-block-5-shell-polish` → `main`, push; смоук: вход с неверным паролем → русский текст,
  шторка синка с участниками, «Оформление» с цветами. **Откат:** promote.
- 🏁 в §4. **Последний блок** — предложить владельцу закрыть бэклог по DoD (все 🏁, хвосты с
  судьбой, `STATE.md`, каталог → `memory/archive/`); следующий шаг после архива —
  `/worker развитие-приложения 2` (Блок 2 RP ждал этой инициативы, Р-17).

---

## Handoff (заполняет исполнитель)

Исполнитель, 2026-09-26, ветка `pv-block-5-shell-polish` от `main` (`73f2b57`), не запушена.

- **Коммиты:** `0ff0e01` PV-19 хвост Б4 «seed» · `416f83f` PV-20 · `d7a6532` PV-21 · `bc4625c` PV-22 ·
  `569e67b` PV-23. Дифф блока: `git diff 73f2b57..569e67b -- frontend` (бэкенд не тронут, Р-15).
- **Что сделано:**
  - **Хвост «seed»** (вариант (а) критика Б4): `Goal.seedSetAt` ставит только `updateGoal({ have })`
    при смене seed; `mergeGoal` берёт seed стороны с поздним якорем, без якоря/при равных — победителя.
    Тесты: поздний офлайн-взнос B после правки A (обе стороны слияния, повторное слияние), обе
    правили seed, якорь не ставят название/взнос/создание.
  - **PV-20:** `lib/authErrors.ts` — таблица всех `error`-строк Go ручек входа, регистрации, кода и
    мидлвара (сверено grep-ом по `backend/internal`); «unauthorized» по контексту (`join` — «Чтобы
    войти по коду, сначала войдите в аккаунт.», `invite` — «Вход истёк. Выйдите и войдите заново.»);
    5xx, сеть, незнакомое — «Не получилось связаться с сервером. Попробуйте ещё раз.» + `console.warn`.
    `Access` — через таблицу (регэкспы убраны); Обзор и мастер — ошибка создания кода текстом под
    кнопкой; баннер приглашения viewer не видит.
  - **PV-21:** `SyncBadge` на `Sheet`: «Последний обмен» — дата и время; «В бюджете» из `people`
    («это вы», у себя при viewer — «только просмотр»); «Пригласить второго» (текст React) — общий
    композабл `components/useInvite.ts` (на нём и Обзор, и мастер); пояснение про офлайн дословно;
    `DangerZone` «Начать бюджет заново» → «Стереть всё» → `resetAll()`, синк, шторка закрыта, мастер.
    **Метка сброса `SyncDoc.resetAt`** (отклонение 1). e2e `pv-block5-shell-polish.test.ts` — 3
    сценария (B с открытым приложением, B открыл приложение после сброса, A уже прошёл мастер).
  - **PV-22:** имя — `people[slot].name` реактивно, пустое возвращает прежнее, то же не пишется,
    подпись React; «Цвета разделов» — `readCategoryHues`/`setCategoryHue` (`ff_category_hues`, дефолт
    по каждому разделу, мусор и сломанное хранилище — дефолт), `applyCurrentPalette` берёт их; ряд —
    `HuePicker` с подписью-пропсом по всем пяти разделам; примечание дословно; `aria-pressed` у
    акцента и оттенков.
  - **PV-23:** (1) мастер — пояснения дня зарплаты и коммуналки, подзаголовки шагов, «Есть кредит?»,
    тексты кода React, цвет цели — `HuePicker` (кит, `Field group`); (2) подсказки планирования —
    сверено, расхождений нет (PV-11); (3) вклад — `useSavedMark` по `updatedAt` (горит и гаснет),
    запись без изменений и так отсекают стор и `NumFieldBlur`; (4) пополнение цели — «Пополнить
    «имя»», placeholder — взнос цели, автофокус, «Внести» (у «Снять» — свои); (5) `SalaryDialog` на
    `Sheet`, автофокус «Новый оклад»; (6) `Hint` — `fixed`, `hintPosition` с зажимом к краям,
    закрытие прокруткой (захват), нажатием мимо и Escape; (7) `NumField` — буква не остаётся;
    (8) `/goals/:id` — «Цель», `/capital/:id` — «Вклад»; (9) строка кредита 0% — «без процентов»;
    Н-9 — ленивые `goals/:id`, `capital/:id`, `ritual`, `plan`: главный чанк 537 → 288 kB, без
    предупреждения, предкэш 12 файлов. Хвосты Б4: взнос убран из окна `GoalSheet`; «Готово» в
    `GoalSheet`/`WishSheet` — через `close()` кита (слот `Sheet` отдаёт `close`); «История цели» —
    по дате, новые сверху (и в слитом порядке «новые первыми», и после взноса в конец).
- **Отклонения от ТЗ:**
  1. **PV-21 — метка сброса `SyncDoc.resetAt` (сверх ТЗ).** Без неё критерий «стирает у обоих» не
     выполняется: телефон B, закрытый во время сброса, первым полным синком видит пустой сервер
     (`isEmptyDoc` → «первый выход в облако») и заливает свой документ обратно; если A уже прошёл
     мастер — слияние по id возвращает старые цели B, а `setupDoneAt` (`local ?? remote`) не снять.
     Так же в React (`src/store/sync.ts:96`). Минимальное решение: `resetDoc` ставит `resetAt`;
     `mergeDocs` при разных метках отдаёт документ с поздней целиком; `isEmptyDoc` с меткой — не
     пустой. Следствия: офлайн-правки партнёра, сделанные до встречи со сбросом, пропадают («Стереть
     всё» отменить нельзя); **старый клиент (до обновления PWA) метку не знает и сольёт по-старому** —
     до деплоя проверить, что оба телефона обновились (PV-07), и не нажимать сброс раньше.
     Мутация: без метки 2 из 3 e2e-сценариев падают.
  2. PV-21: шторка открывается и в демо (раньше «демо» было текстом): «Демо живёт только на этом
     телефоне.», сброс локальный со своим предупреждением (без «у обоих и в облаке»), без
     «Синхронизировать» и приглашения. Блок «Семья» (есть во Vue, нет в React) оставлен. Кнопка —
     «Синхронизировать» (React) вместо «Синхронизировать сейчас». После «Стереть всё» — явный
     `router.replace('/setup')`: гейт мастера во Vue только в роутере (React `SetupGate`
     реактивный); у партнёра с открытым приложением экран с нулями до первого перехода — хвост §4.
  3. PV-20: сверх таблицы ТЗ — `failed to hash password: …` (bcrypt, пароль длиннее 72 байт, 400) →
     «Пароль слишком длинный — выберите покороче.»; «unauthorized» во входе/регистрации — общая фраза
     (недостижимо). Тексты без пары в React — мои: «Аккаунт есть, но семьи в нём нет.», «Проверьте
     почту: в адресе нужен знак @.», «Такая почта уже зарегистрирована. Войдите с ней на вкладке
     «Войти».» (первая фраза — React), «Введите код приглашения.», «Код не найден. Проверьте, нет ли
     опечатки.», «Этот код уже использован. Попросите партнёра создать новый.», «Срок кода истёк: он
     действует две недели. Попросите партнёра создать новый.», «В этой семье уже нет свободных мест.»
     (слотов три, числа нет), «Код может создать только участник с правом правки — у вас только
     просмотр.». **«Код не найден» / «уже использован» на экране недостижимы:** «По коду» на `/access`
     работает только без сессии → Go 401 → «Чтобы войти по коду, сначала войдите в аккаунт.» (хвост
     «новые семьи»); тексты Go для них сверены на стенде API-запросами, перевод — юнит-тестами.
  4. PV-22: разделы — все пять ключей (`d1`–`d5`) с `categoryName` (разделы во Vue ленивые); ряд —
     подпись над образцами (`Field group` кита), а не колонка имени слева, как React.
  5. PV-23 п. 1: в тексте кода «…выбирает «По коду»» вместо React «Войти по коду» — так называется
     вкладка во Vue; «День зарплаты (1–28)» оставлен (Vue). Метки «Название», «За сколько месяцев
     хотите успеть», «Коммуналка в месяц, ₸ — примерно», «Создать код», «Минуту…», «Скопировано» — React.
  6. PV-23 п. 3: тест «гаснет по таймеру» — DOM (`views/Deposit.dom.test.ts`, happy-dom), а не SSR
     `Deposit.test.ts`: в SSR наблюдатели `useSavedMark` не срабатывают.
  7. PV-23 п. 4: автофокус суммы — и у «Снять». П. 5: «С какого месяца» в `SalaryDialog` — `Select`
     кита вместо нативного (Р-13: экран блок и так трогает).
  8. «История цели» — сортировка показа по дате, а не `contribute` в начало (вариант хвоста «показ по
     дате»): не зависит от порядка хранения.
  9. «Готово» в окнах Капитала (`AccountSheet`, `CreditSheet`, `ObligationSheet`) тем же изъяном не
     правил — файлов нет в ТЗ (правило 10) → хвост §4, кандидат — правка критика.
- **Грабли среды:**
  - Pre-commit-хук секретов ловит слово «password» с двоеточием и значением и в тексте ошибки Go про
    хеш пароля (bcrypt, длиннее 72 байт) — в тесте строка собрана из частей с пояснением; хук не
    обходился (в этом Handoff текст тоже без шаблона).
  - **`git checkout <файл>` при мутационной проверке стёр незакоммиченные правки PV-21 в `merge.ts`** —
    восстановил из копии, снятой перед мутацией. Мутации — только копия/восстановление (`cp`), не
    `git checkout`.
  - DOM-тест, в котором правка планирует синк, при прокрутке таймеров стучится на `localhost:3000`
    (happy-dom) — глушить `fetch` (`vi.stubGlobal`).
  - Смесь `renderScreen` глобальная — `setupState` брать только у компонента с нужным действием
    (`if ('submit' in this.$.setupState)`), иначе перезапишет последняя кнопка.
  - Стенд: `innerText` отдаёт подзаголовки в верхнем регистре (CSS `uppercase`); `main`, прокрученный
    до конца, на `scrollBy(0, +N)` события не даёт — прокручивать вверх.
- **Верификация:**
  - `vue-tsc -b` ✅; `npm run build` ✅ — без предупреждения о размере (главный чанк 287,9 kB),
    предкэш 12 файлов; `npm test` — **54 файла, 645/645** ✅ (было 564; +81).
  - Браузер — стенд §6 (Go на моках + Vite, Chrome 390px, два профиля + viewer): сценарий
    `block5.mjs` **58/58** — PV-20 (неверный пароль, «123», занятая почта, «По коду» без входа,
    тексты Go на стенде, 403 создания кода на Обзоре текстом, код создан, viewer без баннера); PV-21
    (шторка кита, дата, участники, пояснение, Escape, viewer — «только просмотр» без кнопок,
    «Стереть всё» → мастер, сервер пуст с меткой, **B открыл приложение после сброса — сервер
    остался пуст**, B в мастере); PV-22 (имя из документа, пустое вернулось, новое в Бюджете, «Слива»
    у «Жилья» → `--d1`, после перезагрузки на месте, тёмная пара); PV-23 (пояснение мастера, «45a» →
    «45», оклад на `Sheet` с фокусом и Escape, `Hint` в пределах 390 и закрывается прокруткой, «без
    процентов», «Вклад»/«Цель», «Сохранено» вклада гаснет, история новые сверху, пополнение — заголовок,
    фокус, placeholder 40 000, «Внести», окно цели без взноса, «Готово» сохранило, ленивые `/plan` и
    `/ritual`); консоль без ошибок. Скрипт, лог и скриншоты (в репо не кладутся):
    `C:/Users/SW/AppData/Local/Temp/claude/c--Users-SW-finance/f225db12-1ffd-4ebe-ae78-236bc8f09c92/scratchpad/stand/`
    (`block5.mjs`, `stand.mjs`, `run-2.log`, `p2*.png`).
  - Мутации: `mergeGoal` без якоря — новый тест seed падает; без метки сброса — 2 из 3 e2e;
    «Готово» через `emit('close')` — DOM-тест окна цели падает.
  - CI — не запускался: ветка не запушена (push — с согласия владельца).
- **Критику — на что смотреть:** метка `resetAt` в `mergeDocs`/`isEmptyDoc` (деньги: сброс побеждает
  офлайн-правки); полнота `authErrorText` против grep Go; хвост «Готово» в окнах Капитала — одна строка
  на окно, по силам правке критика.

## Итоги критика

## Приёмка
