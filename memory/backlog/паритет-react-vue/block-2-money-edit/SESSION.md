# Блок 2 — Правка денег · сессии

**Цель блока:** вернуть семье право исправлять деньги без удаления — кредит, обязательство,
валютный счёт, форму платежа с разделом и оценкой, удаление с честным предупреждением — на
едином ките окон, и показать по каждому платежу по кредиту, сколько ушло в долг, а сколько
банку.

Сессии: исполнитель ✅ · критик ✅ · приёмка ✅ · ревью frontend ✅ · клинап ⬜
<!-- L-блок. Каталог ревью — по факту коммитов Handoff-а; план — только frontend (Р-15). -->

---

## Промпт исполнителя — уровень build · Opus 5 · effort high (ultrathink)

```
/worker паритет-react-vue 2 ultrathink
```

Блок-специфика:

- Ветка `pv-block-2-money-edit` от `main` (после утреннего деплоя Блока 1 PV). Push — с
  согласия владельца; `main` не трогать.
- Задачи по порядку: `PV-09` (кит) → `PV-10` → `PV-11` → `PV-12` → `PV-13`. Кит первым:
  остальные модалки строятся на `Sheet`/`Select`/`Field group`/`useSavedMark`.
- **Деньги (L):** правка кредита/обязательства трогает якоря и отметки Блока 1 RP —
  `updateCredit` с `principal` ставит якорь, остальные поля — нет; производный остаток
  (`financeStore.credits`) — сверка, `creditBalance` на нём не звать; все новые числа
  (`fxToTenge`, `creditOutlook`, `paymentSplit`, `creditTotals`, `budgetInterest`,
  `creditSchedule`, разница «в месяц / за год») — `lib/finance.ts`.
- **Тексты — дословно из React** по строкам в ТЗ (Р-2); подсказки и предупреждения удаления —
  тоже.
- Р-13: на кит переводятся только экраны блока (Капитал, `PaidRow`); чужие модалки не трогать.
- **Что уже в коде после Блока 1 PV и клинапа Блока 1 RP** (факт, критик Блока 1 2026-09-25):
  - `lib/finance.ts`: `openCredits(list)` — живые с производным остатком > 0 (только кредиты из
    геттера стора); `costliestCredits(list)` — открытые с процентами, дороже первым (Капитал
    `rankedDebts`, Ритуал `credit`); `budgetAmounts` — «Кредиты» по `openCredits`, Бюджет и Обзор
    передают `{ ...householdDoc, credits: financeStore.credits }`; `installmentMonths`,
    `scheduleMismatch`, `goalHave`, `INFLATION`, `indexedNeed`, `strategyInputs`, `strategyGain`.
  - Клинап RP: `monthDues({ obligations, credits, payments }, key)` + `duesTotal` — одно правило
    «платежи месяца» (`untilPayday`, «На обязательства» Бюджета, «Впереди» Обзора); `plural(n, one,
    few, many)` в `lib/utils.ts` — все «N платежей / дней / месяцев» через неё (и новые тексты: React
    «платеж{ей|}» → склонение, решение Н-8); `lumpPlan` «снизить платёж» — `Math.max(1,
    Math.ceil(payment × left / debt))`; `prepaySaved(payments, credits)` — только живые кредиты,
    `credits` обязателен; `prepayUndoNote` в Капитале — текст снятия досрочки.
  - `components/StrategyCompare.vue` (вкладка «Копить или гасить» Капитала): пропсы `credits`
    (открытые), `goals`, `obligations`, `monthKey`, `initial?`; внутри — `Field label="Горизонт"` с
    `Segmented` (PV-09 `Field group` — и сюда). Кнопку «Выбрать этот план» добавит PV-15 (Блок 3).
  - Капитал: пропсы `initialAdvice` и `initialDebt` — только для SSR-тестов; форма «Долг или
    рассрочка» — `Segmented` «Без них / Знаю ставку / Знаю срок» в `Field label="Проценты"`,
    предупреждение `scheduleMismatch`, подписи React («День платежа», «Добавить»).
  - `lib/theme.ts` — тема и акцент (`readThemeChoice` / `setThemeChoice`, `readAccent` / `setAccent`,
    `applyCurrentPalette`, `isDark`, `watchSystemTheme`), `main.ts` применяет до `mount`; `Ring` без
    пропа `dark`. `lib/setup.ts` — `setupPlan`; `lib/pwa.ts` — `watchServiceWorkerUpdates`.
  - Номера строк в ТЗ PV-09…PV-13 пересчитаны критиком Блока 1 на HEAD `pv-block-1-calculator`
    (бэклог строился на `87cf02f`). Ветка Блока 2 — от `main` после утреннего деплоя: код тот же,
    но перед стартом `git log main -1` — если после деплоя были правки `Capital.vue`, сверить строки.
  - Грабли Блока 1: `Segmented` внутри `Field` (`<label>`) — имя кнопки включает подпись, в
    Playwright искать `button:has-text(/^Год$/)` (PV-09 это и чинит); `structuredClone` на
    реактивном документе Pinia — `DataCloneError`, в тестах клон через JSON; полностраничные снимки
    headless ловят середину `transition-colors` — ждать 300 мс; скрипты стенда — в scratchpad.
- Среда: стенд §6, два профиля + viewer, 390px; курс на стенде без сети — «недоступен».
- Верификация: `cd frontend && npm run build && npm test` + браузер по критериям ТЗ.
- Следующий шаг — `/critic паритет-react-vue 2 ultracode`.

---

## Промпт критика — уровень review · Opus 5 · effort xhigh (ultracode)

```
/critic паритет-react-vue 2 ultracode
```

Блок-специфика:

- Деньги: правка ставки/дня/платежа не меняет производный остаток и якорь; правка остатка —
  якорь; `Σ body` графика = остаток; разбивка в `PaidRow` = снимок записи (не пересчёт по
  текущему остатку); округления только в `finance.ts`; целые на выходе (прогон на случайных
  входах, как приёмка Блока 1 RP).
- `removeAccount` отвязывает цели в `householdDoc` для приватного счёта; слияние правок
  (LWW) — тест с двух устройств для кредита и обязательства.
- Кит: Escape и фокус; `Field group` там, где группы кнопок; `--scrim` в обеих темах;
  `PaidRow` на `Row` не сломал e2e Блока 1 RP (порядок «оплаченное вниз», знак «−»).
- Тексты React дословно — пройти по строкам ТЗ.
- Живые доки: `frontend/README.md` — секция кита, если есть.
- **Промпт Блока 3 актуализировать**: имена функций разбивки/графика (для PV-17), как устроен
  `Sheet` (для окна выбора плана), `StrategyCompare` после Блока 1, `applyPrepayment` opts.
- Следующий шаг — `/accept паритет-react-vue 2 ultracode`.

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh (ultracode)

```
/accept паритет-react-vue 2 ultracode
```

Блок-специфика:

- Критерии ТЗ PV-09…PV-13 на стенде §6, два профиля + viewer: правка кредита (день, ставка,
  платёж, остаток-сверка), обязательства (раздел, оценка, день, «Чьё это», история суммы),
  валютного счёта (курс, пересчёт), удаление с текстами React и отвязкой целей, повторный «+»,
  разбивка и график (сверить скриптом: Σ тела = остаток), Бюджет «из них проценты».
- Регрессия: весь `frontend/e2e/` + `npm test`; эталоны Блока 1 RP; CI зелёный на HEAD.
- Сценарии — `frontend/e2e/pv-block2-money-edit.test.ts`.
- Кит после критика: фон закрывает только нажатие, начатое на нём (выделение из поля до фона —
  окно остаётся), Tab/Shift+Tab не выходят из окна, Enter на строке после Escape снова
  открывает окно; калькулятор закрытого кредита — «долг закрыт».
- Р-14 и раздел: смену раздела в модалке обязательства владелец отдал клинапу Блока 2
  (`memory/decisions/r2-exceptions-pv-block2.md`) — приёмка её не ждёт. Список Р-14 — как
  есть; дополнительно (только чтение): есть ли в `categories` прода `d1` и `d2` (хвост →
  PV-15 п. 7: без раздела платёж не виден в «Куда уходит»).
- **Сверка испорченных данных (Р-14)** — только с «да» владельца в этой сессии, прод только на
  чтение (`memory/secrets/supabase-db.md`, SELECT по `household_docs.data`): выписать в
  «Приёмка» подозрительные записи — обязательства `category d4` без `estimate` (кроме реальных
  подписок), кредиты с `annualRate 0` при `note 'ежемесячный платёж'`, цели с `have < 0`. Не
  править: владелец правит руками в вернувшихся формах после деплоя.
- **L-блок: приёмка НЕ деплоит.** Следующий шаг — `/dir-review паритет-react-vue 2 frontend`.

---

## Промпт ревью frontend — уровень review · Opus 5 · effort xhigh

```
/dir-review паритет-react-vue 2 frontend
```

Блок-специфика:

- Каталог `frontend/`: кит — не осталось ли копий `fixed inset-0`/`bg-black/40` на экранах
  блока; дубли между модалками Капитала (текст «в долг / банку», выбор счёта — Н-11);
  `PaidRow` на `Row`; согласованность `creditSchedule` с `monthDues`/`untilPayday`.
- Если коммиты тронули `backend/` — это нарушение Р-15: зафиксировать, вердикт.
- Хвост §4 «округление в Капитале» (владелец: через ревью → клинап): все `Math.round` / `*` /
  `/` над деньгами в `Capital.vue` — с файл:строка и целевой функцией `finance.ts`, вердикт
  «клинап». Хвосты «раздел обязательства», «годовое ×12», «валютный счёт и сдвиги» уже
  отданы клинапу владельцем — в REVIEW не дублировать.
- **От приёмки** (2026-09-25):
  - Хвост «Infinity в строке кредита» (владелец: через ревью → клинап, вместе с округлением):
    при платеже ≤ процентов строка кредита Капитала пишет «Infinity платежей» и «переплата ∞»
    (`paymentsLeft` — `Math.ceil(Infinity)`, `sub` — `plain(Infinity)`); окно и калькулятор уже
    пишут «долг не закрывается». Цель — строка на `creditOutlook` (`closes`), вердикт «клинап».
  - `SavedMark` всегда в DOM внутри `h3` шапки (`opacity-0`, `aria-live="polite"`): «Сохранено»
    попадает в `innerText` окна и, вероятно, в озвучку. Имя окна не страдает (`aria-labelledby`
    — только `title`). Вердикт на усмотрение ревью.
  - `frontend/e2e/` не входит ни в `tsconfig.app.json`, ни в `tsconfig.node.json`: `vue-tsc -b`
    тесты e2e не проверяет. В `mountLive` (`e2e/pv-block2-money-edit.test.ts`, ~:274) уже есть
    TS2339 `vm.$.setupState`. Вердикт на усмотрение ревью.
- Следующий шаг — `/cleanup паритет-react-vue 2`.

---

## Промпт клинапа — уровень build · Opus 5 · effort high

```
/cleanup паритет-react-vue 2
```

Блок-специфика:

- Выполнить «клинап»-вердикты `REVIEW-frontend.md`.
- Хвосты §4 критика, отданные клинапу владельцем 2026-09-25 (до деплоя, с тестами):
  1. Модалка обязательства — «В какой раздел бюджета» (кнопки `obBuckets` → `updateObligation({
     category })`, только member; исключение из Р-2 —
     `memory/decisions/r2-exceptions-pv-block2.md`).
  2. `plannedChange(current, planned, every?)`: у годового — `{ monthly: yearShare(d), yearly: d
     }`; `Capital.test.ts` «планирование» → 1 000 / 12 000.
  3. Выбор счёта для тенговых сдвигов — только `payableAccounts`: «Внеплановый доход» → «На
     счёт» (`Capital.vue`) и пополнение/снятие цели (`GoalDetail.vue:322`).
  4. *(хвост приёмки, владелец 2026-09-25)* Калькулятор досрочки: при смене кредита сбрасывать и
     выбор «Сократить срок / Снизить платёж» — `applyMode.value = 'term'` в
     `watch(payoffCreditId)` (`Capital.vue:779-790`); тест: выбор «Снизить платёж» у кредита А
     не переходит в калькулятор кредита Б.
  После правки 1 владелец правит испорченные записи из списка приёмки (Р-14).
- **Деплой** (с «да» владельца): записать ID текущего Production-деплоя, merge
  `pv-block-2-money-edit` → `main`, push; смоук на `family-finance-ff.vercel.app`: остатки те
  же, правка дня у живого обязательства видна на втором телефоне, форма платежа с разделами.
  Владелец сразу после — правка испорченных записей из списка приёмки (Р-14). **Откат:**
  promote записанного деплоя.
- Хвосты из ревью — в §4; «Итоги пост-приёмки», 🏁. Следующий шаг —
  `/worker паритет-react-vue 3 ultrathink`.

---

## Handoff (заполняет исполнитель)

Исполнитель, 2026-09-25 (Opus 5.5, ultrathink). Ветка `pv-block-2-money-edit` от `main` `ff0fe03`,
**не запушена** (push — с согласия владельца, §5). Коды затронуты только в `frontend/` (Р-15:
`git diff main --stat -- backend src` пусто).

- **Коммиты** (репо `finance`, ветка `pv-block-2-money-edit`):
  `dce52d5` PV-09 · `6e27286` PV-10 · `7764766` PV-11 · `d21472e` PV-12 · `e98ecb0` PV-12
  (исправление: тип `Goal` в SSR-тесте — коммит PV-12 прошёл с упавшим vue-tsc, см. грабли) ·
  `cc87eef` PV-13 · + коммит доков (этот Handoff, §4).
- **Что сделано:**
  - **PV-09** — `kit/Sheet.vue` (лист снизу на телефоне / окно по центру с `sm`, `--scrim` +
    `backdrop-blur-xs`, `shadow-lift`, `role="dialog"` с `aria-labelledby`, крестик/фон/Escape;
    Escape — только верхний лист по стеку открытых; фокус в окно при открытии и назад при
    закрытии; пропсы `open`, `title`, `z`; слоты тела, `mark`, `footer`), `kit/Select.vue`,
    `Field group` (`<div role="group" aria-label>`), `Row` (слоты `action`/`note`/под строкой,
    `dense`, `muted`), `kit/useSavedMark.ts` (правило React; `SalaryDialog` на нём), токен
    `--scrim` в `:root` и `.dark` + `--color-scrim`. Капитал: 10 модалок на `Sheet`, 6 `<select>` на
    `Select`, все группы кнопок — `Field group` (+ «Горизонт» в `StrategyCompare`), общий
    Escape-обработчик убран. `PaidRow` на `Row` + `Sheet z=60`: отклик, шеврон, `dragged`; проп
    `minus` — в Бюджете платёж снова «−N».
  - **PV-10** — модалка кредита: поля React по уходу из поля (название, остаток — сверка с якорем,
    платёж, ставка с нулём, день 1..28, примечание), «Посчитать досрочное погашение»,
    `creditOutlook` (`finance.ts`) → «Платежей осталось / Переплата до конца» или текст «долг не
    закрывается», «Сохранено», удаление с текстом React; `updateCredit` — `unchanged()`; снимок
    «Оплатил» — заново при смене id/дня/платежа/ставки. Калькулятор: сброс суммы и режима при
    смене кредита, «Переплата, если не трогать» / «долг не закрывается», подсказка — первый чип,
    «половина переплаты», «Впишите сумму…», «Отдача падает» + «Половину переплаты снимает уже…».
  - **PV-11** — форма: «В какой раздел бюджета» (d1/d2/d4, имена семьи или
    `DEFAULT_CATEGORY_NAMES` в `lib/palette.ts` — ею же пользуется `setCategoryAmount`), флажок
    оценки, сетка месяцев, подсказка про двенадцатую часть (`yearShare`). Модалка обязательства:
    все поля React, «Чьё это», оценка, планирование (подсказка `plain(current)`, автофокус,
    `plannedChange` в месяц и за год, подсказка о месяце, очистка поля), «История суммы»,
    «Сохранено», удаление React; `updateObligation` — `unchanged()`; снимок «Оплатил» — заново при
    смене id/дня/периодичности/месяца; форма сбрасывается только при смене обязательства.
  - **PV-12** — `fxToTenge`; добавление: три состояния курса, «В капитале это …» + «Курс
    запоминается…», «— если есть»; правка валютного (сумма в валюте, курс → тенге и `rateAt`,
    «по этому курсу») и тенгового («Сумма, ₸» — сверка); удаление — текст React со списком целей;
    `removeAccount` отвязывает цели в общем документе и для личного счёта; текст удаления вклада —
    как в React; окна по адресу при закрытии чистят адрес (повторный «+» работает).
  - **PV-13** — `paymentSplit`, `creditTotals`, `budgetInterest`, `creditSchedule` (все целые);
    «в долг / банку» в `PaidRow` и листе «оплачено»; строка кредита — следующий платёж с
    разбивкой; «За всё время» в модалке; разбивка у применённых досрочек; свёрнутый «График
    платежей»; Бюджет — «из них проценты банку N ₸ в месяц».
- **Отклонения от ТЗ (с причинами):**
  1. `Sheet`: при рендере без `document` (SSR-тесты) `Teleport` отключён — окно рисуется на месте.
     В браузере — в `body`, как в ТЗ. Иначе все SSR-проверки модалок (Блок 1 RP/PV и новые) пришлось
     бы переводить на `ctx.teleports`.
  2. `Sheet` сверх ТЗ: слот `mark` («Сохранено» в шапке — нужен PV-10/11/12) и фокус (в окно / назад)
     — под критерий «Escape и фокус». `Row` сверх `action`: слоты `note` и под строкой, `dense`,
     `muted` — без них `PaidRow` на `Row` не встаёт (две строки подписи у оплаченного, «Другая сумма
     или счёт» под строкой, приглушённое название). `PaidRow.minus` — «−» только в Бюджете.
  3. Тест токена `--scrim` — в `e2e/pv-block2-money-edit.test.ts`: в `src/**` Vitest отдаёт CSS
     пустым (и через `?raw`), а `node:fs` не типизирован vue-tsc.
  4. PV-10: viewer видит цифры кредита (остаток, платёж, ставка, день) без полей и удаления;
     калькулятор досрочки ему доступен (это расчёт). Закрытому кредиту (остаток 0) выводы не
     показываются — иначе «долг не закрывается» у закрытого долга. Правка ставки/платежа после
     «Оплатил» сентября переводит строку модалки на октябрь (снимок по ТЗ п.3). Результат
     калькулятора выровнен по React сверх списка п.5 (Р-2): «экономия …», «Останется N платежей
     вместо M» (`plural`). Шапка калькулятора — тем же `creditOutlook`. Подсказка поля — `plain()`
     (React — `String()`, без разрядов).
  5. PV-11: тексты формы — по React: подсказка «Например, интернет или абонемент», кнопка
     «Добавить». Разделы — всегда d1/d2/d4 (частично заведённые `categories` тоже покрыты). «Чьё
     это» в форме — чипы Vue, в модалке — `Segmented` (как React). «Каждый месяц» не стирает
     `month`: `month?: number` не допускает `null`, а `undefined` по LWW не дошёл бы до второго
     телефона; при ежемесячном поле не читается (`dueIn`). Viewer — сумма, день, частота и история
     без полей; выбор группы подписки ему тоже скрыт.
  6. PV-12: очистка адреса — одним наблюдателем «все окна по адресу закрыты», а не функцией на
     каждом пути: путей много (крестик, фон, Escape, «Готово», запись, удаление, переход кредит →
     калькулятор); `applyExtraIncome` больше не чистит `income` сам. Адрес держится, пока открыто
     хоть одно такое окно. e2e — «пустой» рендерер Vue (`createRenderer` без DOM) прямо в тесте:
     SSR наблюдатели не запускает, DOM-библиотек нет (новых не вводим); мутационно проверено —
     без наблюдателя тест падает. Дата курса — разбором `YYYY-MM-DD` («25.09.2026», без сдвига
     пояса). Кнопка «Добавить счёт» не переименована (не в ТЗ). Viewer — цифры счёта без полей.
  7. PV-13: в строке «Оплатил» разбивка — две короткие строки под суммой («в долг X», «банку Y»):
     одной строкой колонка суммы на 390px выдавливает название; в листе «оплачено» — одной строкой
     «Из них: в долг X · банку Y». «из них проценты банку» — в «Куда уходит» под разделом «Кредиты»
     (там сумма d2; раздел виден, если заведён). График не показывается, если платёж не покрывает
     проценты (600 одинаковых строк; выше уже текст «долг не закрывается»). `ScheduleRow.extra` —
     и применённые досрочки месяца (только показ, в остатке уже учтены), и досрочки плана
     (вычитаются до платежа месяца); остаток оплаченных месяцев перед первым неоплаченным —
     обратным ходом. `creditTotals.count` считает и досрочки («N платежей»).
  8. Хвост в §4: viewer видит кнопки добавления, окно группы и экран вклада с полями (было до
     блока, вне ТЗ).
- **Грабли среды:**
  - Цепочка `npm run build 2>&1 | grep …` прячет код выхода: PV-12 закоммичен с упавшим vue-tsc
    (тип теста), исправлено `e98ecb0`. Проверять `echo $?` после сборки.
  - Heredoc в Bash-инструменте ломается на части сочетаний кавычек — скрипты правок и тестов
    писать файлом (Write) в scratchpad.
  - Vitest в Node компилирует `.vue` для SSR: клиентский `createApp` падает на
    `useSSRContext().modules` — `app.provide(ssrContextKey, { modules: new Set() })`; рендера у
    такого компонента нет, живут setup и наблюдатели.
  - Роутер-гард без авторизации уводит на `/access?…`, сохраняя query — проверка «окно открыто по
    адресу» может пройти случайно; телефону в e2e давать `setAuthData`.
  - Значения `NumField` — с неразрывными пробелами разрядов (в Playwright нормализовать).
  - На этой машине сеть есть: `/api/fx-rate` отдаёт живой курс Нацбанка; «недоступен» проверен
    подменой ответа (`page.route` → 503).
  - После `Field group` имя кнопки — только её текст: `getByRole('button', { name: 'Разово', exact:
    true })` работает (грабли Блока 1 с «Как вносите Разово» сняты).
- **Верификация:**
  - `cd frontend && npm run build` — код 0; `npm test` — 37 файлов, **354/354** (было 298, +56:
    кит, стор, `finance.ts` с прогоном 300 случайных кредитов для графика, SSR Капитала/Бюджета/
    `PaidRow`, e2e блока на двух телефонах — `e2e/pv-block2-money-edit.test.ts`).
  - Браузер — стенд §6 (Go на моках + Vite, Chrome headless, 390px; второй телефон — второй
    контекст той же семьи через синк; скрипты в scratchpad): PV-09 42/42 (10 модалок и лист ×
    крестик/фон/Escape, лист поверх модалки, фокус, подпись «Как вносите», лист снизу на 390px и по
    центру на 1280px, тёмная тема — `--scrim` и `--shadow-2` из `.dark`, «−» в Бюджете), PV-10 23/23,
    PV-11 16/16, PV-12 20/20, PV-13 17/17 (Σ тела графика = остаток скриптом, 24 строки), viewer 6/6
    (роль в `ff_member` клиента). Финальный прогон всех сценариев на HEAD — 118/118, консоль чистая.
  - **CI не прогнан** — ветка не запушена (нужно «да» владельца).

## Итоги критика

Критик, 2026-09-25 (Opus 5.5, `ultracode`): workflow — 6 ревьюеров по измерениям (кит;
кредит и график; обязательство; счета и адрес; тексты React и viewer; реюз) → скептик на каждое
измерение пытается опровергнуть. 25 находок: 18 подтверждены (11 правок — одна находка в двух
измерениях — и 6 хвостов), 7 опровергнуты.

- **Коммиты** (ветка `pv-block-2-money-edit`): `11685f3` PV-09 (критик) · `8eab9a6` PV-10…PV-12
  (критик) · + коммит доков.
- **Правки:**
  1. `Sheet`: выделение текста из поля, отпущенное на фоне, закрывало окно и теряло правку (click
     уходит общему предку — оверлею) → закрывает только нажатие, начатое на фоне.
  2. `Sheet`: не было ловушки фокуса — Tab уводил под затемнение, Enter там открывал второе окно
     ПОД текущим, Escape закрывал невидимое → Tab/Shift+Tab зациклены в верхнем окне.
  3. `Row`: после возврата фокуса листом и движения мыши над строкой Enter глушился защитой от
     свайпа → клик с клавиатуры (`detail 0`) не отсекается.
  4. PV-10: калькулятор закрытого кредита писал «долг не закрывается» → «долг закрыт» (кнопку не
     прятали: в окне «Снять» у досрочки, закрывшей долг).
  5. PV-10: снимок «Оплатил» не появлялся, когда закрытый долг снова открыт (сверка остатка,
     снятая синком отметка) → ещё один источник наблюдателя.
  6. PV-11: «Сумма сейчас» не следовала за документом — после «Запланировать» с этого месяца или
     правки партнёра уход из поля откатывал сумму (LWW) → `NumFieldBlur :initial` (образец проекта).
  7. PV-12 (Б-15): удалённая синком запись оставляла id в ref — адрес больше не чистился, «+» не
     открывал форму → очистка по «окно показано»; блок ниже объявлений окон (наблюдатель читает их
     сразу — TDZ поймал тест, скептик считал computed ленивым).
  8. Лесенка «Отдача падает» — «+29 000» без «₸» (React `:1519`).
  9. Упрощение: `payoffCost` ≡ `payoffOutlook` (тот же `debtCost`) — убран; `moveToGroup` без
     повторного `unchanged()`.
  10. Тесты по секциям «Тесты» ТЗ: e2e — «Оплатил» у оценки открывает лист без отметки, без
      оценки — одно нажатие (было — только подпись «оценка»); SSR — правка валютного из окна
      (`onAccountRate` / `onForeignAmount`; стор-тест сам передавал пересчитанную сумму); плюс
      тесты на правки 4–8.
- **Проверено и верно:** `creditSchedule` на 400 случайных историях (Σ тела = производный остаток,
  целые, кап 600, обратный ход оплаченных, досрочки не дважды, якорь между отметками);
  `paymentSplit` — снимок записи; `creditTotals`, `budgetInterest`; `updateCredit` /
  `updateObligation` — `unchanged()`, якорь только от «Остаток»; `removeAccount` (общий и личный,
  `null`); `fxToTenge` — единственное умножение на курс; тексты React PV-09…PV-13 строка в строку
  (кроме п. 8); viewer — ни полей, ни кнопок в новых окнах; `Field group` везде, хрома модалок и
  `<select>` на экранах блока нет; ожидания e2e Блока 1 RP не ослаблены.
- **Хвосты в §4 (6, судьба ⏳ — владелец):** раздел обязательства не меняется (**решить до сверки
  Р-14**); годовое обязательство — «в месяц / за год» ×12 (React так же); запасные разделы без
  раздела в Бюджете; валютный счёт теряет тенговые сдвиги при правке курса; «Назад» не закрывает
  окно по адресу; округление денег в `Capital.vue` (до блока).
  **Судьбы (владелец, 2026-09-25):** клинап Блока 2 — раздел обязательства, «×12» у годового
  (оба — исключения из Р-2, `memory/decisions/r2-exceptions-pv-block2.md`), валютный счёт и
  сдвиги, округление (через ревью frontend); PV-15 п. 7 — разделы в Бюджете/Обзоре; принятый
  долг — «Назад».
- **Опровергнуто (7):** лишний `due` у `paymentSplit` (сигнатура из ТЗ); ручные расчёты строки
  кредита, результата досрочки и разбора полей (до блока, 1:1 React); два правила тела записи;
  «слабые» viewer-тесты; дубли карточек «подпись — значение».
- **Доки:** §6 бэклога — кит (`Sheet`, `Select`, `useSavedMark`, `Field group`, `Row`), функции
  Блока 2, окна по адресу (Б-15), номера строк. `frontend/README.md` — шаблон Vite, секции кита
  нет — не трогал.
- **Промпт Блока 3** актуализирован (разбивка и график, `Sheet`, `StrategyCompare`,
  `applyPrepayment`, Б-15); номера строк ТЗ PV-14…PV-16 пересчитаны на HEAD.
- **Верификация:** `npm run build` — код 0; `npm test` — 37 файлов, **358/358** (было 354).
  Мутационно: откат каждой из правок 4–7 и ветки оценки в `PaidRow.tap` роняет свой тест. Браузер —
  стенд §6 (Go на моках + Vite, Chrome headless, скрипт в scratchpad): 17/17 (1280px: выделение до
  фона, Tab/Shift+Tab ×25, Escape → фокус на строке → Enter, лист поверх модалки, «+» дважды;
  390px: тап по фону), консоль чистая; контрольный прогон на ките HEAD — ровно 4 FAIL (правки 1–3).
  **CI не прогнан** — ветка не запушена (push — с «да» владельца).

## Приёмка

Приёмщик 2026-09-25 (Opus 5.5, `ultracode`, независимая сессия). Ветка `pv-block-2-money-edit`, дифф
`ff0fe03..c0967fa` (25 файлов, только `frontend/`) + свой коммит `7f8772f` (сценарии). Бэкенд и React
`src/` не тронуты (`git diff main..HEAD -- backend api go.mod src` пусто, Р-15). Ветка запушена с
«да» владельца. **L-блок: приёмка не деплоит**, 🏁 не ставит.

**Вердикт: ✅ принято.** Все критерии PV-09…PV-13 выполнены в реальной среде. Расхождений для
исполнителя нет. Три находки вне ТЗ получили судьбу от владельца (ниже, §4).

- **Как проверялось.** Стенд §6: Go на моках + Vite dev, Chrome headless (`playwright-core`), 390 и
  1280px, Asia/Almaty. Workflow из 7 агентов: 4 проверяющих по ТЗ, у каждого своя семья на общем стенде,
  второй телефон — второй контекст, viewer — роль в `ff_member`. Пятый агент писал e2e. Скептик
  воспроизводил каждый FAIL своим скриптом. Мутационная проверка e2e шла после браузера. Итог по
  пунктам: **49 из 49**. Проверяющий записал один FAIL, скептик его опроверг: это не критерий ТЗ.
  Консоль чистая во всех прогонах.
- **Сверка критериев:**
  - PV-09 (14/14). 10 модалок Капитала и 5 листов «Оплатил» (модалки кредита и обязательства, «До
    зарплаты», Бюджет) закрываются всеми тремя путями: крестиком, фоном, Escape. Проверено на 390 и
    1280. Escape над модалкой закрывает только лист. Выделение из поля, отпущенное на фоне, окно не
    закрывает. Tab и Shift+Tab по 25 раз в 4 окнах не выводят фокус наружу. После Escape фокус на
    строке, Enter снова открывает окно, в том числе после движения мыши (правка 3 критика).
    Затемнение: `rgba(13,21,18,.4)` в светлой теме, `rgba(0,0,0,.6)` в тёмной — это `--scrim` из
    `:root` и `.dark`. Тень — `--shadow-2` обеих тем. На 390 — лист снизу, на 1280 — окно по
    центру. Подписи 15 групп ничего не нажимают. В Бюджете «−N», на Капитале и Обзоре знака нет.
    Viewer видит цифры без полей и удаления.
  - PV-10 + PV-13 (15/15). Правки дня, ставки, платежа и названия дошли до строки, Бюджета и
    «Оплатил» и видны второму телефону. Производный остаток не сдвинулся. Сверка остатка ставит
    якорь. «Сохранено» гаснет за ~1,8 с. Удаление и калькулятор — с текстами React: «Отдача
    падает», «Половину переплаты…», «Впишите сумму…». При смене кредита поле пустое и стоит
    «Каждый месяц». У закрытого долга — «долг закрыт». Разбивка: тело + банку = платёж в строке,
    модалке и листе. Проценты равны `round(остаток × ставка / 12)`, скрипт считал их независимо.
    Σ «в долг» графика = производный остаток. «За всё время» после отметки выросло ровно на
    снимок записи. «из них проценты банку» в Бюджете равно сумме по открытым кредитам.
  - PV-11 (11/11). В форме разделы d1, d2, d4, по умолчанию d4, оценка снята, есть сетка месяцев.
    На семье без разделов — запасные имена. «Интернет» без оценки — подписка: вопрос «Оставить?»
    с нового квартала (часы контекста переведены на 02.10). Коммуналка с оценкой спрашивает сумму,
    без оценки хватает одного нажатия. Аренда в «Жильё» — это d1. Правка дня и «Чьё это» видна в
    Бюджете и на втором телефоне. Модалка, планирование, «История суммы» (2 строки) и удаление —
    с текстами React.
  - PV-12 (9/9). Курс подставляется с датой и источником. При 503 — «недоступен — впишите
    вручную». Правка суммы в валюте или курса: тенге = `round(валюта × курс)`, строка списка
    сходится. Удаление счёта с привязанной целью: текст с её именем. Капитал «на счетах» стал меньше
    ровно на сумму счёта, цель снова считается отдельно — и в общем документе, и у личного счёта.
    Текст удаления вклада — как в React. «+» → «Кредит или рассрочка» дважды: форма открывается
    оба раза, адрес чистится (то же для «Платёж» и «Внеплановый доход»).
- **Верификация:** `npm run build` — код 0; `npm test` — **37 файлов / 365 тестов** (было 358, +7
  сценариев приёмки); весь `e2e/` — 11 файлов / 54 теста. Эталоны Блоков RP и PV-1 не тронуты.
  **CI `ci-frontend` ✅**: `c0967fa` (run 36114553468) и `7f8772f` (run 36119781146).
- **Сценарии в e2e:** `frontend/e2e/pv-block2-money-edit.test.ts`, describe «Приёмка Блока 2 —
  сценарии критериев», 7 сквозных сценариев на двух телефонах (`7f8772f`). Покрыто: подписка и
  правка дня/«Чьё это» (PV-11); история суммы и viewer (PV-11); удаление счёта с целями и
  валютный счёт (PV-12); «За всё время», проценты Бюджета, Σ тела графика (PV-13); правка
  платежа без якоря, калькулятор при смене кредита, «долг закрыт» (PV-10). Ожидаемые числа —
  литералы по формулам, их сверил приёмщик: 27 500 → 26 661 → 23 911 → 22 974; 130 500 /
  164 589 и 51 411; 1 337 × 479,26 = 640 771; 22 платежа. **Мутационно: все 26 мутаций краснят свой
  сценарий**, после проверки `frontend/src` чистый.
- **Сверка Р-14** (прод только на чтение, «да» владельца в этой сессии, один SELECT по
  `app.household_docs.data`):
  - обязательства `d4` без оценки — **нет**. Живых обязательств два, оба в `d1`;
  - кредиты под 0% с `note 'ежемесячный платёж'` — **«Фитнес Аруна»** (`pjhcewbx`). По сути это
    ежемесячный платёж, заведённый кредитом. Две рассрочки «Айфон Ильяс» (`tmnb5cu6`) и «Айфон
    Аруна» (`hakoc1s0`) под 0% с `note 'рассрочка'` — законные, не правятся;
  - цели с `have < 0` (и `seed + Σ движений < 0`) — **нет**;
  - в `categories` прода есть **все разделы d1–d5**. Хвост PV-15 п. 7 в проде этой семьи не
    проявляется, только у новых семей.
  - Безопасность (советник Supabase): у таблиц схемы `app` выключен RLS. У ролей `anon` и
    `authenticated` нет `USAGE` на схему `app` (проверено `has_schema_privilege`), ключом anon
    они недоступны. Сообщено владельцу, ничего не менялось.
- **Находки вне ТЗ** (судьбы — владелец, 2026-09-25; в §4):
  1. Калькулятор досрочки: при смене кредита не сбрасывается выбор «Сократить срок / Снизить
     платёж» (`applyMode`, RP-08; `Capital.vue:779-790` сбрасывает остальное). Это и было FAIL
     проверяющего: скептик воспроизвёл, но это не критерий ТЗ, и так было до блока. → **клинап
     Блока 2** (п. 4 промпта клинапа).
  2. Строка кредита при платеже ≤ процентов: «Infinity платежей» / «переплата ∞». Так в React и
     до блока. → **ревью frontend → клинап** (в промпте ревью).
  3. При каждом открытии приложения первый круг синка шлёт документ без правок
    (`stores/syncEngine.ts:54`, RP-04): rev +1 за открытие. У настоящего viewer — 403 и ошибка
    синка при каждом старте. Воспроизведено на коде `main`: 4 загрузки → 4 одинаковых POST, не
    регрессия блока. → **хвост «новые семьи» в `memory/STATE.md`**.
- **Замечания** (не блокируют):
  - `Select` на Капитале — 5, а не 6: месяц годового обязательства стал сеткой (PV-11). Сырого
    `<select>` нет.
  - Бюджет у кредита с платежом меньше процентов считает «банку» весь платёж (`creditSplit`
    ограничивает проценты платежом). Так по формуле ТЗ PV-13.
  - График начинается с текущего месяца (`from = monthKey()`), оплаченные прошлые месяцы в нём не
    показываются. Так задумано.
  - В `SavedMark` и `e2e`/tsconfig — вопросы к ревью frontend (в его промпте).
- **Грабли стенда:**
  - Push — `{ last_seen_rev, data }`. Регистрация сразу создаёт документ семьи: засев — POST с
    его rev.
  - Личный документ тянется только при входе: личные счета засевать через `ff_private_doc`.
  - При 4 параллельных проверяющих первый `goto` на Vite бывает дольше 8 с — таймаут 60 с.
  - «ОТДАЧА ПАДАЕТ» — CSS uppercase, сверять по `textContent`. В калькуляторе две кнопки
    «Закрыть» — крестик брать по `aria-label`.
  - `core.autocrlf=true`: `git checkout -- <файл>` после мутации переписывает LF в CRLF.
    Возвращать сохранённой копией байт в байт.
  - Стенд погашен (5173, 5174, 8080 свободны). Проверочный worktree `main` удалён: сначала
    junction `node_modules`, потом сам worktree. Пустая папка критика `frontend/src/__critic_tmp__`
    удалена.

## Итоги пост-приёмки (только L)
