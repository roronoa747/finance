# Блок 1 — Калькулятор и точные расчёты · сессии

**Цель блока:** вернуть семье калькулятор «копить или гасить» таким, каким он был в React
(горизонт, цели-страховки, подушка, накопленное, вывод в обе стороны), и убрать неверные
цифры: закрытый кредит в плане, ставка «по сроку» под 0%, минус у цели, инфляция 8%, прогноз
цели; плюс мастер, PWA и тема на старте — без чего проверка остального в браузере неудобна.

Сессии: исполнитель ⬜ · критик ⬜ · приёмка ⬜ · утренний деплой ⬜

**Ночной прогон (решение владельца 2026-09-24, §5):** три роли идут подряд субагентами, без
вопросов владельцу; приёмка **не деплоит**; деплой и 🏁 — утром с владельцем. Прод (Supabase,
Vercel) не трогать; `main` не трогать; push ветки `pv-block-1-calculator` ради CI — можно.

---

## Промпт исполнителя — уровень build · Opus 5 · effort high

```
/worker паритет-react-vue 1
```

Блок-специфика:

- **Ветка `pv-block-1-calculator` от `rp-block-1-payments`** (не от `main`) — после того, как
  клинап Блока 1 RP закоммитил свои правки (проверить: в
  `../развитие-приложения/block-1-payments/SESSION.md` заполнены «Итоги пост-приёмки», в
  трекере «клинап ✅»; последний коммит ветки — клинапа). Push ветки — можно (CI);
  `main` не трогать; прод не трогать.
- Прочитать «Итоги пост-приёмки» Блока 1 RP: клинап мог завести `monthDues` (Н-3),
  `plural` (Н-8), поправить `lumpPlan` (Н-1), `prepaySaved` (Н-4) — ТЗ ссылаются на код до
  клинапа, номера строк в `Capital.vue`/`finance.ts` могли сдвинуться.
- Задачи по порядку: `PV-01` → `PV-02` → `PV-03` → `PV-04` → `PV-05` → `PV-06` → `PV-07` →
  `PV-08`. PV-01 — фундамент калькулятора (закрытые кредиты вне стратегии); PV-05 ждёт
  `INFLATION` из PV-04.
- **Эталон — React `src/`** (Р-2): тексты дословно по строкам из ТЗ, `src/` не менять.
  Все числа — `lib/finance.ts`, компоненты только показывают; деньги целые.
- Ловушка производных остатков: `financeStore.credits` уже вычел отметки — `creditBalance` на
  нём не звать (§6).
- Среда: локальный стенд (§6) — Go на моках + Vite dev, два профиля, 390px; PWA (PV-07) — в
  `npm run preview`. Тема (PV-08) — проверить и на `/access` после выхода.
- Верификация: `cd frontend && npm run build && npm test` (весь `e2e/` внутри) + браузер по
  критериям каждого ТЗ. Handoff — с эталонными числами калькулятора (для приёмки).
- Следующий шаг — `/critic паритет-react-vue 1`.

---

## Промпт критика — уровень review · Opus 5 · effort xhigh

```
/critic паритет-react-vue 1
```

Блок-специфика:

- Паритет с React по файл:строка из ТЗ: пройти пункты 1–7 `StrategyCompare` и тексты PV-03,
  PV-04, PV-05 дословно; расхождение текста — правка, не хвост.
- Деньги: `strategyInputs` и `strategyGain` — целые; `simulateStrategy` не менялся (diff
  против React `:317-399` — идентичен); закрытый кредит не попадает ни в `budgetAmounts`, ни в
  стратегию, ни в Ритуал; `creditBalance` на геттере не зовётся (grep).
- Тема: `lib/theme.ts` без обращения к `window` на уровне модуля (SSR-тесты в Node);
  `isDark` реактивен; `Ring` без пропа `dark`.
- Мастер: `setupPlan` — единственное место решения «что сохранять»; `joining` по живым.
- Тесты по секциям «Тесты» ТЗ — добить; SSR-тест `StrategyCompare` с числами =
  `simulateStrategy`.
- Живые доки: только если тронуты секции `frontend/README.md` (тема/PWA) — коротко.
- **Промпт Блока 2 актуализировать** по факту: имена `openCredits`/`costliestCredits`,
  `StrategyCompare` (пропсы — PV-15 добавит кнопку плана), `lib/theme.ts`, что клинап RP
  сделал с `monthDues`/`plural`/`lumpPlan` (номера строк `Capital.vue` для PV-09…PV-13).
- Следующий шаг — `/accept паритет-react-vue 1`.

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh

```
/accept паритет-react-vue 1
```

Блок-специфика:

- Критерии приёмки всех ТЗ PV-01…PV-08 на локальном стенде §6, два профиля + viewer:
  калькулятор — семь элементов, цифры = `simulateStrategy` до тенге (независимый прогон
  функции на тех же входах), закрытый кредит не влияет; ставка «по сроку» — предупреждение с
  числами и сохранение как рассрочка; минус у цели → 0 у обоих; вклад 10,2% + плашка; мастер
  «Пропустить» не сохраняет; PWA в `npm run preview` перезагружается сама; тема на старте и
  «Авто» живьём, кольца тёмные.
- Регрессия: весь `frontend/e2e/` + `npm test`; эталоны Блоков 0/1 RP (478 011, 324 990,
  675 010, 154 457, …) не изменились.
- Сценарии приёмки — `frontend/e2e/pv-block1-calculator.test.ts`.
- **CI:** push ветки `pv-block-1-calculator` (разрешён), `ci-frontend` зелёный на HEAD.
- **Ночью НЕ деплоить и 🏁 не ставить** (§5): вердикт в «Приёмка», блок в §4 — «✅ принят»,
  своя ✅ в трекер. Следующий шаг — не `/worker паритет-react-vue 2`, а **утренний деплой**
  (промпт ниже) — владелец запускает сам.

---

## Промпт утреннего деплоя — уровень build · Opus 5 · effort high

Роль приёмки, часть «деплой» (BACKLOG-GUIDE «Приёмка»: принято → деплой + смоук). Slash-команды
для этого шага нет — промпт целиком:

```
Ты — сессия утреннего деплоя (memory/backlog/паритет-react-vue/00-backlog.md §5 «Ночной
прогон», п.4). Предусловия: Блок 1 «развитие-приложения» — клинап ✅ (без деплоя); Блок 1
«паритет-react-vue» — приёмка ✅ принят (без деплоя); CI зелёный на HEAD обеих веток.
Владелец в сессии и говорит «да» на каждый merge.
1. Записать ID текущего Production-деплоя Vercel в этот SESSION.md (откат = promote).
2. Деплой Блока 1 RP по промпту клинапа ../развитие-приложения/block-1-payments/SESSION.md:
   merge rp-block-1-payments → main, push; перезапуск PWA на обоих телефонах до первой
   отметки; смоук на family-finance-ff.vercel.app (остатки те же, одна настоящая отметка
   видна на втором телефоне). 🏁 Блока 1 RP в его §4.
3. Деплой Блока 1 PV: merge pv-block-1-calculator → main, push; смоук: калькулятор «копить
   или гасить» на живых данных (семь элементов, цифры разумные), тема применилась на старте,
   PWA обновилась сама на обоих телефонах (PV-07). Откат — promote записанного деплоя.
4. «Приёмка» этого файла дополнить деплоем и смоуком, 🏁 Блока 1 в §4, ✅ «утренний деплой» в
   трекер, строка в memory/STATE.md. Следующий шаг — /worker паритет-react-vue 2 ultrathink.
```

---

## Handoff (заполняет исполнитель)

- **Коммиты:**
- **Что сделано:**
- **Отклонения от ТЗ:**
- **Грабли среды:**
- **Верификация:**

## Итоги критика

## Приёмка

## Утренний деплой (владелец + агент)
