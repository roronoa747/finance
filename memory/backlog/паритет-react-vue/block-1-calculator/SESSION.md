# Блок 1 — Калькулятор и точные расчёты · сессии

**Цель блока:** вернуть семье калькулятор «копить или гасить» таким, каким он был в React
(горизонт, цели-страховки, подушка, накопленное, вывод в обе стороны), и убрать неверные
цифры: закрытый кредит в плане, ставка «по сроку» под 0%, минус у цели, инфляция 8%, прогноз
цели; плюс мастер, PWA и тема на старте — без чего проверка остального в браузере неудобна.

Сессии: исполнитель ✅ · критик ⬜ · приёмка ⬜ · утренний деплой ⬜

**Ночной прогон (решение владельца 2026-09-24, §5):** три роли идут подряд субагентами, без
вопросов владельцу; приёмка **не деплоит**; деплой и 🏁 — утром с владельцем. Прод (Supabase,
Vercel) не трогать; `main` не трогать; push ветки `pv-block-1-calculator` ради CI — можно.

---

## Промпт исполнителя — уровень build · Opus 5 · effort high

```
/worker паритет-react-vue 1
```

Блок-специфика:

- **Ветка `pv-block-1-calculator` от `rp-block-1-payments`** (не от `main`) — после того, как
  клинап Блока 1 RP закоммитил свои правки (проверить: в
  `../развитие-приложения/block-1-payments/SESSION.md` заполнены «Итоги пост-приёмки», в
  трекере «клинап ✅»; последний коммит ветки — клинапа). Push ветки — можно (CI);
  `main` не трогать; прод не трогать.
- Прочитать «Итоги пост-приёмки» Блока 1 RP: клинап мог завести `monthDues` (Н-3),
  `plural` (Н-8), поправить `lumpPlan` (Н-1), `prepaySaved` (Н-4) — ТЗ ссылаются на код до
  клинапа, номера строк в `Capital.vue`/`finance.ts` могли сдвинуться.
- Задачи по порядку: `PV-01` → `PV-02` → `PV-03` → `PV-04` → `PV-05` → `PV-06` → `PV-07` →
  `PV-08`. PV-01 — фундамент калькулятора (закрытые кредиты вне стратегии); PV-05 ждёт
  `INFLATION` из PV-04.
- **Эталон — React `src/`** (Р-2): тексты дословно по строкам из ТЗ, `src/` не менять.
  Все числа — `lib/finance.ts`, компоненты только показывают; деньги целые.
- Ловушка производных остатков: `financeStore.credits` уже вычел отметки — `creditBalance` на
  нём не звать (§6).
- Среда: локальный стенд (§6) — Go на моках + Vite dev, два профиля, 390px; PWA (PV-07) — в
  `npm run preview`. Тема (PV-08) — проверить и на `/access` после выхода.
- Верификация: `cd frontend && npm run build && npm test` (весь `e2e/` внутри) + браузер по
  критериям каждого ТЗ. Handoff — с эталонными числами калькулятора (для приёмки).
- Следующий шаг — `/critic паритет-react-vue 1`.

---

## Промпт критика — уровень review · Opus 5 · effort xhigh

```
/critic паритет-react-vue 1
```

Блок-специфика:

- Паритет с React по файл:строка из ТЗ: пройти пункты 1–7 `StrategyCompare` и тексты PV-03,
  PV-04, PV-05 дословно; расхождение текста — правка, не хвост.
- Деньги: `strategyInputs` и `strategyGain` — целые; `simulateStrategy` не менялся (diff
  против React `:317-399` — идентичен); закрытый кредит не попадает ни в `budgetAmounts`, ни в
  стратегию, ни в Ритуал; `creditBalance` на геттере не зовётся (grep).
- Тема: `lib/theme.ts` без обращения к `window` на уровне модуля (SSR-тесты в Node);
  `isDark` реактивен; `Ring` без пропа `dark`.
- Мастер: `setupPlan` — единственное место решения «что сохранять»; `joining` по живым.
- Тесты по секциям «Тесты» ТЗ — добить; SSR-тест `StrategyCompare` с числами =
  `simulateStrategy`.
- Живые доки: только если тронуты секции `frontend/README.md` (тема/PWA) — коротко.
- **Промпт Блока 2 актуализировать** по факту: имена `openCredits`/`costliestCredits`,
  `StrategyCompare` (пропсы — PV-15 добавит кнопку плана), `lib/theme.ts`, что клинап RP
  сделал с `monthDues`/`plural`/`lumpPlan` (номера строк `Capital.vue` для PV-09…PV-13).
- Следующий шаг — `/accept паритет-react-vue 1`.

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh

```
/accept паритет-react-vue 1
```

Блок-специфика:

- Критерии приёмки всех ТЗ PV-01…PV-08 на локальном стенде §6, два профиля + viewer:
  калькулятор — семь элементов, цифры = `simulateStrategy` до тенге (независимый прогон
  функции на тех же входах), закрытый кредит не влияет; ставка «по сроку» — предупреждение с
  числами и сохранение как рассрочка; минус у цели → 0 у обоих; вклад 10,2% + плашка; мастер
  «Пропустить» не сохраняет; PWA в `npm run preview` перезагружается сама; тема на старте и
  «Авто» живьём, кольца тёмные.
- Регрессия: весь `frontend/e2e/` + `npm test`; эталоны Блоков 0/1 RP (478 011, 324 990,
  675 010, 154 457, …) не изменились.
- Сценарии приёмки — `frontend/e2e/pv-block1-calculator.test.ts`.
- **CI:** push ветки `pv-block-1-calculator` (разрешён), `ci-frontend` зелёный на HEAD.
- **Ночью НЕ деплоить и 🏁 не ставить** (§5): вердикт в «Приёмка», блок в §4 — «✅ принят»,
  своя ✅ в трекер. Следующий шаг — не `/worker паритет-react-vue 2`, а **утренний деплой**
  (промпт ниже) — владелец запускает сам.

---

## Промпт утреннего деплоя — уровень build · Opus 5 · effort high

Роль приёмки, часть «деплой» (BACKLOG-GUIDE «Приёмка»: принято → деплой + смоук). Slash-команды
для этого шага нет — промпт целиком:

```
Ты — сессия утреннего деплоя (memory/backlog/паритет-react-vue/00-backlog.md §5 «Ночной
прогон», п.4). Предусловия: Блок 1 «развитие-приложения» — клинап ✅ (без деплоя); Блок 1
«паритет-react-vue» — приёмка ✅ принят (без деплоя); CI зелёный на HEAD обеих веток.
Владелец в сессии и говорит «да» на каждый merge.
1. Записать ID текущего Production-деплоя Vercel в этот SESSION.md (откат = promote).
2. Деплой Блока 1 RP по промпту клинапа ../развитие-приложения/block-1-payments/SESSION.md:
   merge rp-block-1-payments → main, push; перезапуск PWA на обоих телефонах до первой
   отметки; смоук на family-finance-ff.vercel.app (остатки те же, одна настоящая отметка
   видна на втором телефоне). 🏁 Блока 1 RP в его §4.
3. Деплой Блока 1 PV: merge pv-block-1-calculator → main, push; смоук: калькулятор «копить
   или гасить» на живых данных (семь элементов, цифры разумные), тема применилась на старте,
   PWA обновилась сама на обоих телефонах (PV-07). Откат — promote записанного деплоя.
4. «Приёмка» этого файла дополнить деплоем и смоуком, 🏁 Блока 1 в §4, ✅ «утренний деплой» в
   трекер, строка в memory/STATE.md. Следующий шаг — /worker паритет-react-vue 2 ultrathink.
```

---

## Handoff (заполняет исполнитель)

Исполнитель 2026-09-25, ночной прогон (Opus 5.5). Репо `finance`, ветка `pv-block-1-calculator` от
`bc03949` (клинап Блока 1 RP ✅), запушена ради CI; `main` и прод не тронуты. Бэкенд и React `src/` не
тронуты (`git diff bc03949..HEAD -- backend api go.mod src` пусто).

- **Коммиты:** `062ed04` PV-01 · `c580fad` PV-02 · `95960ea` PV-03 · `0aaf92d` PV-04 · `1c10f5b` PV-05 ·
  `ac5bf4e` PV-06 · `d8bffc4` PV-07 · `acf15a9` PV-08 (+ коммит доков).
- **Что сделано:**
  - **PV-01** — `openCredits` (живые с остатком > 0, производные кредиты) и `costliestCredits` (открытые с
    процентами, дороже — первым; при равной ставке — больше процентов в месяц, правило `rankedDebts`) в
    `finance.ts`. `budgetAmounts`: «Кредиты» — по `openCredits`; Бюджет и Обзор передают
    `{ ...householdDoc, credits: financeStore.credits }`. Капитал: `rankedDebts` — через
    `costliestCredits`. Ритуал: `credit = costliestCredits(credits)[0]`.
  - **PV-02** — `components/StrategyCompare.vue` (Р-21): пункты 1–7 React дословно, колонки подсвечены по
    знаку выигрыша, нативные чекбоксы `accent-[var(--brand)]`, проп `initial`. Числа — `strategyInputs`
    (формулы React `:1568-1582`, всё целое; закрытые/удалённые отсекает сам) и `strategyGain`;
    `simulateStrategy` не менялся (тело совпадает с React `:317-399` построчно — diff пуст). Капитал: `strat*`
    удалены, вкладка «Копить или гасить» — `<StrategyCompare :credits="openCredits(credits)" …/>`.
  - **PV-03** — `scheduleMismatch` (`paid/gap/suggest`, `null` при сходящемся графике или входах ≤ 0) и
    `installmentMonths` (платежей без процентов; `Math.ceil` ушёл из компонента) в `finance.ts`. Капитал:
    «Без них / Знаю ставку / Знаю срок» на `Segmented` в `Field`, текст «Рассрочка: платите ровно столько…»,
    «Сколько платежей осталось», плашка «Ставка получается N годовых», предупреждение с числами + «Записать
    всё равно можно…»; запись не блокируется (0%, «рассрочка»). Мастер: «Знаю ставку», «Ставка (ГЭСВ из
    договора), % годовых», «Сколько платежей осталось» (placeholder 17), плашка ставки / предупреждение
    React `:382-385`, подсказка про ГЭСВ.
  - **PV-04** — `goalHave(seed, movements) = max(0, seed + Σ)` — одна формула для `contribute` и `mergeGoal`
    (`sumMovements` в `merge.ts` удалён); `INFLATION = 0.102` (Р-19) и `indexedNeed` (`null` при
    `months = Infinity`). Экран цели: «Цель дорожает вместе с рынком» вместо «Дисциплины накоплений»; при
    взносе 0 плашки нет.
  - **PV-05** — `Deposit.vue` на `INFLATION`; первая плашка дословно React («…с доходом.»), вторая — «Проценты
    считает приложение, а не банк».
  - **PV-06** — `lib/setup.ts`: `setupPlan(form, skips)` — единственное место решения «что сохранять»;
    `setupCreditRate` и `setupGoalMonthly` — те же числа для экрана и записи. `Setup.vue`: флаги пропуска
    (Р-22), `finish()` применяет план; `joining` — по `liveObligations` / `liveGoals`, решение фиксируется
    до первой записи.
  - **PV-07** — `lib/pwa.ts` `watchServiceWorkerUpdates({ sw, doc, win })` (перенос React `main.tsx`),
    вызов в `main.ts` после `mount`; `pwa-build` проверяет `controllerchange` / `visibilitychange` /
    `getRegistration` в собранном JS.
  - **PV-08** — `lib/theme.ts`: `readThemeChoice` / `readAccent` (try/catch, мусор → дефолт),
    `setThemeChoice` / `setAccent`, `applyCurrentPalette`, реактивный `isDark` (false в Node),
    `watchSystemTheme` (под «Авто»); на уровне модуля ничего не выполняется. `main.ts` — тема до `mount`.
    `AppearancePanel` — через `lib/theme`. `Ring` без пропа `dark` (дуга по `isDark`); «Ритм цели» —
    `rhythmColor` по `isDark`.
- **Отклонения от ТЗ** (минимальные, правило 2):
  - PV-02: `strategyInputs` отдаёт сверх списка ТЗ `cushionSize` и `spare` — React показывает «Сначала
    подушка — round(mandatory/1000)·1000» и «Вложить уже накопленное — max(0, movable − buffer)» и при
    снятых галках, а `buffer`/`lump` тогда 0; без этих полей компонент считал бы сам. `mandatory` —
    `Math.round` месяца (годовые дают дробь), `cushionSize` — от сырой суммы, как React.
  - PV-02/PV-03: пропы Капитала `initialAdvice` (стартовая вкладка) и `initialDebt` (поля формы долга) — только
    для SSR-тестов, по образцу `Budget.initialView`: без них вкладку и режим «Знаю срок» в Node не открыть.
  - PV-03: «N платежей» — через `plural` (как уже во Vue после Н-8 клинапа RP), а не React
    «платеж{ей|}» («1 платеж», «2 платежей»); для 12 и 100 текст совпадает дословно. Ветка React «Выходит
    больше остатка…» перенесена, но недостижима: `rateFromSchedule` даёт `null` только при
    `months × payment < principal`, т.е. `gap > 0` всегда.
  - PV-06: флаги — один объект `skips` + `go(step, skip)` вместо трёх `ref`; `setupPlan` принимает строки
    полей (разбор внутри). Поведение Vue, отличное от React, сохранено как было в `finish()` (ТЗ его не
    трогает): d1 пишется только при сумме > 0 (React — всегда), кредит — только при платеже > 0.
    `joining` по `liveObligations` не считает группы подписок (React считал любое неудалённое) — по ТЗ.
  - PV-07: у `update()` добавлен `.catch(() => {})` — офлайн-проверка в React давала необработанный reject.
- **Грабли среды:**
  - Кнопки `Segmented` внутри `Field` (`<label>`) получают имя подписи («Горизонт Год Два Три») —
    `getByRole('button', { name: 'Год' })` их не находит, искать по тексту `button:has-text(/^Год$/)`; Chrome
    ещё и вешает `:hover` на первую кнопку группы. Хвост уже есть (PV-09), не чинил.
  - Полностраничные снимки headless Chrome ловят середину `transition-colors`: подсветка сегмента выглядит
    на прежней кнопке. `aria-pressed` и снимок элемента — верные; для скриншотов ждать 300 мс.
  - PWA: вкладка первой установки открыта без контроллера (`hadController = false`) и не перезагружается
    никогда — так и в React. Проверка: установка → повторный запуск → новая сборка → фокус.
    `vite preview` отдаёт пересобранный `dist` без рестарта.
  - `structuredClone` на реактивном документе Pinia — `DataCloneError`; в тестах клон через JSON.
  - Второе устройство на стенде — тот же пользователь во втором контексте: вход партнёра по коду упирается в
    хвост «новые семьи» (А-10); для критериев Блока 1 этого достаточно.
- **Верификация:**
  - `cd frontend && npm run build && npm test` — ✅, **31 файл / 290 тестов** (было 26 / 239; новые файлы:
    `StrategyCompare.test.ts`, `lib/setup.test.ts`, `lib/pwa.test.ts`, `lib/theme.test.ts`,
    `Ring.test.ts`). Весь `e2e/` (9 файлов) — внутри; ожидания эталонов Блоков 0/1 RP не менялись (в `e2e/`
    только новая проверка `pwa-build`). Мутационные контроли: откат правок PV-01 в Бюджете/Обзоре/Ритуале →
    4 красных; `contribute` без `goalHave` → красный тест PV-04.
  - **CI:** `ci-frontend` ✅ на `acf15a9` (run 36069828581).
  - **Браузер** — стенд §6 (Go на моках + Vite dev; PWA — `vite preview`), Chrome headless через
    `playwright-core`, 390px, Asia/Almaty, 25 сентября; числа — своими формулами (перенос React, не код
    Vue): **67/67**, консоль чистая, стенд погашен (порты 5173/8080/4173 свободны).
    - Семья: доход 900 000 + 600 000, аренда 220 000 (с 2027-06 — 200 000), «Еда и быт» 200 000; Кредитка
      300 000 / 40% / 30 000, Банк 1 000 000 / 18% / 91 680, Рассрочка 200 000 / 0% / 20 000; цели
      Квартира 6 000 000 (есть 800 000, 150 000/мес), Декрет 2 000 000 (есть 300 000, 50 000/мес).
    - PV-01: «Свободно» 738 320 → после досрочки 300 000 по Кредитке (UI, «Разово») **768 320** на Обзоре и
      в Бюджете; «Кредиты» 141 680 → 111 680; Ритуал: «Сейчас: 13 платежей, переплата 70 967 ₸» (Кредитка)
      → «Сейчас: 12 платежей, переплата 100 160 ₸» (Банк); второе устройство после синка — 768 320.
    - PV-02 (эталон для приёмки): по умолчанию (Три, подушка 362 000 = 220 000 + 141 680 до тысяч) — «Копим» 11 729 239 / долг 0 /
      проценты 171 241 / через 13 мес., «Сначала долги» 11 811 831 / 0 / 88 649 / через 6 мес.,
      **выгоднее на 82 593**; «Год» — 82 235; + «Декрет» не останавливать — 66 958; без подушки — 103 318;
      + вложить накопленное — 159 847; все цели отмечены — текст React; после закрытия Кредитки (Три,
      подушка) — 48 987. Все колонки и вывод = `simulateStrategy` React до тенге; 390px без прокрутки в
      обеих темах; подсвеченная колонка в тёмной — токен `--brand-soft`.
    - PV-03: Капитал 1 000 000 / 10 000 / 12 → «12 платежей по 10 000 — это 120 000 ₸, а остаток вы указали
      1 000 000 ₸. Не хватает 880 000 ₸: похоже, платежей 100, а не 12.» + «Записать всё равно можно…» →
      сохранён 0% «рассрочка»; 91 680 → «18,0% годовых» → сохранён 18,0%. Мастер: те же входы —
      предупреждение React и плашка 18,0%, «Дальше» активна.
    - PV-04: «Декрет» снять 500 000 из 300 000 → «0 из 2 000 000 ₸» у обоих устройств, на сервере движение
      −500 000 целиком; прогноз «Декрета» 2 764 619 ₸ (40 мес.), «Квартиры» 7 964 911 ₸ (35 мес.).
    - PV-05: вклад 14% / 12 мес. → «При инфляции 10,2% эффективная ставка 14,9% оставляет примерно 4,3%
      настоящих.» (при 8% было бы 6,4%) + вторая плашка.
    - PV-06: жильё 220 000 + «Пропустить», цель + «Пока без цели» → обязательств и целей нет, d1 не записан;
      «Пропустить» → «Назад» → «Дальше» — аренда и d1 = 220 000 сохранены; «Кредитов нет» — кредита нет.
    - PV-07: первая установка — без перезагрузки; повторный запуск; новая сборка + фокус → перезагрузка
      ровно одна, на экране новая версия.
    - PV-08: «Тёмная» — класс `dark` уже к DOMContentLoaded на `/`, `/setup`, `/access` (после выхода и
      после перезагрузки); кольца Обзора, Целей, экрана цели и «Ритм цели» — `HUES[…].dark`; акцент
      «Кобальт» на старте; «Авто» + `prefers-color-scheme` живьём в обе стороны (класс и кольца); панель
      «Оформление» по-прежнему меняет тему.
- **Для критика:** «День списания» и «Добавить долг» в форме долга Капитала остались Vue (React — «День
  платежа», «Добавить»; в ТЗ PV-03 не перечислены, форму пересобирает PV-10 на ките) — решить: правка или
  PV-10. Хвостов в §4 не добавлял.
- **Следующий шаг:** `/critic паритет-react-vue 1`.

## Итоги критика

## Приёмка

## Утренний деплой (владелец + агент)
