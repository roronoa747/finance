# Блок 3 — Выбранный план «Сначала долги» · сессии

**Цель блока:** калькулятор перестаёт быть картинкой — выбранный план хранится в общем
документе, ставит цели на паузу с объяснением, каждый месяц называет одну досрочку в самый
дорогой долг и вносит её одним нажатием, а в конце уходит в историю с итогом «сэкономили».

Сессии: исполнитель ✅ · критик ✅ · приёмка ✅ · ревью frontend ✅ · клинап ⬜
<!-- L-блок. Каталог ревью — по факту коммитов Handoff-а; план — только frontend (Р-15). -->

---

## Промпт исполнителя — уровень build · Opus 5 · effort high (ultrathink)

```
/worker паритет-react-vue 3 ultrathink
```

Блок-специфика:

- Ветка `pv-block-3-debt-plan` от `main` (после деплоя Блока 2). Push — с согласия владельца;
  `main` не трогать.
- **До PV-14 — хвосты ревью frontend Блока 2** (судьбы — владелец, 2026-09-25; рецепты — §4 и
  `../block-2-money-edit/REVIEW-frontend.md`), по коммиту на хвост, сообщение `PV-14…PV-17 (хвосты
  Б2): Н-N — …`, поведение экранов не меняется:
  1. **Н-2** (первая задача) — DOM-тесты кита: dev-зависимость `happy-dom`,
     `// @vitest-environment happy-dom` только в новом `kit/Sheet.dom.test.ts` (два листа — Escape
     верхнему; Tab/Shift+Tab по кругу; `pointerdown` на карточке + `click` на фоне — открыто; фокус
     назад; `Row` — `click` `detail: 0` после `pointermove` > 8 px). Остальные тесты — в Node.
  2. **Н-3** — разрез `Capital.vue` на окна `components/capital/` (`CreditSheet`, `ObligationSheet`,
     `PayoffSheet`, `AccountSheet`; пропсы — id записи, emit `close`; очистка адреса Б-15 и
     `QUERY_KEYS` остаются в Капитале). До PV-16. Все SSR-тесты и e2e Капитала — зелёные без правки
     ожиданий (переезд, не переделка).
  3. **Н-6 + Н-7** — `frontend/tsconfig.e2e.json` в `references` (`vue-tsc -b` и CI проверяют e2e),
     20 ошибок фикстур — по реальным типам; `e2e/support/family.ts` (`fakeServer`, `phone`,
     `screen`, `at`), три файла на него. До первого сценария `pv-block3-*`.
- Задачи по порядку: `PV-14` (модель, математика, стор, слияние — без UI) → `PV-15` → `PV-16`
  → `PV-17`. К UI не переходить, пока тесты PV-14 не зелёные.
- **Деньги и документ (L):** `plans` — новый список верхнего уровня: тип → `defaultSyncDoc()`
  → `known` в `mergeDocs` (§6); пауза цели не пишется в цель (Р-9); досрочка по плану — обычная
  запись `prepay` с `planId` через `applyPrepayment` (Р-10, счёт по Р-5 RP); все суммы —
  `finance.ts`; закрытые кредиты — `openCredits` (PV-01); `creditBalance` на геттере не звать.
- Р-11: `lumpPlan` при платеже ≤ процентов — `openEnded`, не `null`.
- Р-16: месяц шага и пропуск — `monthKey()` по Алматы; тесты на фиксированном времени.
- **Что уже в коде после Блока 2** (факт, критик Блока 2 2026-09-25; подробно — §6 бэклога):
  - Разбивка и график (PV-13, `lib/finance.ts`): `paymentSplit(record | null, credit, due)`,
    `creditTotals(payments, creditId)`, `budgetInterest(credits)`, `creditSchedule(credit,
    payments, { from?, extra? })` → `ScheduleRow[]`. `extra: { period, amount }[]` — будущие
    досрочки: гасят тело до платежа своего месяца (PV-17 передаёт шаги плана); уже применённые
    досрочки месяца видны в `extra` строки и второй раз не вычитаются. В модалке кредита —
    свёрнутый «График платежей» и «За всё время».
  - Окна: `kit/Sheet.vue` (`open`, `title`, `z?`, emit `close`; слоты тела, `mark`, `footer`;
    Escape — только верхний, Tab не выходит из окна, фокус назад). Окно выбора плана — `Sheet` с
    содержимым под `v-if`. Новое окно Капитала по адресу — в `QUERY_KEYS` и `queryModalOpen`
    (Б-15, «окно показано»). Группы кнопок — `Field group`; `Select`; `useSavedMark`; `Row` со
    слотом `action` вне кнопки строки (кнопка «Внести по плану» PV-16 — туда).
  - `StrategyCompare` Блоком 2 не менялся (кроме `Field group` у «Горизонт»): пропсы `credits`
    (открытые), `goals`, `obligations`, `monthKey`, `initial?: { months, kept, cushion,
    useSaved }` (для SSR-тестов).
  - `applyPrepayment(creditId, by, { amount, mode: 'term' | 'payment', accountId? })` →
    `Payment | null` (`stores/finance.ts:997-1031`) Блоком 2 не менялся; `planId?` добавит PV-14.
    `lumpPlan` при платеже ≤ процентов — всё ещё `null` (Р-11 → PV-14). Калькулятор закрытого
    кредита пишет «долг закрыт».
  - Номера строк в ТЗ PV-14…PV-16 пересчитаны критиком Блока 2 на `7f8772f`; **клинап Блока 2
    сдвинул `Capital.vue`** (2 048 → 2 040 строк), разрез Н-3 сдвинет ещё — искать по именам, не
    по номерам.
  - Хвост Блока 2 → **PV-15 п. 7** (владелец): строки «Куда уходит» и сегменты Обзора — по
    ключам `d1`–`d4`, не по `categories`.
  - **Клинап Блока 2 (факт, `ef45ee8..b35d82a`):** `creditOutlook` + `monthlyInterest`,
    `sharePct`; `prepayOutcome(c, extra, 'monthly' | 'once')` → `… | null`, `payoffChips`,
    `payoffLadder` (`finance.ts`) — в Капитале нет `Math.` над деньгами, строка кредита: «долг
    закрыт» / «долг не закрывается» / «N платежей» (шаг плана PV-16 встаёт рядом — не вернуть
    `annuityMonths` в шаблон). Раздел в модалке обязательства; `plannedChange(current, planned,
    every?)`; в Капитале `payAccounts` (= `payableAccounts`) — один список на доход и досрочку,
    шаг плана PV-16 берёт его же; `applyMode` сбрасывается при смене кредита. `SavedMark` — текст
    через `v-if`, слот `mark` у `Sheet` рядом с `<h3>`.
- Среда: стенд §6, два профиля + viewer; смена месяца — `vi.setSystemTime` в тестах, в
  браузере — фикстура документа с датой старта плана в прошлом месяце.
- Верификация: `cd frontend && npm run build && npm test` + браузер по критериям ТЗ.
- Следующий шаг — `/critic паритет-react-vue 3 ultracode`.

---

## Промпт критика — уровень review · Opus 5 · effort xhigh (ultracode)

```
/critic паритет-react-vue 3 ultracode
```

Блок-специфика:

- Слияние: два активных плана офлайн → поздний; надгробие плана; старый клиент без `plans`
  не теряет ключ (RP-02/03); `resetDoc` шлёт `plans: []`.
- Деньги: шаг = Σ пауз + освободившиеся платежи; `applyPlanStep` не удваивает в месяце;
  `settlePlan` закрывает план ровно при отсутствии открытых процентных долгов; «Свободно» не
  меняется от выбора плана; `openEnded` не даёт `NaN`/`Infinity` на экране.
- Тексты паузы и пропуска — без упрёка (Р-4, тон RP Р-29).
- **Р-18: переписать «Что уже есть» в ТЗ RP-23** (`../развитие-приложения/block-5-goal-parts-otbasy/RP-23-otbasy-ui.md`):
  калькулятор и план — по факту кода (`StrategyCompare`, `keep` = «Что не останавливать»,
  `DebtPlan`, `planStep`), Отбасы встраивается в план (пополнения жилищного вклада — цель
  «не останавливать» по умолчанию). Коммит в этой сессии.
- Промпт Блока 4 актуализировать: `Sheet`/`useSavedMark` после блоков 2–3, `wishlist` в
  сторе (есть ли методы), `updateGoal` после PV-14.
- Следующий шаг — `/accept паритет-react-vue 3 ultracode`.

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh (ultracode)

```
/accept паритет-react-vue 3 ultracode
```

Блок-специфика:

- Критерий брифа на двух профилях: выбор плана → партнёр видит паузу целей → досрочка по
  плану одним нажатием → факт обновился у обоих → отмена возвращает цели; закрытие последнего
  долга → план в истории с итогом. Тест слияния списка планов есть и зелёный (критерий брифа).
- Регрессия: весь `frontend/e2e/` + `npm test`; эталоны Блоков 0–2; CI зелёный на HEAD.
- Сценарии — `frontend/e2e/pv-block3-debt-plan.test.ts`.
- **После критика (факт, 2026-09-25):** пустой план (кроме подушки, всё «не останавливать»)
  выбрать нельзя — кнопка неактивна с подсказкой; шаг месяца один на семью (отмена и повторный
  выбор в том же месяце шаг не повторяют); «Снять» в окне досрочки плана возвращает шаг;
  бессрочный долг — «экономию не считаем» на экране плана. Скрипт критика —
  `critic-check.mjs` (29 проверок, рядом `stand.mjs`/`seed.mjs`) в
  `C:/Users/SW/AppData/Local/Temp/claude/c--Users-SW-finance/560f45f6-c87d-498f-9ed3-5a4c6a6f60d6/scratchpad/`,
  в репо не кладётся.
- **Хвосты §4 критика — показать владельцу до вердикта** (судьбы не назначены): «Вложить
  накопленное» учитывается дважды (важно до деплоя), шаг обрезается остатком долга, подушка
  в том же месяце, калькулятор против снимка прогноза, d4 в Бюджете, `pauseShift` для новых целей.
- **L-блок: приёмка НЕ деплоит.** Следующий шаг — `/dir-review паритет-react-vue 3 frontend`.

---

## Промпт ревью frontend — уровень review · Opus 5 · effort xhigh

```
/dir-review паритет-react-vue 3 frontend
```

Блок-специфика:

- Каталог `frontend/`: план рядом с `payments`/`goals` — единообразие слияния; дубли между
  `StrategyCompare`, `DebtPlan`, `Ritual` (расчёт шага — одно место?); выбор счёта (третье
  место — Н-11/RP-10); `budgetAmounts` с планом против Бюджета/Обзора.
- **Уже назначено в клинап владельцем (приёмка, §4)** — ревью не дублирует находками, но может
  дать рецепт/место: «вложить накопленное» дважды (а); остаток месяца — вторым шагом; под кнопкой
  выбора — черновик плана с `payments` (шаг, «вложить», прогноз); гигантские проценты
  калькулятора при незакрываемом долге; сброс прокрутки при смене экрана; таблица «План и факт»
  в фазе подушки.
- Следующий шаг — `/cleanup паритет-react-vue 3`.

---

## Промпт клинапа — уровень build · Opus 5 · effort high

```
/cleanup паритет-react-vue 3
```

Блок-специфика:

- Выполнить «клинап»-вердикты `REVIEW-frontend.md` (8; там же рецепты к хвостам ниже). **Н-2
  ревью — первым**, до п. 2 хвостов: шаг месяца и «шаг ждёт оплаты» — в одном месте, тогда второй
  шаг правится один раз. Хвосты ревью Н-8, Н-9, Н-10 — в §4 (судьбу назначает владелец).
- **Хвосты §4 с судьбой «Блок 3 — клинап»** (владелец, приёмка 2026-09-25) — все до деплоя, по
  коммиту на хвост, суммы — только `finance.ts`, тест на каждый; e2e приёмки
  (`pv-block3-debt-plan.test.ts`, describe «Приёмка Блока 3») не должен краснеть — он не фиксирует
  поведение этих хвостов (везде `lump` 0, шаг не упирается в остаток):
  1. **«Вложить накопленное» учитывается дважды** — вариант (а): при внесении шага месяца старта
     `lump` снимается с целей на паузе (пропорционально `have`, движение с пометкой плана, счёт
     цели не сдвигается), со счёта списывается шаг один раз. После отмены калькулятор не
     предлагает вложить уже вложенное.
  2. **Остаток суммы месяца — вторым шагом** в следующий по ставке долг в том же месяце (сумма
     месяца одна, Р-4). Учесть факт приёмки: в месяц закрытия долга шагом «Досрочно по плану»
     уже растёт на его полный `payment`, а «Кредиты» теряют последний платёж — «Свободно» −4 333.
  3. **Под кнопкой «Выбрать этот план» — то, что запишется**: прогноз, шаг и «вложить» — по
     черновику плана (`planForecast(draft)`, `planStep` с `payments`); после отмены в том же
     месяце не обещать уже внесённый шаг; подпись «Вложить уже накопленное — N» = `lump` плана
     (с учётом радио подушки). Калькулятор React не трогается.
  4. **Калькулятор при незакрываемом долге** (платёж ≤ процентов): вместо «процентов банку
     1 034 211 678 603 997 400 ₸» — «долг не закрывается — экономию не считаем» (Р-11).
  5. **Сброс прокрутки наверх при смене экрана** (`AppShell`, `<main overflow-y-auto>`).
  6. **Таблица «План и факт» в фазе подушки** — без «плана 100 000 / факт —» за месяцы подушки и
     «0 ₸» в текущем (Р-4: без упрёка).
- Браузерные скрипты приёмки (в репо не кладутся) — повторить после правок:
  `C:/Users/SW/AppData/Local/Temp/claude/c--Users-SW-finance/0765ca20-3fb4-4c74-884c-6e24bcf46ca2/scratchpad/accept/`
  (`brief-chain/chain.mjs`, `plan-step/accept-pv16.mjs`, `plan-screen/s*.mjs`,
  `viewer-regress/{viewer,old-doc,regress}.mjs`; стенд — `stand.mjs` в каждой папке).
- **Деплой** (с «да» владельца): ID текущего Production-деплоя, merge `pv-block-3-debt-plan` →
  `main`, push; смоук: документ прода без `plans` открылся, «Свободно» и остатки прежние,
  калькулятор предлагает выбрать план (не выбирать без владельца). **Откат:** promote.
- «Итоги пост-приёмки», 🏁. Следующий шаг — `/worker паритет-react-vue 4`.

---

## Handoff (заполняет исполнитель)

- **Коммиты** (ветка `pv-block-3-debt-plan` от `main` `3fb2436`; не запушена — push с согласия
  владельца, CI не запускался):
  `5010c58` Н-2 · `73e827f` Н-3 · `93c1531` Н-6 + Н-7 · `3b03c6d` PV-14 · `b55d079` PV-15 ·
  `a332f1b` PV-16 · `65a346e` PV-17 · + коммит документов.
- **Что сделано:**
  - **Н-2** — `kit/Sheet.dom.test.ts` (dev-зависимость `happy-dom`, окружение только в этом файле):
    Escape верхнему из двух листов, Tab/Shift+Tab по кругу, фон закрывает только начатое на нём
    нажатие, фокус в окно и назад, `Row` — `detail 0` после свайпа. Мутации 5/5 ловятся.
  - **Н-3** — окна Капитала в `components/capital/` (`AccountSheet`, `CreditSheet`,
    `ObligationSheet`, `PayoffSheet`; пропс — id, emit `close`, у кредита ещё `payoff`). Очистка
    адреса Б-15 и `QUERY_KEYS` — в Капитале, «окно показано» — по id среди живых записей.
    `Capital.vue` 2 040 → 1 140 строк. `categoryName` (`palette.ts`). Ожидания тестов прежние.
  - **Н-6 + Н-7** — `tsconfig.e2e.json` в `references` (`vue-tsc -b` и CI проверяют e2e), 20 ошибок
    по реальным типам; `e2e/support/family.ts` (`fakeServer`, `backend`, `phone`, `screen`, `at`,
    `setOnline`), три файла на нём. Экран в SSR — `renderScreen` (`src/test/screenState.ts`):
    маршруты приложения без охранника входа, текст без SSR-комментариев.
  - **PV-14** — `DebtPlan` в `SyncDoc.plans` (`defaultSyncDoc`, `known` в `mergeDocs`),
    `Payment.planId`; `finance.ts`: `activePlan`, `pausedGoals`, `planExtra`, `planStep`,
    `planForecast`, `planFact`, `planMonths`, `settlePlans`, `creditMonthPayment` (п. 5 — одно
    правило месяца закрытия для «Кредитов», `monthDues` и плана), `budgetAmounts` с `plans`/
    `payments` и `planExtra`; Р-11 — `lumpPlan` `openEnded` вместо `null`, текст в окне досрочки.
    Стор: `plans`, `activePlan`, `planState`, `choosePlan`, `cancelPlan`, `applyPlanStep`,
    `settlePlan` (после правок долгов, после слияния — в отправляемом документе).
  - **PV-15** — выбор плана в `StrategyCompare` (радио подушки, совет завести подушку,
    пояснение по черновику плана), карточка активного плана; пауза в целях и `GoalDetail`;
    «Досрочно по плану» в Бюджете и Обзоре, строки/сегменты по d1–d4 (п. 7); `/plan` и
    `DebtPlan.vue`, заголовок «План».
  - **PV-16** — `PlanStepAction.vue` («Внести по плану» одним нажатием, лист счёта как в
    `PaidRow`); строка кредита-цели в Капитале, «Изменить режим» → окно досрочки с суммой шага
    и `planId`; Ритуал — корзина «Досрочно по плану», подушка только из плана; шаг месяца и
    строка о пропуске на экране плана.
  - **PV-17** — `pauseShift`, `pauseMissed`, `planSchedule`; экран плана целиком (выигрыш, месяцы,
    «что не ушло в цели», график «по плану», история, Callout завершения); `ScheduleTable.vue`.
- **Отклонения от ТЗ** (минимальные локальные решения):
  1. Н-3: обвязки тестов поменяли механизм, не ожидания — `screenMixin` подаёт поле тому
     компоненту, у которого оно есть, `act` — первому, где есть все его поля; живой калькулятор
     и окно кредита в e2e монтируются отдельно (рендерер на объектах не рисует SSR-собранные
     дочерние). Проверка «без литерального затемнения» (PV-09) смотрит и новые окна.
  2. Н-6: фикстуры — роль `'owner'` → `'member'` (так её и читал `isViewer`), раздел `'cat-rent'`
     → `'d1'`, счёт `'broker'` → `'deposit'` (тест ждёт «не кэш»).
  3. PV-14: у шага `cushion` поле `missing` (текст PV-16 «не хватает N ₸»); `planExtra` принимает
     `payments`, `key` (правило месяца закрытия); шаг — не больше остатка долга; `applied`
     проверяется раньше подушки; «вложить накопленное» — только в месяц старта, как в ТЗ (если
     шаг месяца старта — подушка, взнос не предлагается). `settlePlan` ещё и после `editPaid`
     (правка суммы может закрыть долг). Отменённый план (и старший из двух) получает
     `result.savedInterest` — для строки истории «отменён, сэкономили N».
  4. PV-14: стор финансов импортирует стор входа (viewer) — цикл импортов, работает: вызов только
     внутри действий.
  5. PV-15: `StrategyCompare` остался на пропсах (`plan`, `step`, `canChoose`, событие `choose`) —
     его SSR-тесты без Pinia; в две обвязки тестов добавлен роутер (`RouterLink`). Совет
     «Заведите цель-подушку» — при выбранном «Без подушки» (буквальное «целей нет» при
     `redirected > 0` недостижимо); по умолчанию — «Без подушки» (по названию не угадываем).
     «План выбран в сентябре 2026» (`monthIn` вместо `monthTitle` — падеж). «Вложить накопленное»
     плана не берёт деньги цели-подушки. `/capital?advice=strategy` открывает калькулятор
     (ссылка «Плана нет»). Подпись «Цели» при плане — «взносы целей, кроме тех, что на паузе».
  6. PV-16: «Внести по плану» и «Изменить режим» — под строкой кредита, а не действием `Row`
     справа: на 390px кнопка сжимала подпись кредита в столбик (скриншот стенда). В списке
     применённых досрочек — «· по плану». Лист счёта `PlanStepAction` — третья копия выбора счёта
     (хвост Н-9 подтверждается; `AccountChoice` — RP-10, вне скоупа ТЗ).
  7. PV-17: по Р-8 окно кредита-цели тоже показывает график с шагами плана (ТЗ назвало только
     экран плана). «План» прошлого месяца без факта — оценка по нынешним взносам (снимков по
     месяцам нет); `planSchedule` считает, что подушка добирается за текущий месяц. «Сэкономили»
     плана — только досрочки с `planId` (обычная досрочка, закрывшая последний долг, в итог
     плана не входит).
- **Грабли среды:**
  - Bash-обёртка иногда ломает heredoc с Python (`unexpected EOF while looking for matching ''`)
    — скрипты правок писать файлом (Write) и запускать.
  - `innerText` в Playwright учитывает `text-transform` (заголовки секций — капсом); `money()` —
    с неразрывными пробелами: в скриптах нормализовать.
  - Viewer на моках Go не завести (вход по коду всегда `member`) — роль подменяется в
    `localStorage`, сервер моков push всё равно принимает.
  - Часы телефона на стенде — подмена `Date` в `addInitScript` (следующий месяц PV-16).
- **Верификация:** `vue-tsc -b` (с e2e) ✅; `npm run build` ✅; `npm test` 459/459 (41 файл, весь
  `e2e/` внутри). Браузер (стенд §6, 390px, два профиля + viewer): Н-3 окна Капитала 21/21
  (одна проверка скрипта на неразрывный пробел — экран верен), PV-14 `openEnded` 11/11, PV-15
  31/31, PV-16 22/22 + лист счёта 6/6, PV-17 20/20; ошибок в консоли нет (у viewer 403 первого
  круга синка — хвост «новые семьи»). Скрипты — в scratchpad сессии, в репо не кладутся.
  **CI не запускался**: ветка не запушена (push — с согласия владельца).

## Итоги критика

Критик 2026-09-25 (Opus 5.5, `ultracode`: два прогона ревью субагентами — 6 измерений +
скептики; находки перепроверены по коду). Коммиты (ветка `pv-block-3-debt-plan`, не запушена):
`e744d42` Р-18 (RP-23) · `302a648` и `28b17e1` правки кода и тесты · + коммит документов.

- **Исправлено (деньги и критерии ТЗ):**
  1. Сумма кредита за месяц — одна `creditMonthPayment` для «Кредитов», `monthDues` и плана
     (ТЗ PV-14 п. 5 требовал одно правило; было общим только условие): последний платёж —
     остаток с процентами, отметка другой суммой — сумма отметки.
  2. Шаг месяца один на семью (Р-4): после «Отменить план» и повторного выбора в том же месяце
     (или двух планов офлайн) план просил внести шаг второй раз — `planPrepay` ищет досрочку
     любого плана за месяц.
  3. График плана терял шаг месяца, если платёж уже отмечен «Оплатил» (`creditSchedule`).
  4. Прогноз при долге, где платёж ≤ процентов, писал «не отдадим банку 70 031 511 531 044 ₸» и
     сохранял это в документ — теперь `savedInterest: null`, экран: «экономию не считаем» (Р-11).
  5. Итог плана: экономия удалённого кредита не в счёт (`prepaySaved`); история и Callout — живым
     `planFact` (досрочка партнёра после отмены видна); «завершён» сильнее позднего «отменён»
     при слиянии (Р-5).
  6. Окно досрочки плана: после «Снять» повторная досрочка шла без `planId` (шаг снова «к
     оплате»), при шаге партнёра — вторая запись с `planId`, шаг прилипал к окну другого
     кредита — `planPending` выводится из документа.
  7. Пустой план (всё, кроме подушки, «не останавливать») выбирался и вёл шагом «0 ₸» —
     кнопка неактивна с подсказкой, «0 ₸» нигде не показывается.
  8. Экран плана: «В … досрочки не было» в фазе подушки — убрано; «Сейчас (от факта)» —
     «ещё не отдадим банку» (от нынешних остатков; уже сэкономленное — строкой ниже).
  9. Ритуал: имя долга внесённого шага — из шага (не следующий по ставке); цели на паузе
     больше не обещают «быстрее на N мес.» (добавка уходит в досрочку); подушка меряется тем
     же месяцем списаний, что и шаг плана (`planMandatory`, раньше — суммы разделов).
  10. Подпись «Досрочно по плану» — по составу (паузы и платежи закрытых долгов).
- **Упрощено / реюз:** `budgetLines` — строки «Куда уходит» и сегменты Обзора в одном месте;
  `endedPlan` — одна сборка итога для стора и `settlePlans`; `derivedCredits` в сторе;
  `monthShort` вместо двух копий; сумма пауз в калькуляторе — `planExtra`; `missed` — из
  `planMonths`.
- **Тесты добиты** (секции «Тесты» ТЗ; 459 → 495): п. 5 с планом; «вложить накопленное» вне
  месяца старта и по Алматы; `settlePlan` после «Оплатил»/удаления/ставки 0/снятия отметки;
  счёт шага при двух счетах, «не списывать», история у другого кредита; Р-11 в сторе и окне
  досрочки; `PlanStepAction` (нажатие, лист, «не списывать»); проводка калькулятора в Капитале
  (viewer, `lump` без денег подушки); подушка Ритуала по id; Σ сегментов Обзора = доход;
  переизбрание плана; график с отмеченным платежом; слияние done/cancelled. Пустая проверка в
  `DebtPlan.test.ts` («Цели на паузе» — такого заголовка нет, `indexOf` −1) исправлена;
  `renderScreen` теперь падает, если действие `screenMixin` не нашло компонента.
- **Проверено и верно:** Н-3 — чистый переезд (шаблоны и скрипты окон построчно совпадают,
  тексты и viewer-гейты прежние, Б-15 работает); e2e под `vue-tsc -b`, `happy-dom` только в
  `Sheet.dom.test.ts`; два активных плана офлайн сходятся без пинг-понга; viewer ничего не
  пишет; цикл импортов сторов безопасен; демо без запросов; `resetDoc` шлёт `plans: []`.
- **Не делаем (с причиной):** удаление долга или ставка 0% завершают план как `done` — так в ТЗ
  PV-14 п. 3; `debtFreeMonth` = `addMonths(key, m)` — как «через m мес.» калькулятора (сдвиг на
  месяц зависит от того, отмечен ли платёж месяца); сложение корзин Ритуала (`base + extra`) —
  распределение, как `used`/`left` до блока; `PlanStepAction` сам считает шаг — самостоятелен,
  как `PaidRow`; копии `render` в `Capital.test.ts` и фикстур в `stores/finance.test.ts` —
  тестовая обвязка без выигрыша для денег (кандидат ревью frontend).
- **Хвосты §4 (6, судьбы — владелец):** «Вложить накопленное» учитывается дважды (**важно до
  деплоя**); шаг обрезается остатком долга; подушка в том же месяце; калькулятор против снимка
  прогноза; платежи d4 сверх базы не видны в Бюджете; `pauseShift` для целей, заведённых позже.
  Н-9 (выбор счёта) — подтверждён: лист `PlanStepAction` — третья копия.
- **Доки:** §6 бэклога (сумма кредита за месяц, `budgetLines`, `endedPlan`, `planPrepay`,
  `mergePlan`, `savedInterest | null`); Р-18 — «Что уже есть» в ТЗ RP-23 по факту кода
  (`e744d42`); промпт Блока 4 — факты после Блока 3 (кит окон, окна Целей на самодельном хроме,
  `wishlist` без методов, `updateGoal` без `unchanged`, связь правки цели с шагом плана).
- **Верификация:** `vue-tsc -b` ✅; `vite build` ✅; `npm test` 495/495 (41 файл, весь `e2e/`);
  браузер (стенд §6, 390px, два профиля + viewer) 29/29 — пустой план, фаза подушки, Ритуал,
  Бюджет/Обзор, «Снять» в окне досрочки, Р-11 на экране плана; ошибок в консоли нет. CI не
  запускался — ветка не запушена.

## Приёмка

Приёмка 2026-09-25 (Opus 5.5, `ultracode`: мультиагентный прогон — 5 браузерных проверяющих на
стенде §6 по своим семьям, e2e-писатель, сверка «Тестов» ТЗ с мутациями, скептик на каждый FAIL;
затем финальный последовательный перепрогон всех браузерных скриптов). **Вердикт: принято.**
Расхождений с критериями ТЗ PV-14…PV-17 и брифа нет; новые наблюдения вне ТЗ — хвосты §4 с
судьбами владельца (6 — в клинап этого блока). **L-блок: не деплоим** — дальше ревью `frontend`.

- **Коммиты приёмки** (ветка `pv-block-3-debt-plan`, запушена с согласия владельца):
  `2963a27` сценарии приёмки в e2e · + коммит документов.
- **Верификация (реальная среда):**
  - `npm run build` (`vue-tsc -b` с e2e + `vite build`) ✅; `npm test` **500/500** (41 файл, весь
    `e2e/`; было 495 + 5 сценариев приёмки). Бэкенд, `api/` и React `src/` в диффе блока не
    тронуты (Р-15, Р-2).
  - **CI `ci-frontend`:** ✅ run `36155751936` на `cc9a793` (HEAD критика); run `36159703714` на
    `2963a27` (e2e приёмки) — ✅ success.
  - **Браузер** (стенд §6: Go на моках + Vite, 390px, профили A, B и viewer, часы телефона —
    июль/август/октябрь). Финальный последовательный перепрогон на чистом дереве (сторож:
    `frontend/src` за прогон не менялся) — **466/468**: критерий брифа цепочкой 88/88 (+1
    наблюдение), фаза подушки 4/4, PV-16 100/100, PV-17 114/114 (история, график, конец плана,
    отмена), Р-11 31/32 (гигантское число калькулятора — хвост), viewer 37/37, документ старого
    клиента без `plans` 20/20, регрессия экранов Блоков 1–2 и RP 72/72 (окна кредита, обязательства,
    валютного счёта, досрочки, калькулятор, цели, Ритуал, тема; нет NaN/Infinity, горизонтального
    скролла, ошибок консоли). Оба FAIL — наблюдения ниже, владелец назначил клинап.
  - **Критерий брифа на двух профилях** (UI, числа сверены независимым расчётом): выбор плана с
    подушкой в калькуляторе → `/plan`; у B теги «На паузе ради плана» у Отпуска и Машины, Callout с
    «Взнос 60 000 ₸ идёт в досрочку…», «Досрочно по плану» 100 000 в Бюджете и Обзоре, «Свободно»
    597 000 без изменений → «Внести по плану» одним нажатием со счёта прошлой оплаты (листа нет):
    Кредитка 285 000 → 185 000 и карта 975 000 → 875 000 у обоих, экономия 48 513 одинакова в окне
    досрочки («· по плану»), в «Уже сэкономили» у A и B и в `result` → отмена с подтверждением: у B
    паузы сняты, «Плана нет» и история «отменён, сэкономили 48 513 ₸»; повторный выбор в том же
    месяце шаг не повторяет. Закрытие последнего процентного долга досрочкой — план `done` у обоих,
    Callout «Долги с процентами закрыты — цели возобновились», цели без паузы. Слияние двух
    активных планов офлайн — тест e2e PV-14 зелёный.
  - **Регрессия:** весь `e2e/` (Блоки 0–2 RP и паритета) в `npm test`; браузерная регрессия выше.
  - **Сверка «Тестов» ТЗ:** 87 утверждений — у каждого есть тест, пустых проверок нет; 10
    мутаций денег (повтор шага, «Свободно» с планом, `settlePlans`, освобождение платежа,
    `costliestCredits`, `activePlan`, `plans` вне `known`, `planPrepay` без месяца, подпись шага
    у другого кредита) — все пойманы. Слабые места (для ревью): `resetDoc` проверяется по
    локальному документу, не по телу push; Hero «Свободно в …» Обзора с планом не сравнивается.
  - **e2e приёмки** — `frontend/e2e/pv-block3-debt-plan.test.ts`, describe «Приёмка Блока 3»:
    критерий брифа цепочкой, viewer (Р-12), «Изменить режим» → «снизить платёж» (Р-10), фаза
    подушки (Р-7), пропуск месяца (Р-4, Р-16). Нажатия — настоящими обработчиками экранов; 8
    мутаций продукта ловятся. Поведение хвостов клинапа не фиксируют.
- **Хвосты §4 — судьбы владельца (2026-09-25):** критика — «вложить накопленное» дважды (а),
  остаток месяца вторым шагом, прогноз под кнопкой (а) → **клинап Блока 3**; подушка в том же
  месяце, d4 в Бюджете, `pauseShift` новых целей → принятый долг. Новые от приёмки: гигантские
  проценты калькулятора при незакрываемом долге, под кнопкой после отмены обещан внесённый шаг и
  «вложить 477 000» против записанных 127 000, прокрутка после выбора плана, таблица «План и
  факт» в фазе подушки → **клинап Блока 3**; повторный выбор в том же месяце (факт «—») →
  принятый долг. Хвост 1 воспроизведён с числами: при «Без подушки» и накоплениях сверх месяца
  списаний шаг 157 000 списан с карты, «Накоплено по целям» 350 000 прежнее; после сверки счёта
  с банком «Чистый капитал» вырос бы на `lump`.
- **Отклонения, принятые без возврата:** строка пропуска без года («В августе досрочки не было»,
  `monthIn(…, false)`; ТЗ — `monthIn(прошлый)`) — косметика; «Выигрыш» — «долги с процентами
  закроются в …» вместо «без процентных долгов через …» — смысл тот же; экономия досрочки без
  `planId`, закрывшей последний долг, в итог плана не входит — отклонение 7 Handoff.
- **Грабли среды (для следующих приёмок):** мутационная проверка в рабочем дереве и браузерные
  проверяющие на общем Vite-стенде несовместимы — HMR отдаёт мутанты (окно ~17:43–17:47 дало
  ложные FAIL у двух проверяющих); мутации — на снимке (`git worktree`/`git archive`) или после
  браузера. 409 синка при 3–5 телефонах одной семьи — штатный конфликт, стор сливает. Viewer на
  моках — подмена роли в `localStorage`.
- **Деплой:** нет (L-блок) — в клинапе после ревью `frontend`. Стенд остановлен.

## Итоги пост-приёмки (только L)
