# PV-05 — Вклад: инфляция из общей константы, плашка «считает приложение» (фронт)

Блок 1 · MVP · Каталог: `frontend/` · Зависит от: PV-04 (`INFLATION`) · Роли: все (просмотр)

## Контекст (что уже есть)

- Сверка А-7: `frontend/src/views/Deposit.vue:28` `const inflation = ref(0.08)`; `:43-45`
  `realEffective = realRate(calcResult.effectiveRate, inflation)`; плашка `:207-211` «Реальная
  доходность ниже той, что на витрине» — концовка «…не путать номинал с чистым доходом»
  (React: «…с доходом.»). Второй плашки нет.
- React `src/screens/Deposit.tsx:19` `inflation = settings.inflation` (0.102); `:39`
  `realRate(...)`; плашка `:143-147` — та же, что во Vue (текст сверить дословно); плашка
  `:149-153` (тон `warn` по умолчанию): `<Callout title="Проценты считает приложение, а не
  банк">Формула аннуитета и капитализации работает офлайн, на ваших цифрах. Когда появится
  ИИ-советник, он получит уже посчитанный результат и будет только объяснять его словами —
  считать деньги модели не доверяем.</Callout>`.
- `realRate(nominal, inflation)` — `lib/finance.ts:150`. `INFLATION` — PV-04 (Р-19).
- Тесты: `views/Goals.test.ts:111-131` — `realRate(res.effectiveRate, 0.08)` без стора;
  `:198-234` — SSR `Deposit.vue` («Kaspi Депозит», «Эффективная ставка», …), инфляция и плашки
  не проверяются. Отдельного `Deposit.test.ts` нет.

## Задача

1. `Deposit.vue`: `inflation` → `INFLATION` из `lib/finance.ts` (локальный `ref` убрать; в UI
   инфляция не правится — как в React).
2. Текст первой плашки сверить с React `:143-147` дословно; добавить вторую плашку «Проценты
   считает приложение, а не банк» с текстом выше, сразу после первой.

## Тесты

- `views/Goals.test.ts` (или новый `views/Deposit.test.ts`): SSR вклада — «10,2%» в первой
  плашке, `realRate(effective, INFLATION)` в «настоящих»; заголовок «Проценты считает
  приложение, а не банк» присутствует. Тест `:111-131` — на `INFLATION` вместо `0.08`.

## Критерии приёмки

- Браузер: экран вклада показывает «При инфляции 10,2% …» и обе плашки; цифра «настоящих»
  ниже, чем при 8% (≈ на 2 п.п.).
- `cd frontend && npm run build && npm test` зелёные.

## Вне скоупа

- «Сохранено» на вкладе не гаснет — PV-23 (после `useSavedMark` из PV-09).
- Отбасы (жилищный вклад) — RP-22/RP-23.
