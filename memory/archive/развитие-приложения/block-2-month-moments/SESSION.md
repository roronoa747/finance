# Блок 2 — Моменты месяца · сессии

**Цель блока:** встроить приложение во второй и третий момент месяца — «пришла зарплата» и
«месяц кончается» — и дать спокойное чувство движения: строки прогресса и итог месяца «мы»
без сравнений.

Сессии: исполнитель ✅ · критик ✅ · приёмка ✅ — блок 🏁 2026-09-26

---

## Промпт исполнителя — уровень build · Opus 5 · effort high

```
/worker развитие-приложения 2
```

Блок-специфика:

- Ветка `rp-block-2-month-moments` от `main` (после деплоя Блока 1). Push — с согласия
  владельца; `main` не трогать.
- Задачи по порядку: `RP-10` → `RP-11` → `RP-12` → `RP-13`.
- Опора — записи `payments` Блока 1 (факты кода, сверены критиком Блока 1 2026-09-24):
  - **Запись** `Payment` (`types/finance.ts`): `id`, `kind: 'obligation' | 'credit' | 'prepay'`,
    `targetId`, `period` (`YYYY-MM` месяца по графику; у досрочки — месяц применения),
    `amount` (целое, сколько ушло), `principal?` (тело кредита, снимок), `accountId`
    (`null` — «не списывать»), `by`, `at` (момент оплаты — с ним сравнивается якорь),
    поля досрочки `saved`/`mode`/`prevPayment`/`newPayment`, `updatedAt`/`deletedAt`.
    Список — `SyncDoc.payments?` (старые документы без ключа → `?? []`), в `defaultSyncDoc()`
    и в `known` `mergeDocs` (`mergeList` по id) — новые `kind` туда же, отдельного слияния не надо.
  - **RP-10 — зарплата = новый `kind`** (например `'salary'`, `targetId` = `PersonId`):
    `countedPayments` схлопывает пару `kind:targetId:period` (ранняя по `at`, досрочки не
    схлопываются) — зарплате подходит как есть. **`accountBalance` сейчас вычитает `amount` у
    каждой учитываемой записи со счётом** — знак зачисления вводится там (и только там:
    `shiftedBase` и стор берут остаток из неё). `creditBalance` берёт только `credit`/`prepay`.
    `ScheduledKind = 'obligation' | 'credit'` (аргумент `paidFor`/`markPaid`/`unmarkPaid`) —
    расширить или завести своё действие; запись строит внутренний `newPayment(fields,
    accountId, at?)` стора (счёт по умолчанию — `lastAccountFor(payments, targetId, accounts)`,
    Р-5; без истории — `null`). Снятие — `unmarkPaid` (надгробие на пару), правка суммы/счёта —
    `editPaid(record, {amount, accountId})` (новая запись с `at` исходной).
  - **Якорь** `Account.amountSetAt` ставит только ручной ввод остатка (`setAccountAmount`);
    сдвиги — взнос в цель со счёта, внеплановый доход — `shiftAccountAmount(id, delta)` без
    якоря. Зарплата — запись, а не сдвиг и не `setAccountAmount`.
  - **Функции:** «оплачено за период» — `paidFor(payments, kind, targetId, period)`;
    «следующий платёж» — `nextObligationDue` / `nextCreditDue(x, payments, now = today())` →
    `Due { kind, targetId, period, day, amount }`; `creditDueIn(c, payments, key)` — ждёт ли
    кредит платежа (закрытый — нет, кроме месяца закрытия); `creditBalance` 0 = долг закрыт
    (RP-12: момент — запись, обнулившая остаток). `untilPayday(state)` → `due` (без
    оплаченного), `paid`, `dueTotal`; пункты несут `kind`/`targetId`/`paid`; `state` —
    **геттеры стора** (`householdAccounts`, `credits` — производные остатки) + `payments`, как в
    `Overview.vue`, не сырой `householdDoc` (RP-10 п.5 — «до зарплаты» переключается на
    следующую зарплату — здесь).
  - UI: строка «Оплатил» — `components/PaidRow.vue` (пропсы `kind`, `targetId`, `period`,
    `title`, `note`, `accent`, `clickable`, `more`, `dense`; оценку берёт из обязательства);
    viewer — отметки видны, кнопок нет. Время — `today(d?)`/`monthKey(d?)` по Алматы,
    `atLabel(iso)` — день отметки.
  - **Хвост §4 про закрытый кредит в плане** (`budgetAmounts` d2 и «Свободно» считают платёж
    закрытого долга; Бюджет и Обзор передают ей сырой `householdDoc`; `Ritual` — `credits[0]`)
    касается RP-12 «освободилось N ₸»: **пересчёт плана отдан в PV-01 бэклога
    `паритет-react-vue`** (Р-18 паритета, 2026-09-25) и к старту Блока 2 уже в `main` —
    RP-12 делает только момент «освободилось», `budgetAmounts`/Ритуал не трогает.
  - RP-13 «оплачено за месяц» — по `period`; в записи есть `by`, но в итог разрезы по
    участникам не выводить (Р-22).
- Все новые числа — функции `finance.ts` (итог месяца, доля свободного на зарплату,
  моменты прогресса); компоненты только показывают.
- Среда: локальный стенд (§6), два профиля; для «конца месяца» — мок времени в тестах.
- Верификация: `cd frontend && npm run build && npm test`.

---

## Промпт критика — уровень review · Opus 5 · effort xhigh

```
/critic развитие-приложения 2
```

Блок-специфика:

- Зарплата — зачисление (знак) в производном остатке; дедупликация пары для зарплат;
  кнопка только своему участнику. Сверка остатка после зачисления (якорь), правка и снятие
  зачисления — как у платежей Блока 1 (`editPaid` держит `at` исходной); слияние двух
  офлайн-зачислений — тест в `merge.test.ts`.
- Тексты: без упрёков, без сравнения участников (Р-19, Р-21, Р-22) — прочитать все новые
  строки UI.
- Моменты и итог — вычисляемые, в документ ничего лишнего не пишется.
- Промпт Блока 3 актуализировать по факту кода (что есть у `WishItem` после блоков 1–2).

---

## Промпт приёмки — уровень review · Opus 5 · effort xhigh

```
/accept развитие-приложения 2
```

Блок-специфика:

- Критерии брифа Блока 2: «пришла зарплата» зачисляет на карту и открывает раскладку; в
  конце месяца есть вход в `/ritual`; закрытие долга даёт строку прогресса; итог месяца без
  сравнения участников.
- Регрессия: весь `frontend/e2e/` + `npm test`; CI `ci-frontend` зелёный.
- Сценарии приёмки — в `frontend/e2e/` (файл Блока 2).
- **Деплой (M):** с «да» владельца — записать ID текущего Production-деплоя (откат), merge
  `rp-block-2-month-moments` → `main`, push; смоук на `family-finance-ff.vercel.app`: вход,
  остатки не изменились от деплоя, кнопка «пришла зарплата» у своего участника. **Откат:**
  promote записанного деплоя.
- Следующий шаг — `/worker развитие-приложения 3 ultrathink`.

---

## Handoff (заполняет исполнитель)

Исполнитель — 2026-09-26, Opus 5.5. Старт по Р-17 паритета: Блок 5 паритета 🏁 (судьбы его хвостов
§4 — у владельца, не ворота Блока 2).

- **Коммиты** (репо `finance`, ветка `rp-block-2-month-moments` от `main` `76b53b0`, **не запушена**):
  `b454085` RP-10 · `ea9e694` RP-11 · `94578c9` RP-12 · `2c85f77` RP-13 · `c4b6525` RP-10 (досрочка в
  итоге разового решения) · + коммит доков. Каталог — только `frontend/` (Go не трогался).
- **Что сделано:**
  - **RP-10 «Пришла зарплата».** `Payment.kind: 'salary'` (цель — `PersonId`, период — месяц её дня) в
    том же `payments`; `countedPayments` схлопывает пару как есть. Знак зачисления — только в
    `accountBalance` (`shiftedBase`/стор берут остаток оттуда). `MonthlyKind = ScheduledKind | 'salary'` —
    аргумент `paidFor`/`unmarkPaid`. `finance.ts`: `SALARY_EARLY_DAYS = 3`, `salaryOpen(p, payments, period,
    now)` — окно кнопки (за 3 дня до дня и до конца месяца; следующий месяц — только в окне перед днём, день
    31 → последний день месяца); `salaryFree(d5, people, record)` — доля свободного пропорционально окладам +
    разница пришедшего с окладом (премия целиком в свободное), не меньше 0, целые; `untilPayday` берёт
    ближайшую **непришедшую** зарплату (п. 5). Стор `markSalary(personId, {period, amount, accountId})` —
    оклад месяца, счёт прошлой зарплаты (`lastAccountFor`), пара второй записи не получает; правка —
    `editPaid`, снятие — `unmarkPaid('salary', …)`. UI — `components/SalaryRow.vue` (зеркало `PaidRow`):
    строка «Зарплата · Имя» в Бюджете (календарь и список, вместо `Row`), режим `button` — «Пришла
    зарплата» + «Другая сумма или счёт» в карточке «До зарплаты» на Обзоре. Кнопка — только
    `auth.slot === personId` и не viewer; партнёр видит «пришла <день> · <счёт>» и ✓ без кнопок. Первая
    отметка — лист со счётом (`AccountChoice` с пропсами `label="На какой счёт"`, `none="Не зачислять —
    только отметить"`). После отметки — `/ritual?from=salary&person=&period=`.
  - **Ритуал — источник суммы из адреса** (`Ritual.vue`): без параметров — как было (освободившееся
    обязательство); `from=salary` — `salaryFree`; `from=rest` — сумма остатка (RP-11); `from=credit` —
    закрытый долг (RP-12). Подпись экрана по источнику, пустые состояния по источнику.
  - **RP-11.** `finance.ts` `MONTH_END_DAYS = 3`, `monthEndAsk(answered, now)`. Обзор — карточка «Остались
    деньги с <месяца>?»: поле суммы → «Распределить» (`/ritual?from=rest&amount=&period=`), «Всё ушло», «Не
    сейчас». Ответ помнит **устройство** (`localStorage['ff_month_end']` = месяц ответа): документ не
    трогается, партнёра спросят на его телефоне (у него могут остаться свои деньги). Viewer не видит.
  - **RP-12.** `finance.ts` `progressMoments({credits, goals, payments})` → `Moment[]` (`closed` / `half` /
    `saved`), новые первыми, в документ ничего не пишется. Обзор — «История семьи» (до 5 строк, внизу):
    ««Кредит» закрыт · <день> · освободилось N ₸ в месяц» → `/ritual?from=credit&credit=<id>`; ««Цель»:
    собрали половину»; «Не отдадим банку N ₸ · досрочка в «Кредит»». Без имён, значков, серий.
  - **RP-13.** `finance.ts` `monthSummary(state, key)` → `MonthSummary` (оплачено: число и сумма; пришло
    зарплатой; закрытые долги; досрочки и сэкономленное; взносы и снятия целей; `closest` — «было % →
    стало %»; купленное из списка), `SUMMARY_FIRST_DAYS = 5`, `summaryMonth(now)`. Компонент
    `components/MonthSummaryCard.vue` «Наш <месяц>» — **слот по умолчанию под действие RP-28
    «Разобрать»**; «Итог <месяца раньше>» переключает месяц в той же карточке. Обзор — после вопроса RP-11.
- **Отклонения от ТЗ (с причинами):**
  1. **Разовые источники (зарплата, остаток) в Ритуале пишут разовые взносы**, а не `setGoalMonthly`:
     «Подтвердить» → `contribute(…, 'из зарплаты' | 'из остатка месяца')` + `shiftAccountAmount(счёт,
     −Σ)` — как окно цели со счётом. Прибавка ежемесячного взноса от каждой зарплаты копилась бы месяц за
     месяцем. Счёт — «Откуда отложить в цели» (`AccountChoice`): у зарплаты по умолчанию её счёт, у остатка
     — счёт своей зарплаты; не известен — без выбора «Подтвердить» не активна (иначе деньги посчитались бы
     дважды). Ежемесячные источники (обязательство, закрытый долг) — как было. Эффекты целей/подушки/
     досрочки у разовых считаются как разовый взнос (`prepayOutcome(…, 'once')`).
  2. **Шаг Ритуала:** последнее «+» забирает остаток не кратный 10 000 — доля зарплаты почти никогда не
     кратна шагу, «Подтвердить» была бы недостижима. Затрагивает и прежний источник (раньше на
     некратной сумме Ритуал застревал с «Осталось 5 000 ₸»).
  3. **После разового решения** «На главную» — `router.replace`: «назад» не возвращает в раскладку, второе
     подтверждение не отложит те же деньги ещё раз.
  4. **Кнопка на Обзоре** — только когда «До зарплаты» смотрит на свою зарплату в окне. Если свой день
     прошёл без отметки, «До зарплаты» уже смотрит на следующую — отметить можно в Бюджете (строка ждёт до
     конца месяца, без упрёка).
  5. **Доля свободного** считается от `d5` **текущего** месяца (`budgetAmounts` без параметра месяца): у
     зарплаты следующего месяца, отмеченной заранее (1–3 дня до конца месяца), доля по плану этого месяца
     — расходится только если на стыке месяцев меняются версии сумм.
  6. **RP-12:** кредиты в `progressMoments` — **из документа** (база сверки), не производные: у
     производного закрытого остаток 0, и какая запись его обнулила, не видно (ТЗ говорит «производный
     остаток кредита (закрыт = 0)» — это про факт закрытия, запись ищется по базе). Правило половины
     цели (ТЗ: «зафиксировать правило в тесте»): одна на цель — последнее пересечение вверх и только пока
     цель не ниже половины; цель, начатая с половины, момента не даёт. Долг из активного плана «Сначала
     долги» ведёт на `/plan` («его платёж идёт в следующий долг по плану»), Ритуал `from=credit` для него
     пуст — платёж уже распределён планом (PV-14). Viewer строку не нажимает (раскладка — решение, Р-13).
  7. **RP-13:** в итог сверх минимума ТЗ — «Пришло зарплатой» (вместе, без разреза), «Взяли из целей»
     (честность рядом с «Отложили») и «Купили из списка» (приближение к желаниям-покупкам). Мера
     приближения — цель, ближе всех к сумме среди тех, где в месяце было движение: накопленное на начало
     и конец месяца в процентах. Месяц записи: оплаты — `period`, взносы и закрытие — дата по Алматы,
     покупки — `boughtOn` (ISO или старый «dd.mm.yyyy»).
  8. `AccountChoice` получил пропсы `label`/`none` вместо нового компонента; досрочка Капитала на него не
     переведена (Н-11 ждёт «да» владельца).
  9. Хвосты в §4: старый PWA и зарплата (процедура деплоя), Ритуал не вносит досрочку, «31 сентября» в «До
     зарплаты» (было).
  Мелочи вне хвостов: ключ `ff_month_end` не стирается выходом (настройка устройства: другой аккаунт на
  том же телефоне в том же месяце вопроса не увидит); «История семьи» — 5 последних строк, экрана всей
  истории нет; момента «цель собрана» нет (не в ТЗ).
- **Грабли среды:**
  - На `:5173` — чужой Vite React-проекта `C:\Users\SW\project` (не эталон finance) — не трогал; Vue на
    `:5174`, Go (моки) на `:8080`; `stand.mjs` — копия из scratchpad прошлой сессии с `APP=:5174`.
  - Bash heredoc обрезал дописываемый тест на кавычке — дописывал через Edit (память «heredoc»).
  - `innerText` отдаёт заголовок `Section` в верхнем регистре (CSS) — в скрипте «ИСТОРИЯ СЕМЬИ».
  - `\s` в регэкспе JS схлопывает и неразрывный пробел `money()` — в SSR-тестах нормализовать только
    `[ \t\r\n]`.
  - Обзор прокручивается внутри оболочки: `fullPage` скриншот его не снимает — `scrollIntoView`.
- **Верификация:**
  - `cd frontend && npm run build` ✅ (vue-tsc + vite, предкэш 12); `npm test` — **702/702** (было 655; +47:
    finance 20, merge 1, SSR `SalaryRow` 8, SSR `views/monthMoments` 11, e2e `block2-month-moments` 7).
    Накопленный `e2e/` — 87/87.
  - Браузер (локальный стенд §6, Chrome через playwright-core, 390 px, два профиля + viewer, «сейчас»
    подменено): RP-10 **33/33**, RP-11 **22/22**, RP-12 **17/17**, RP-13 **21/21**; светлая и тёмная тема
    карточек — скриншоты; консоль без ошибок. Скрипты — scratchpad сессии
    (`C:\Users\SW\AppData\Local\Temp\claude\c--Users-SW-finance\48408b5c-e5fd-441f-a96a-e251dac071fd\scratchpad\`:
    `stand.mjs`, `rp10.mjs`…`rp13.mjs`, `rp13-shot.mjs`, `*.png`).
  - CI — не запускался: ветка не запушена (push — с согласия владельца, §5).
  - **Деплой (для приёмки):** до первой «Пришла зарплата» после деплоя — перезапуск PWA на обоих
    телефонах (хвост «старый PWA» §4).

## Итоги критика

Критик — 2026-09-26, Opus 5.5. Дифф `76b53b0..4b4b174` сверен с критериями RP-10…RP-13; коммит критика
`a0429ce` (ветка `rp-block-2-month-moments`, не запушена) + коммит доков.

- **Проверено, в порядке:** знак зачисления — только в `accountBalance` (`shiftedBase`, стор, «До зарплаты»
  берут остаток оттуда); пара `salary:участник:месяц` схлопывается, двойная офлайн-отметка — один раз;
  кнопка — только `auth.slot === personId` и не viewer; действие строки вне её кнопки (`Row`) — «Пришла» не
  открывает `SalaryDialog`; зарплата до ручной сверки не прибавляется, правка держит `at` исходной, снятие
  после сверки остаток не трогает, текст снятия этого не обещает; слияние двух офлайн-зачислений —
  `merge.test.ts`; разовое решение Ритуала сдвигает и личный счёт (`shiftAccountAmount` знает личный
  документ); моменты и итог вычисляемые (e2e: в документе нет новых ключей). Все новые строки UI прочитаны:
  без упрёков, без имён и сравнений участников (Р-19, Р-21, Р-22).
- **Находки и правки (`a0429ce`):**
  1. **Зарплата «+0» одним нажатием.** Оклад месяца 0 (декрет — версия 0): `SalaryRow` показывал «Пришла» и
     одно нажатие писало зачисление 0, а `PaidRow` при сумме 0 кнопку прячет. `salaryOpen` → false при
     `salaryAt ≤ 0` — одно правило для строки Бюджета и карточки «До зарплаты». Тест, мутация (защита снята →
     тест красный), браузер 7/7.
  2. **Реюз:** `monthSummary` заново писал «накоплено в цели на начало месяца» — теперь `goalHaveBefore`
     (PV-14) на `key` и `addMonths(key, 1)`.
  3. Имя теста «по Алматы» совпадает с проверяемым моментом (19:30 UTC).
- **Тесты:** секции «Тесты» всех ТЗ покрыты исполнителем (RP-10: зачисление/повтор/снятие/доля — finance,
  SSR кнопки, e2e; RP-11: функция и SSR окна; RP-12: закрытие/снятие/половина с правилом/SSR; RP-13: набор,
  чужой месяц, надгробия, нет полей по участникам, SSR). Добавлено: e2e «сверка остатка после зачисления →
  правка и снятие не двигают остаток» (блок-специфика), оклад 0 в `salaryOpen`. **703/703**.
- **Хвосты в §4 (3):** решение раскладки не записано — закрытый долг раскладывается повторно (и партнёром),
  к раскладке зарплаты не вернуться; `SalaryRow` копирует лист `PaidRow`; «До зарплаты» пропадает, когда все
  ближайшие зарплаты отмечены заранее.
- **Мелочи без хвоста:** итог прошлого месяца без отметок — «В августе отметок пока нет» («пока» лишнее);
  «Решение записано» у закрытого долга говорит «взносы увеличены», даже если всё ушло в досрочку/«на себя» —
  как у прежнего источника.
- **Доки:** §6 индекса — записи `payments` после Блока 2 (виды, выводимое, источники Ритуала),
  устаревшая строка про разбивку; статус и хвосты §4; `STATE.md`. Других живых доков фронт не держит.
- **Промпт Блока 3 актуализирован** (SESSION.md Блока 3, «Факты кода после Блоков 1–2»): `WishItem` не
  менялся; действия стора желаний уже есть (PV-18) — п. 5 RP-16 сделан; шапка и маршруты после паритета;
  итог месяца и скрытые подарки; режим суммы Ритуала для «это приближает».
- **Для приёмки:** e2e-файл Блока 2 — `frontend/e2e/block2-month-moments.test.ts` (дописывать туда).
  Браузерные скрипты — scratchpad критика
  (`C:\Users\SW\AppData\Local\Temp\claude\c--Users-SW-finance\81d01b81-3cd3-4a65-b5d3-d14c7870e9d0\scratchpad\`:
  `stand.mjs`, `rp10…rp13.mjs` — копии исполнителя, `critic-zero.mjs`).
- **Верификация:** `cd frontend && npm run build` ✅, `npm test` **703/703**; браузер (стенд §6: Go-моки
  `:8080`, Vue `:5174`, 390 px, два профиля + viewer) — повтор сценариев исполнителя на коде критика: RP-10
  33/33, RP-11 22/22, RP-12 17/17, RP-13 21/21; оклад 0 — 7/7. CI не запускался (ветка не запушена).

## Приёмка

Приёмка — 2026-09-26, Opus 5.5. **Вердикт: принято** — критерии RP-10…RP-13 выполнены в реальной
среде, расхождений для возврата нет. Коммит приёмки `a0c1176` (только `frontend/e2e/`).

- **Сверка с критериями ТЗ:**
  - **RP-10** — «Пришла зарплата» зачисляет на карту и открывает раскладку; партнёр видит зачисление
    после синка, кнопку своей зарплаты — только у себя; viewer — без кнопок. ✅
  - **RP-11** — в последние 3 дня месяца (по Алматы) на Обзоре вход в `/ritual` с введённой суммой;
    после ответа («Распределить», «Всё ушло», «Не сейчас») карточка ушла и не возвращается. ✅
  - **RP-12** — последний «Оплатил» по маленькому кредиту даёт строку «История семьи», нажатие ведёт
    в раскладку «освободилось N ₸»; снятие отметки строку убирает у обоих. ✅
  - **RP-13** — «Наш <месяц>» у обоих одинаков слово в слово, без имён и оценок; цифры совпали с
    посчитанными **руками** по сиду (не только с `monthSummary`). ✅
  - `npm run build` ✅ (vue-tsc + vite, предкэш 12), `npm test` **706/706** (703 + 3 сценария приёмки),
    весь накопленный `e2e/` — 15 файлов, **91/91** (регрессия Блоков 0–1 и паритета).
- **Браузер** (стенд §6: Go-моки `:8080`, Vue `:5174`, Chrome через playwright-core, 390 px, два
  участника + viewer + второй телефон того же участника, «сейчас» подменено; консоль всех телефонов
  чистая) — **115/115**, сценарии свои, не повтор исполнителя:
  - RP-10 **43/43**: одно нажатие на Обзоре (счёт прошлой зарплаты — без листа) → раскладка с долей
    «Свободно» **с экрана** (572 000 × 7/12 = 333 667); некратный остаток раскладывается до нуля;
    «Решение записано» → «До зарплаты» на Аруну; карта 1 000 000 + 700 000 − 20 000, на сервере база
    сдвинута без нового якоря; у Аруны «пришла 10 сентября · Kaspi Gold» без кнопок; окно её кнопки —
    16-го нет, 17-го есть (первая зарплата — лист «Спрашиваем один раз», Halyk, доля 238 333); премия
    правкой — раскладка второй раз не открывается, +50 000; **два телефона Ильяса жмут «Пришла» без
    связи с сервером** → на сервере обе записи, на карте зачисление одно.
  - RP-11 **35/35**: окно на стыке месяца — 30.09 23:00 по Алматы вопрос есть, 01.10 00:30 (ещё
    30-е по UTC) нет, 27.09 и 28.10 нет; текст без упрёка; «Распределить» неактивна без суммы; 45 000 →
    `/ritual?from=rest…` → в цель со счёта своей зарплаты, взнос «из остатка месяца», ежемесячный
    прежний; в документе нет ключа ответа; у Аруны вопрос остаётся (память устройства), «Не сейчас»
    гасит; **счёт неизвестен → «Выберите, откуда отложить»**, после выбора Halyk сдвинут; ответ за
    сентябрь не гасит октябрь.
  - RP-12 **19/19**: половина цели и досрочка видны до закрытия; «Телефон» закрыт через «Оплатил» →
    первая строка, порядок новые-первыми, без имён, значков и серий; у Ильяса та же история; viewer
    видит, но строка не ведёт; снятие → строки нет у обоих, прямой адрес раскладки — «ещё не закрыт».
  - RP-13 **18/18**: карточка 29.09 — «Оплатили 3 платежа 298 400 ₸» (дубль аренды — раз, надгробие и
    август не в счёт), «Пришло зарплатой 1 200 000 ₸», «Закрыли долг «Телефон»», досрочка 100 000 /
    «не отдадим банку 45 000 ₸», в цели 230 000, из целей 30 000, «Машина» 35% → 55%, купили 2 на
    50 000 (включая старую дату «dd.mm.yyyy»); у Аруны и viewer — та же; «Итог августа» ↔ «Итог
    сентября»; 3.10 и 01.10 00:30 по Алматы — итог сентября; 20.09 и 6.10 — карточки нет.
  - Тёмная тема: новые карточки и раскладка — токены, читается; в диффе блока нет «сырых» цветов.
  - Скрипты и скриншоты — scratchpad приёмки
    (`C:\Users\SW\AppData\Local\Temp\claude\c--Users-SW-finance\0c085704-c5b3-4a03-901c-6ea2fbddd4ab\scratchpad\`:
    `stand.mjs` (+ `storage` телефона, терпеливый `open`), `seed.mjs`, `acc-rp10…13.mjs`, `dark.mjs`,
    `*.png`).
- **Сценарии в `e2e/`** (`block2-month-moments.test.ts`, «приёмка Блока 2», +3): два телефона одного
  участника офлайн — зачисление одно; окно вопроса на стыке по Алматы и память устройства; итог месяца
  на наборе с дублем, надгробием, чужим месяцем и старой датой покупки — числа руками, у обоих
  одинаково. Мутации (`answered` в `monthEndAsk`, старая дата в `boughtMonth`) → тесты красные;
  `finance.ts` восстановлен копией.
- **Наблюдения без возврата** (было до блока или в духе ТЗ): «До зарплаты» у партнёра скрыт, пока до
  чужой зарплаты нет списаний (своему участнику карточку держит кнопка); закрытый в этом месяце кредит
  ещё в «Свободно» месяца закрытия (PV-01), отсюда доля зарплаты меньше на его долю — честно.

**Деплой (M) — точка отката:** Production до merge — **`dpl_Cr2SscecERSVpUZABkiH3Vgdwbvz`** (`main` =
`76b53b0`, код = Блок 5 паритета, READY); равноценный — `dpl_fi2XyakQMoiscc7aPTbjtmxsWiUn` (`5ab521a`).
Откат — promote. Схема БД не меняется, Go не тронут (дифф блока — только `frontend/`). Новое в
документе — записи `payments` с `kind: 'salary'`: клиент Блока 5 их сохраняет (RP-02), но вычитает
как списание — **после деплоя перезапуск PWA на обоих телефонах до первой «Пришла зарплата»** (хвост
§4 «старый PWA»); при откате после первой зарплаты — снять её отметку до promote.

- **Деплой ✅** («да» владельца в этой сессии): push ветки → CI ✅ `ci-frontend`
  [`36239422693`](https://github.com/roronoa747/finance/actions/runs/36239422693) на `a40b015` (со
  сценариями приёмки) → merge `--no-ff` `rp-block-2-month-moments` → `main` = **`fe2952e`** (дерево =
  `a40b015`; `backend/`, `api/`, React `src/`, `go.mod` в диффе `76b53b0..fe2952e` не тронуты), push
  `76b53b0..fe2952e` → Production **`dpl_EkcPQCxs81JVXEgZeh23nLZPN8Ra`** READY; прод отдаёт
  `index-B0z8-49d.js` — сборку приёмки.
  - **Смоук агента ✅ 33/33** (без входа в семью — живые данные не тронуты): `/api/health` →
    `db: connected`; 8 маршрутов и файлов PWA → 200, синк без токена → 401; 7 чанков предкэша SW → 200,
    тексты блока в сборке («Пришла зарплата», «Остались деньги с», «История семьи», «Итог месяца»,
    «освободилось», «Откуда отложить в цели», «Спрашиваем один раз»). Браузер на проде **в демо**
    (Р-32, «сейчас» = 29 сентября): «Остались деньги с сентября?» и «Наш сентябрь» на Обзоре; в Бюджете
    «Пришла» — одна, у своей зарплаты; первая отметка — лист «На какой счёт» → раскладка «Куда
    направить 296 875 ₸» (= 475 000 × 750 000 / 1 200 000); **ни одного запроса к `/api`**, консоль
    чистая. Скрипт — `smoke-prod.mjs` в scratchpad приёмки.
  - **Смоук владельца — открыт:** (1) **перезапустить приложение на обоих телефонах** — до этого
    «Пришла зарплата» не нажимать (старая версия вычтет зарплату со счёта); (2) войти — остатки счетов
    и кредитов те же, что до деплоя; (3) у своей зарплаты в Бюджете («Список») — «Пришла» (кнопка
    видна с 3 дней до дня зарплаты и до конца месяца), у зарплаты партнёра кнопки нет; (4) с 28
    сентября на Обзоре — «Остались деньги с сентября?» и «Наш сентябрь». При расхождении — откат.
- **Откат:** promote `dpl_Cr2SscecERSVpUZABkiH3Vgdwbvz` (или равноценный
  `dpl_fi2XyakQMoiscc7aPTbjtmxsWiUn`) — код Блока 5 паритета; отмеченные зарплаты перед promote
  снять (клиент Блока 5 вычитает их как списание).
- 🏁 Блока 2 в §4. Следующий — Блок 3 «Желания с фото» (L): `/worker развитие-приложения 3 ultrathink`.
