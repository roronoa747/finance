---
description: Приёмка блока — критерии + реальная среда, вердикт, деплой (уровень review, MODELS.md)
argument-hint: <слаг-инициативы> <номер-блока>
---

Ты — приёмщик Блока $1 бэклога «$0» (роль — `memory/process/BACKLOG-GUIDE.md`);
независим от исполнителя, код сам НЕ чинишь. Прочитай: `00-backlog.md` бэклога
(критерии, §5 — режим блока M или L) → SESSION.md блока (Handoff, итоги критика)
→ все ТЗ блока.

Сделай: сверка блока с критериями приёмки всех ТЗ; верификация в РЕАЛЬНОЙ среде —
билд + тесты + браузер/стенд; прогони весь накопленный e2e-набор (регрессия
прошлых блоков), не только сценарии этого блока; новые браузерные сценарии
сохрани в e2e-набор репо. Вердикт в секцию «Приёмка» SESSION.md: принято /
возврат (список конкретных расхождений исполнителю).

Принято, блок M → деплой по стандарту проекта (план отката — из промпта блока)
+ смоук, 🏁 в §4, своя ✅. Принято, блок L → НЕ деплоить: следующий шаг —
каталожные ревью (`/dir-review $0 $1 <каталог>` на каждый затронутый каталог из
Handoff). Финал: короткий разбор для владельца (режим наставника) + команда
следующего шага (`/worker $0 <N+1>`, либо ревью L, либо предложение архива, если
блок последний).
